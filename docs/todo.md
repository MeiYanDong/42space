# Todo Outline

Current priority: keep the single `/opt/42space` program install with isolated profiles and retain strict T+19 split execution. Bot2 and Bot5 both use `1.1gwei`, `0.003 BNB`, a `700ms` private retry lead, randomized T+20.5-21.5 full exit, and fixed T+21 fallback. Their automatic buying is temporarily paused with the profile-local hard buy gate and zero enabled planned buys; discovery, dashboards, and automatic selling remain active.

Immediate execution proof: on `$哈基米 FDV by July 15th, 12:00 UTC?`, Bot2 bought outcomes 1-3 and Bot5 bought outcomes 4-6 at `10U` each in the same T+19 block. Both winning BlockRazor tips mined, no public fallback was used, and RPC-only full exits landed in the T+21 block with all six positions cleared and contiguous nonces.

| Priority | Todo | Status |
| --- | --- | --- |
| 1 | [Single Install Multi Profile Runtime](./todos/07-single-install-multi-profile-runtime.md) | Active; all five profiles remain isolated. Bot2/Bot5 Event and Dashboard units are active, but both buy paths are temporarily hard-paused after the Hakimi split. |
| 2 | [Multi Bot Profile Merge](./todos/06-multi-bot-profile-merge.md) | Complete; canonical repo migration and rollout are done |
| 3 | [Multi Region Execution](./todos/05-multi-region-execution.md) | US West primary live; next proof is real-buy latency |
| 4 | [Execution And Risk](./todos/03-execution-risk.md) | Hakimi proves both profiles' strict T+19 split buy and shared T+20.5-21.5 sell window end to end. |
| 5 | [Discovery And Filtering](./todos/02-discovery-filtering.md) | Bot1/Bot3 exact-score selection remains profile-scoped. Bot2/Bot5 selection rules are retained but their buy hard gates are temporarily closed and all planned rows are disabled. |
| 6 | [Dashboard And Ops](./todos/04-dashboard-ops.md) | Bot1 Strategy shows exact-score five-outcome selection, T+20 Builder execution at `2.1gwei` plus `0.005 BNB`, and randomized T+23-27 full exit. Bot1-Bot5 show separate worker, automatic-buy, and automatic-sell live status with PID/cgroup-validated heartbeat and latest sell-scan evidence. |
| 7 | [Event Intelligence](./todos/08-event-intelligence.md) | First-observed market intelligence sidecar active; Bot2/Bot3/Bot5 Feishu follows the same configurable display filters and covers all non-filtered events; provider proof pending |
| 8 | [Operating Contract](./todos/01-operating-contract.md) | Server deploy proof current |

## Open Items Snapshot

- Current strategy-role boundary: the FIFA/Sports exact-score selector is active only for native Bot3 or an explicit `EVENT_PROFILE_ROLE=bot3_like`. Bot1 uses `bot3_like` with five outcomes x `10U`, planned-buy priority, strict T+20, and randomized T+23-27 full exit; Bot3 remains native T+19 with ten-hour pre-start exit. Historical Bot3-only notes below describe the earlier rollout stage.
- Current automation-health proof: active workers write a 5s profile-local heartbeat and their `/api/automation-status` endpoints report fresh cgroup-matching PIDs. On 2026-07-13 Bot2/Bot5 automatic buying was paused with `MARKET_BUY_QUESTION_ALLOWLIST_REGEX=a^`, all `13/1` previously enabled planned rows were disabled, and restart cleared the prior `6/6` pending/prepared queues to `0/0`. Both Event/Dashboard pairs are active with `NRestarts=0`; automatic selling returned from startup guard to `watching` with errors `0`.
- Builder not-before recovery: the original 24 rounds proved fixed timing unsafe. The legacy Bot2 Guard stress then produced `10/12` target-second Builder inclusions and `0` T-1, but that separate-nonce path is now superseded in production. Bot2 has a dedicated atomic executor at `0x961E5954B4e1812d87955a2b7A614cCc8B1E7Cc1`; global dual Builder is active in strict `builder_only` T+19 mode with a `700ms` retry lead, `0.003 BNB` tip, and `1.1gwei` buy gas.
- Current Bot2 RPC readiness: the disabled Ankr credential was replaced profile-locally without changing Chainstack or trading parameters. HTTP/WSS probes pass and production doctor reports paid broadcast RPC `2/2`, funding/allowance ready, and `blockers=[]`.
- Bot3 dual-Builder atomic rollout: contract `0x14e3f8172388FDDb85918792a63294F4C740343B` is deployed and verified, 48Club and authenticated BlockRazor are active, default tip is `0.003 BNB`, default buy gas is `1.1gwei`, and the old timestamp Guard is disabled. BlockRazor receives `positionFirst`/`noMerge` and uses `maxTimestamp` while the atomic executor supplies the enforceable lower bound. Global strict `builder_only` T+19 mode is enabled and cannot public-fallback.
- Bot1/Bot5 atomic rollout: Bot1 executor `0xe40b5c53d2C6566219f85431a334BA9692d0d6E4` and Bot5 executor `0xe8e714bE74480788cABe470935DEb82236793bc3` are deployed, owner/operator/Router/collateral/runtime hash and max BUSDT allowances are verified. Bot1 remains strict T+20 at `0.005 BNB`/`2.1gwei`; Bot5 matches Bot2's strict T+19 at `0.003 BNB`/`1.1gwei`, T+18.300 private submit, randomized T+20.5-21.5 exit, and fixed T+21 fallback.
- GRVT contract replacement and execution: original `0xFBB0...e302` became `resolved` before open and replacement `0xd283...bB4` opened at `2026-07-12T09:30:00Z`. Address-only plans excluded the retired contract. Bot2/Bot5 bought at T+19 txIndex `1/3`, tips mined at `2/4`, both sold in T+25 blocks, all selected balances are zero, final nonces are `863/863` and `39/39`, and the plans are disabled.
- ARROW split execution: the July 14 market bought Bot2 outcomes 1-3 and Bot5 outcomes 4-6 in T+19, then cleared all six balances. The July 31 market bought only Bot2 outcomes 1-3 in T+19; Bot2's advanced sell target was T+21.979 and RPC accepted it at T+22.032. Bot5's pre-signed outcomes 4-6 transaction did not mine because an external wallet transaction had already converted `66 BUSDT` to USDC, leaving the Router simulation underfunded. The failure was not a nonce collision or shared Builder-token limit.
- Hakimi split execution: market `0x8CEA4bE7Dd0A17131C9De260A4e591DdfA96Ce19` bought Bot5 outcomes 4-6 at txIndex `3` and Bot2 outcomes 1-3 at txIndex `5` in T+19 block `109707822`; their `0.003 BNB` BlockRazor tips followed at txIndex `4/6`. RPC-only sells targeted T+21.014/T+21.033 and both mined in T+21 block `109707827`; final positions are empty and nonces are Bot5 `50/50`, Bot2 `875/875`.
- CRCL dual-profile result: market `0x8511dE73458aB21020A76D78F8f42C2A9442cE22` opened at `2026-07-11T11:30:00Z`. Bot2 tx `0x13df624b64f0b0d475e9db8b61aad36f5a7a80e5120569d6dfb66c58cccab474` ranked first overall in T+19; Bot5 tx `0x16e893838769b65a16fd0633e337ad8d1d95e992a872142a06ef01103b5fc97c` ranked fourth overall and third in the first T+20 block. Each bought the three planned `5U` outcomes. Fast exits failed strict Builder `maxTimestamp` validation and the stale monitor pause delayed actual RPC sells to T+74/T+96; txs `0x672c0ecde842951b5d98302fc1e0531e9bc14f0265d3bf1da1edc04b2c622c2f` and `0xd5c163d8d5b71f1752b10d8b823757430f87373a2616f6cd79c88cf062281276` cleared all positions with contiguous nonces.
- Shared BlockRazor Token: the official sendBundle limits report unlimited BPS and unlimited accepted bundles per block. A six-request concurrent authenticated empty-bundle probe passed `6/6` in `473-558ms`. Sharing the token is supported for the current load, but revocation/account outage remains a shared failure domain; 48Club remains the independent second Builder.
- Builder tip Gas accounting: as of 2026-07-09, mined builder-tip transactions count both the `21000` transfer gas and the BNB tip value through `extraFeeBnb` / `totalFeeBnb`; dashboard/project/aggregate net PnL prefers total fee fields. Bot2 ASML and MU backfill added `0.004042 BNB / 2.3027274U` of paid builder-tip cost; the Bot3 Runner-Up concurrent-mode tip tx remains `not_found` and paid no builder tip.
- Bot2 World Cup Winner volume buy and emergency exit: completed on 2026-07-10. Six ranges bought through Builder at `20U` each, then all six were sold after receipt proof showed block `109095952` had timestamp open+18s despite the bundle's open+19s min/max timestamp. Sell tx `0x30941d9e9c2b998aa9a3b30063e1012dae557506f8d6242897282db4fc2f7eb2` succeeded, all six on-chain balances are zero, the planned-buy row is disabled, and Bot2 event/dashboard are active.
- Fast-buy hot path: retain prebuilt calldata and `minOut=1`; do not add opening-time quote RPCs or re-signing. The abandoned price-protection patch was restored before any production restart.
- CASHCAT Builder proof result: Bot3 buy tx `0x0fdb7e2113d34382bd3b1ca9849e0e8b4caaed60ce1f39847abb0f07376a36d1` succeeded in block `109167936`, timestamp T+20, txIndex `19`, gas `2.1gwei`, and market rank `7`; all three `10U` outcomes are held and auto-sell is disabled. 48Club rejected the early atomic bundle, BlockRazor rejected equal min/max timestamps, neither same-nonce `0.003 BNB` tip candidate mined, and the buy escaped through the preset-overridden public fallback. The plan is disabled and the final nonce is contiguous. Its defects are closed by the later JUGGERNAUT and HOODRAT proofs; do not re-enable Bot3's legacy separate-nonce Guard path.
- Post-fix Bot3 Builder proof: JUGGERNAUT tx `0x599a87003e898d8533050f0136e7c0ffdbb68c6e73d69320092e1758cc7f0906` landed in block `109169931`, T+19, txIndex `2`, market rank 1; BlockRazor tip `0x0c56233df93dd65a137162817b3b24f77ad298de2774a34cc5299b5b04359bf5` landed at txIndex `3`. HOODRAT tx `0x2793e31a0aeb932f9614c79cff7c6b3345d69b4096dbb35d8648c34158b106e1` landed in block `109171931`, T+19, txIndex `1`, market rank 1; BlockRazor tip `0x46088ba0d5d76f5c28e83b2778cbafdbdafc586ebf8a215e5ba5de8c17dd9837` landed at txIndex `2`. Both used `builder_only` and skipped public RPC.
- Bot3 Final Matchup incident: the six-outcome `120U` buy and `0.002 BNB` tip did not mine; only Guard nonce `156` mined. Builder exposed that wallet-budget sizing assigned nearly the whole BNB balance to buy Gas without preserving Guard/tip reserves. The expired plan is disabled and the signer now reserves those costs before computing buy Gas. The wallet's later `130 BUSDT -> 129.8071924 USDC` swap was a separate nonce-157 transaction, not the Bot3 buy.
- Runtime layout: keep the server on one `/opt/42space` code install with isolated `42space`, `42space-2`, `42space-3`, `42space-4`, and staged `42space-5` profile env/data files. Center discovery can share only structured read-only event observations through `EVENT_DISCOVERY_FEED_FILE`; execution RPCs, wallets, nonce, state, and sell monitors stay profile-local. RPC reduction work must preserve the current competitive buy/sell timers, Builder/fanout, nonce, Gas, and trigger semantics.
- Shared RPC observer: address monitoring remains paused. The failed Ankr WSS was removed on 2026-07-14 and replaced with the Bot1 monitor Chainstack WSS; the observer now uses Chainstack WSS plus a temporary one-second Chainstack HTTP audit. All five orderflow readers were cut over one at a time with their own rollback drop-ins, each measured `0 RPC/min` and zero errors, while the shared audit measured `120 RPC/min` with zero errors. Keep this safety cadence for a clean production burn-in, then return the audit to 60 seconds for the final `2 RPC/min` steady state.
- Main auto-sell RPC: all five workers now reuse a positively confirmed operator approval and reduce active stop-loss quotes from three to two chain calls without changing cadence or execution checks. Activation used a cold one-profile-at-a-time rollout; Bot4's two prepared buys were 950 minutes from open and rebuilt successfully, while all five latest/pending nonces remained unchanged and contiguous.
- Gas accounting: on 2026-07-06 a profile-local `GAS_LEDGER_FILE` was added and backfilled from tx hashes in fills/dashboard action logs. Live receipt/action writes now also price BNB gas into USDT from the block-minute BNB/USDT candle, after the France vs Morocco and Bot2 TCC buy/approval/sell entries exposed `gasFeeBnb` present but `gasFeeUsdt=null` or missing approval Gas. Future live coverage now includes ordinary buys, planned buys, auto-sells, fast open-exits and their operator approvals, Router BUSDT approval/reset, market operator approvals, manual minimal approve+buy, manual `event:sell --execute` / Dashboard sells, async receipt timeout classification that later finds a receipt, and the Bot3 Runner-Up orderflow-trigger sidecar. The backfill script can also read 42 REST activity `transactionHash` rows for profiles whose local fills/actions have no tx records. Current production backfill totals are Bot1 `0.0619632213 BNB / 35.932796U`, Bot2 `0.0462023986 BNB / 27.497576U`, Bot3 `0.0406950808 BNB / 22.856756U`, Bot4 `0.0185389834 BNB / 10.316664U`, Bot5 `0.0003858789 BNB / 0.246306U`, and legacy root data `0.0004258247 BNB / 0.280368U`; tx hashes with no transaction receipt are treated as zero-Gas dropped/unmined transactions.
- Bot3: exact-score profile `42space-3` is configured on `47.251.26.212` with public dashboard port `4244` open and verified. The event worker keeps profile-local planned buys as the highest-priority execution path. A Bot3-only FIFA/Sports exact-score auto-buy strategy now exists behind `BOT3_FIFA_EXACT_SCORE_AUTO_BUY_ENABLED`; when enabled on Bot3 only, it parses `A vs B`, requires the ten canonical win-side exact-score outcomes, uses the original three-score price tiers (`1-0/2-0/2-1` versus `0-1/0-2/1-2`) to choose the lower-price non-draw side, then buys that side's five-score basket at `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT=10` per outcome. It skips side markets such as Total Goals/Total Score/Goal Differential/Score Different. Existing manual planned-buy rows still override this selector. Bot3 production default is now `OPEN_BROADCAST_DELAY_MS=18850`, buy `GAS_PRICE_GWEI=1.1`, sell `AUTO_SELL_GAS_PRICE_GWEI=0.15`, global `AUTO_SELL_STRATEGY=pre_start_exit`, `AUTO_SELL_STOP_LOSS_ENABLED=0`, plus `MARKET_BUY_QUESTION_ALLOWLIST_REGEX=a^` for ordinary non-Bot3-FIFA strategy buys. Its global buy transport is now the strict atomic dual-Builder path: `builder_only`, target T+19, `0.003 BNB` tip, authenticated BlockRazor plus 48Club, and no public fallback unless an explicit planned-buy override selects another mode. On 2026-06-26 the USA vs Bosnia and Herzegovina 5-outcome `10U` plan was added, funded, prepared, and bought with the then-current T+18.985s timing. On 2026-06-27, chain and journal evidence showed Bot3 broadcast started at T+18.988, first RPC accepted at T+19.049, and receipt landed in block `106577487`; the earlier buyer `0x3a92A09faA9C1aD28b629c681c850b99607E937d` paid only `0.5gwei` but landed in previous block `106577486` with the same second timestamp, so the miss was propagation/block-boundary timing rather than gas price. A one-off Bot3 OpenRouter Python June 28 planned buy bought `DeepSeek V4 Flash` at `2U` with T+18.900 and `1gwei`, and disabled auto-sell with `hold_to_settlement`. On 2026-06-28 Bot3 bought Mexico vs Ecuador (`MEX 1-0 ECU`, `MEX 2-0 ECU`, `MEX 2-1 ECU`, `5U` each) successfully after correcting the record to exact market `0x519F7d36E0ac447235A4d7E715739bF747D67D8d`; Bot3 then bought Australia vs Egypt at T+18.850 and `1gwei`, ranking `2` in the first 19s block behind a `1.3gwei` same-block buyer, and bought Argentina vs Cabo Verde at T+18.850 and `2.2gwei`, ranking `3` overall. On 2026-06-29 Bot3 bought the non-exact-score `2026 FIFA World Cup 3rd Place ?` market `0x86308B8059183eA443fd1885D5493cF6C5222F1f`, selecting France, Argentina, Spain, England, and Brazil at `10U` each, with T+18.850, `4.4gwei`, and `hold_to_settlement`, ranking `1/9`; later the same day Bot3 bought `2026 FIFA World Cup Runner-Up?` market `0x6B7F30fb52B26814BB49312442010450e43e226D`, selecting France, England, Argentina, and Spain at `30U` each, with T+18.850, `4.4gwei`, and `hold_to_settlement`, ranking `3/9`. On 2026-06-30 Bot3 bought `France vs. Sweden - Handicap` market `0x8f14974D980d971c606a9b2f5862285ADA77C55d`, outcome `France −1.5`, at `10U`, per-record `1.1gwei`, T+18.850, and `pre_start_exit` anchored to kickoff `2026-06-30T21:00:00Z`. The long-dated World Cup baskets do not enter the 10h pre-start auto-sell path. `npm run bot3:buy-rank` and `42space-bot3-buy-rank-evidence.timer` are deployed and write `/opt/42space/output/bot3-buy-rank/latest.{json,md}` only when a new Bot3 buy appears; the latest Runner-Up report is `bot3-buy-rank-evidence-2026-06-29T07-46-29-761Z`.
- Bot3 builder live test: on 2026-07-09, a one-off temp planned buy bought Runner-Up `Spain` for `1U` with a `0.001 BNB` 48Club builder bundle tip configured. Buy tx `0xdd499389bef5d57dd80ed23b1f49c4a5f714cc01eb24ceafbb25982cf0ae5746` succeeded in block `108965568`, txIndex `11`; Ankr accepted in `63ms`, Chainstack in `345ms`, and builder accepted in `359ms`, but tip tx `0xa13884e9bcfba3dc1dc819200b5670b6483cf550ffa065317feeb1ad3e048614` did not mine. Builder defaults remain off globally. Follow-up for concurrent mode: do not treat builder acceptance as guaranteed builder-paid inclusion.
- Builder-only proof: on 2026-07-09, Bot2 ASML used a per-planned-buy `builderBundle.mode="builder_only"` override and paid a `0.002 BNB` 48Club tip. The buy tx `0x6bea5412dd963b8eeb983dbd96a14987569b0eb96391ff71755052a9ea031d6c` and tip tx `0xe0f7512296d56d3ff562b8f1fb1c46b15d402d38cbdabaf2718ebcdc71526d2c` both mined in block `108968007`, txIndex `1` and `2`, proving the builder-paid path works when public fanout is skipped.
- Builder hybrid mode: on 2026-07-09, code and Bot2 config were updated for explicit builder plans to prefer `builder_then_fanout`: the private builder bundle is submitted first, the process waits `120ms`, and only then sends the buy raw transaction through the profile's existing Chainstack/Ankr broadcast RPCs if the builder has not accepted. Bot2 still has no global positive builder tip, so non-builder plans are unchanged.
- Builder hot-path optimization: on 2026-07-09, builder-enabled pre-signed buys were upgraded to pre-sign the `nonce+1` builder tip and prebuild the bundle skeleton before open; broadcast-time builder submission now defaults to skipping the optional max-block RPC lookup and records segment-level builder latency fields for the next live audit.
- Bot3 manual website position enrollment: on 2026-06-30, website-bought `France vs Sweden` positions were added to Bot3 retained auto-sell management without creating a new buy, then updated from `30%` to `10%` retention. The four outcomes `FRA >=4-1 SWE`, `FRA >=4-0 SWE`, `FRA 3-1 SWE`, and `FRA 3-0 SWE` retain `10%` of current chips and sell the remainder under Bot3's 10h `pre_start_exit` rule.
- World Cup orderflow triggers: on 2026-06-30, `42space-bot3-runner-up-orderflow-sell.service` was enabled for Bot3 Runner-Up; on 2026-07-07 the same matched-outcome sell strategy was expanded to Bot3 3rd Place and to Bot1 Runner-Up/3rd Place. Each service watches current held token IDs for that profile/market, including website/manual buys that did not create Bot fill records, sells `100%` of the matched held outcome, and keeps failed watched transactions retryable instead of marking them handled before execution completes. Runner-Up and 3rd Place thresholds are now `150U`.
- Bot3 Spain vs Belgium orderflow trigger: `42space-bot3-spain-belgium-orderflow-sell.service` watches Bot3's held exact-score outcomes on market `0x94FA631F5A8d830919db6d5B1571e438f0222Fb0`; an external watched-outcome buy of at least `50U` sells `100%` of the matched Bot3-held outcome.
- Bot3 single-outcome value trigger: `42space-bot3-spain-belgium-esp-2-1-value-sell.service` watches `Spain vs Belgium` tokenId `2048` (`ESP 2-1 BEL`) on market `0x94FA631F5A8d830919db6d5B1571e438f0222Fb0`, quotes the full sell every 5s, and sells 100% when expected sell return reaches `99U`; execution uses `minCollateralOut=99U`.
- Address transaction watch: watchers cover `0x96FDe227f3863812464dC1320B505016837a3650`, `0x1Bc7dF2AA0DBE1a489A7205f2D1fF92C3d51A80b`, and `0x51349f0B9b8C21A34781273e37F16B0233239C41`. Each sends Feishu on direct BSC transactions or token transfers involving the address, then cools down for 10 minutes while continuing to log hits under `/opt/42space/data/42space/`; as of 2026-07-10, nested 42 trades are decoded into buy/sell, event, outcome, actual USDT, position size, and optional BNB input before notification, with meaningful 42 trades allowed to upgrade a generic alert that just started the cooldown. Dashboard 4242 keeps the watched-address activity view.
- Bot3 exact-score auto-buy auto-sell: on 2026-07-06 the automatic exact-score path was aligned with planned-buy `pre_start_exit` semantics. Planned `kickoffAt` still wins; otherwise Sports/FIFA exact-score positions use market `endDate` as the match anchor, so 10h pre-start clearing is 10h before match time rather than 10h before open. Total Goals / Goal Differential / Handicap side markets do not use this fallback.
- Bot4 OpenRouter T+20: the recurring plan buys `Hy3 (free)` at `20U` and `MiMo - V2.5` at `10U`, total `30U`, with `0.5gwei` buy gas. It is strict `builder_only`: pre-signs the atomic call, submits privately to 48Club and authenticated BlockRazor at T+19.300, declares the non-zero `[T+20,T+21)` Builder window, targets exact-second executor `0xC2B2F78C620228Ea8d1B2E155664ceBbc7212148`, pays only the winning same-nonce `0.001 BNB` tip, and never public-fallbacks. BNB/USDT remains a separate T+22 RPC action at `0.15gwei`. OpenRouter early exit is REST-price based: `MiMo - V2.5 >= 0.0017` and `Hy3 (free) >= 0.0020`, each selling `100%`; the monitor uses 1s checks for ten minutes after buy, then 60s, while Beijing 19:00 clears all remaining positions and stop loss remains disabled.
- Bot4 July 14 failure and repair: the old zero-width Builder hints caused both providers to reject the OpenRouter transaction during pre-target simulation; the recovery path then released only the tip nonce while buy nonce `118` remained missing, so BNB re-signing at nonce `119` failed with `gapped-nonce tx`. No buy landed. The deferred exact-second Roadmap is now implemented: exact contract/window enforcement, expiry-time clearing and chain-pending nonce reset, retry-cooldown enforcement, and no late local buy. Contract deployment/approval succeeded, current nonce is contiguous at `120/120`, local/remote verification and readiness pass, and a no-broadcast July 15 real-market pre-sign produced the expected dual-Builder payload. The next opening still needs receipt/rank evidence.
- Bot4 T+20 receipt proof: buy tx `0xb1d3f3a42dd2fb1f00ac63f9fe2dba5f6400732cfc18624d8c91cac5386e57b8` landed in first T+20 block `109263919`, txIndex `1`, and ranked `1/4` overall in the first 60 seconds. 48Club accepted in `239ms`; its `0.001 BNB` tip mined in the same block. BlockRazor timed out, its same-nonce tip was absent, and public broadcast was skipped. The evidence false timeout caused by missing failed-provider ordering fields is fixed; the same receipt now concludes `complete`.
- Current Bot1 execution supersedes the historical RPC notes below: strict dual Builder T+20 is globally enabled with executor `0xe40b5c53d2C6566219f85431a334BA9692d0d6E4`, T+19.500 private submission, `0.005 BNB` tip, `2.1gwei` buy gas, and no public fallback. Bot1 uses `bot3_like` exact-score selection at five outcomes x `10U`, defaults to randomized T+23-27 full exit at `0.15gwei`, keeps stop loss off, and preserves planned-buy priority. The five currently held Runner-Up/3rd Place outcomes retain their event-level ten-hour pre-start overrides and were not sold during cutover. Current BNB is `0.015639099424996188`; top up to at least `0.02 BNB` before the next five-outcome buy because the conservative combined buy-plus-sell requirement is `0.018656 BNB`.
- Current Bot5 execution supersedes the historical funding/RPC notes below: the profile uses its own executor `0xe8e714bE74480788cABe470935DEb82236793bc3`, strict T+19 with T+18.300 private submission, `0.003 BNB`, `1.1gwei`, no public fallback, Bot2-like three-outcome selection, randomized T+20.5-21.5 full exit, fixed T+21 fallback, and 10% stop loss. The Hakimi plan was funded and completed, so the earlier external BUSDT-to-USDC funding blocker is no longer current.
- Bot5: production profile `42space-5` is configured on `47.251.26.212` with dashboard port `4246`, `EVENT_PROFILE_ROLE=bot2_like`, its own Chainstack RPC/WSS, wallet/private key, Feishu webhook, executor, runtime config, follow state, fills, seen keys, auto-sell state, and dashboard action log. It is enabled with `DRY_RUN=0`, `EXECUTE=1`, three outcomes at `10U`, strict T+19, T+18.300 private submit, buy gas `1.1gwei`, `0.003 BNB` tip, randomized T+20.5-21.5 full exit, fixed T+21 fallback, sell gas `0.15gwei`, and -10% full stop-loss. Dashboard and Event services are active with `NRestarts=0`.
- Multi-region: record real broadcast evidence after the next live buy from US West.
- Execution: Bot1 default remains `pre_start_exit`, hold without take-profit sells, clear 10h before kickoff, stop-loss disabled, buy gas `2.2gwei`, sell gas `0.15gwei`, default `OPEN_BROADCAST_DELAY_MS=19850`, and buy broadcast RPC now pinned to single Ankr while Chainstack remains the read RPC. Bot1 bought Mexico vs Ecuador `15U` successfully at T+19.900 with tx `0xf0fa51021088100bec31a61dcbc7502257f6e0cee863b2a47351e9f7a8ffef2f`; Bot1 then bought Spain vs Austria (`ESP 1-0 AUT`, `ESP 2-0 AUT`, `ESP 2-1 AUT`, `5U` each) with a per-record T+19.700 override, tx `0x47c037da917b524a5d6e848ffe5cafd16102ad2a5912f95867b2052dec16948d`, first accepted by Ankr at `56ms`, receipt success in block `106845595`, and positions visible for all three outcomes. Bot1 Australia vs Egypt used the then-current per-record T+19.800 and `2gwei`, started broadcast at T+19.805, first accepted at T+19.851, but landed in block `106913364` with timestamp T+19.000; the same-market evidence ranked Bot1 `3` overall, after two lower-gas 19s-block buyers. Bot1 Argentina vs Cabo Verde used T+19.850 and `2.2gwei`, started broadcast at T+19.859, first accepted by Ankr at T+19.928, and landed in block `106945360` timestamp T+20.000 with txIndex `10`; same-market rank was `5`, behind one 18s tx, two 19s txs, and a lower-gas 20s tx at txIndex `1`. USA vs Bosnia Bot1 evidence showed T+19.985 started fanout at T+19.994, first RPC accepted at T+20.066, and landed in block `106577489` txIndex `9`; lower-gas `0x604C5E022a66c29194ce43C862D5096e7Ac2E99c` paid `1.1gwei` but landed in the same block txIndex `1`. Bot1 production now uses fixed T+19.850; fixed-delay mode cannot guarantee both avoiding 19s premium and ranking first in the first 20s block. Block-aware mode remains local-only and not deployed. After the Argentina buy, Bot1 status shows no future market batch and BUSDT below the generic `45U` funding guard until topped up. Bot2 buy defaults target the first 19s block with `OPEN_BROADCAST_DELAY_MS=18840`, buy gas `1.1gwei`, 2 broadcast RPCs, `REBROADCAST_DURATION_MS=5000`, and `EVENT_OUTCOME_SELECTION=first` / `EVENT_OUTCOME_COUNT=3` for the first three display/token-order outcomes at `10U` each; automatic buying is restored by clearing `MARKET_BUY_QUESTION_ALLOWLIST_REGEX`, while the old Bot2 planned-buy rows remain disabled. The first restored candidate `$FXI FDV by end of July 7th?` pre-signed but did not broadcast because a stale future seen key from the paused period prevented open-broadcast scheduling; this is patched so future eligible automatic buys clear old seen records across startup, discovery, scheduling, and pre-sign. A later same-wallet nonce-652 transaction `0x6a69e8339e74f06967fbea0f22a07993e9384e2197b1372491f1acf33416d0d1` used the nonce and left Bot2 with `0.8276 BUSDT`, so Bot2 is now waiting for BUSDT funding. Bot2 uses the profile-scoped `open_timed_exit` strategy, selling 100% at market-open T+25s while keeping the -10% stop-loss unchanged; the fast randomized open-exit path pre-signs from on-chain balances after buy receipt and targets T+24.5s to T+26.0s, but now respects per-planned-buy `hold_to_settlement`/disabled auto-sell overrides before scheduling. Bot2's 2026-07-03 GRASS one-off bought three middle outcomes at `10U` with `1.1gwei`, then exposed the pre-fix fast-open-exit override gap by selling all three at T+24.740; the row is disabled, GRASS positions show `remainingSize=0`, and the fix is deployed with local/remote `npm run verify` passing. Bot2's 2026-07-03 CCXI one-off bought `$17 – $18`, `$18 – $20`, and `$20– $22` at `5U` each with per-record `1.1gwei`, then auto-sold all three at T+24.822 with sell gas `0.15gwei`; the row is disabled and positions show `remainingSize=0`. Bot2's May 31 auto-sell succeeded after transient positions API 5xx recovery, and short positions API outages are retry-only warnings; the 2026-06-26 PENGU duplicate planned-buy incident is patched by making null per-record delays inherit the profile delay and blocking planned-buy seen overrides after market start; review the next realtime premium watcher close-open report for sub-3s samples after non-blocking REST hydration; verify non-presigned raw-tx rebroadcast and receipt-dropped evidence on the next live miss, add GTA, `$GENIUS`, and PENGU duplicate-prevention replays.
- Bot1 latest planned buy: `2026 FIFA World Cup 3rd Place ?` bought `France`, `Argentina`, `Spain`, `England`, and added `Brazil` at `10U` each with per-record buy gas `4.4gwei`, fixed `openBroadcastDelayMs=19850`, and single-Ankr broadcast. Tx `0xc0ba3f918ddea2b5058fe0057490c5264e96c57d077d6b391dcc3d92d621b632` started broadcast at `2026-06-29T07:30:19.853Z`, first accepted at `07:30:19.901Z`, succeeded in block `107021346`, and ranked `6/9` in the first 60s.
- Bot1 latest planned buy: `2026 FIFA World Cup Runner-Up?` bought `France`, `England`, `Argentina`, and `Spain` at `20U` each with per-record buy gas `4.4gwei`, fixed `openBroadcastDelayMs=19850`, and single-Ankr broadcast. Tx `0xb3d60f7209d115c65e26f22a82861b1115433e0639ae891f38258df012d775f7` started broadcast at `2026-06-29T07:45:19.853Z`, first accepted at `07:45:19.900Z`, succeeded in block `107023346`, and ranked `6/9` in the first 60s.
- Bot1 staged buy: `France vs. Sweden - Handicap` is configured for `France −1.5` at `10U`, per-record buy gas `1.1gwei`, fixed `openBroadcastDelayMs=19850`, and single-Ankr broadcast. The plan is prepared for `2026-06-30T09:15:00Z` but funding-blocked with BUSDT `5.130428114356515073` vs required `10U`; BNB is sufficient.
- Bot1 next single-Ankr test: Colombia vs Ghana exact-score market `0xe3AF7421974F09A340B3B821B8927e73b0C729a6` is staged in Bot1 planned buys for `COL 2-0 GHA`, `COL 1-0 GHA`, and `COL 2-1 GHA` at `5U` each, `openBroadcastDelayMs=19850`, buy gas `2.2gwei`, `pre_start_exit`, stop-loss disabled, and 10h-before-kickoff clearing. Production status readback shows required `15U`, wallet `18.06401318494609335 BUSDT`, `0.015771905447891948 BNB`, allowance/BUSDT/BNB ready, broadcast RPC count `1`, and doctor blockers empty. `42space-bot1-buy-rank-evidence.timer` is enabled to collect the post-buy rank report under `/opt/42space/output/bot1-buy-rank/`.
- Test tooling: make `event:bench` use the same REST fallback as production discovery.
- Discovery: monitor real `rest-discovery-poll` rows after the raw REST discovery fix, especially `knownFutureEligible` for future eligible markets that are known to REST but not yet pending.
- Dashboard: add simple source labels: Website, Chain, Both; verify follow-controlled buy gating, deduplicated project holdings/history expansion, 4242 orderflow monitor edits, and Bot2 non-basic event display on the next live opening.
- Event intelligence: review the next live Bot2/Bot3/Bot5 non-filtered-event Feishu card after the UX revision; configurable display filters default to BTC Price-only, daily fixed templates, Total Goals/Total Score, and Goal/Score Differential, and an empty filter list displays all monitored data-complete events; non-BTC Meme price-range and FDV price-resolution events remain visible/notifiable; strategy question allowlists block auto-buy but do not hide display rows; configure `TWITTERAPI_IO_KEY` and web search credentials on the server; Bot2 and Bot5 buy focus defaults only Meme/Binance strong, excludes low-liquidity Tweet Count from default-follow, and Bot3 must keep that Meme default-follow buy filter disabled unless explicitly requested.
- Ops: keep using `scripts/health_check.sh` for quick status checks; the 2026-06-27 SWAS stall was traced to global OOM under dashboard/premium-watch load, recovered by reboot, and mitigated by persistent swap with `vm.swappiness=20`; add alerting only if manual checks become insufficient.

Update this file after code changes that change priorities or task status.
