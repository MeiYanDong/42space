# Todo Outline

Current priority: keep the single `/opt/42space` program install while running isolated bot profiles for `42space` production, `42space-2` A/B testing, `42space-3` exact-score setup, `42space-4` daily-template OpenRouter Python plus BNB/USDT Daily Futures Volume strategy with Hermes OpenRouter token usage buying paused, and staged `42space-5` Bot2-like execution.

| Priority | Todo | Status |
| --- | --- | --- |
| 1 | [Single Install Multi Profile Runtime](./todos/07-single-install-multi-profile-runtime.md) | Active; templated services are live, Bot3 exact-score worker keeps planned-buy priority and now has a Bot3-only FIFA/Sports exact-score auto-buy switch that selects the lowest price win-side tier at `10U` per outcome; Bot3 default T+18.850s timing, `1.1gwei` default buy gas, `0.15gwei` sell gas, `pre_start_exit`, and stop-loss disabled remain the baseline; Bot3 also completed one-off World Cup 3rd Place and Runner-Up country baskets with `hold_to_settlement`, bought France vs. Sweden Handicap `France −1.5` at `10U` with per-record `1.1gwei`, and now retains `10%` of the four website-bought France vs Sweden exact-score positions; central discovery feed rollout is verified, Bot4 dashboard/worker are created with direct 4245 public dashboard access verified and staggered single planned buys, and Bot5 now has Bot2-like selection/sell policy with T+19.900 buy broadcast, dedicated env/data, direct 4246 plus port-80 public dashboards, and active waiting-for-funds worker |
| 2 | [Multi Bot Profile Merge](./todos/06-multi-bot-profile-merge.md) | Complete; canonical repo migration and rollout are done |
| 3 | [Multi Region Execution](./todos/05-multi-region-execution.md) | US West primary live; next proof is real-buy latency |
| 4 | [Execution And Risk](./todos/03-execution-risk.md) | planned buys, Bot1 fixed T+19.850 exact-score timing with `2.2gwei` and single-Ankr buy broadcast after T+19.800 entered a 19s timestamp block and Argentina vs Cabo Verde still ranked behind a same-20s lower-gas tx, Colombia vs Ghana staged as the next funded Bot1 single-Ankr test with buy-rank timer enabled, Bot2 T+18.840 timing with `1.1gwei` buy gas and automatic buying restored for Meme/Binance strong middle-three `10U` events while old planned-buy rows remain disabled, stale future seen records no longer block restored automatic buys, Bot2 currently waits for BUSDT top-up, Bot3 T+18.850 timing with `1.1gwei` buy gas, Bot4 OpenRouter Python T+19.900 at `0.5gwei` for `DeepSeek V4 Flash`, `Owl Alpha`, and `Hy3 preview`, BNB/USDT T+22.000 at `0.15gwei` on the secondary Bot4 RPC, Hermes OpenRouter token usage paused, active Bot4 daily-template batch cap `50U`, Bot4 post-cutover daily buys now use Beijing 19:00 timed full exit with stop-loss still disabled, pre-start/hold auto-sell, strategy-gated auto-sell return quotes, Bot2 fast randomized open-exit production readback/restart verified, Bot3 retained-settlement sell production config verified, Bot3 buy-rank evidence collector and timer deployed with USA vs Bosnia replay, auto-sell no longer waits behind buy funding, receipt evidence, profile-local Gas ledger/backfill, and realtime premium watcher active |
| 5 | [Discovery And Filtering](./todos/02-discovery-filtering.md) | Raw REST discovery, known-future eligible recheck, duration filter, speed-first fallback, decision log, Bot2/Bot3 configurable display filters plus default display/notification for non-filtered events independent of strategy question allowlists, Bot4 daily-template display include plus OpenRouter Python and BNB/USDT buy-only allowlist while Hermes OpenRouter token usage remains display-only, Bot2-only Meme/Binance default-follow filter, planned-buy override, and event-library verification active |
| 6 | [Dashboard And Ops](./todos/04-dashboard-ops.md) | Independent upcoming/project pages, follow-controlled buy gating, deduplicated holdings/history layout, Gas cost and net PnL display from profile-local ledgers, Bot2/Bot3 concrete checkbox rule summary, Bot4 positive display include summary, non-filtered event display/notification active even when buy allowlists block execution, and production dashboard background refresh disabled for the multi-profile host |
| 7 | [Event Intelligence](./todos/08-event-intelligence.md) | First-observed market intelligence sidecar active; Bot2/Bot3/Bot5 Feishu follows the same configurable display filters and covers all non-filtered events; provider proof pending |
| 8 | [Operating Contract](./todos/01-operating-contract.md) | Server deploy proof current |

## Open Items Snapshot

- Runtime layout: keep the server on one `/opt/42space` code install with isolated `42space`, `42space-2`, `42space-3`, `42space-4`, and staged `42space-5` profile env/data files. Center discovery can share only structured read-only event observations through `EVENT_DISCOVERY_FEED_FILE`; execution RPCs, wallets, nonce, state, and sell monitors stay profile-local.
- Gas accounting: on 2026-07-06 a profile-local `GAS_LEDGER_FILE` was added and backfilled from tx hashes in fills/dashboard action logs. Live receipt/action writes now also price BNB gas into USDT from the block-minute BNB/USDT candle, after the France vs Morocco and Bot2 TCC buy/approval/sell entries exposed `gasFeeBnb` present but `gasFeeUsdt=null` or missing approval Gas. Future live coverage now includes ordinary buys, planned buys, auto-sells, fast open-exits and their operator approvals, Router BUSDT approval/reset, market operator approvals, manual minimal approve+buy, manual `event:sell --execute` / Dashboard sells, async receipt timeout classification that later finds a receipt, and the Bot3 Runner-Up orderflow-trigger sidecar. Current production backfill totals are Bot1 `0.0619632213 BNB / 35.932796U`, Bot2 `0.0462023986 BNB / 27.497576U`, Bot3 `0.0406950808 BNB / 22.856756U`, Bot4 `0.0185389834 BNB / 10.316664U`, Bot5 `0`, and legacy root data `0.0004258247 BNB / 0.280368U`; tx hashes with no transaction receipt are treated as zero-Gas dropped/unmined transactions.
- Bot3: exact-score profile `42space-3` is configured on `47.251.26.212` with public dashboard port `4244` open and verified. The event worker keeps profile-local planned buys as the highest-priority execution path. A Bot3-only FIFA/Sports exact-score auto-buy strategy now exists behind `BOT3_FIFA_EXACT_SCORE_AUTO_BUY_ENABLED`; when enabled on Bot3 only, it parses `A vs B`, requires the six canonical exact-score outcomes and uniform price tiers, buys the lowest-price non-draw win-side tier at `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT=10` per outcome, and skips side markets such as Total Goals/Total Score/Goal Differential/Score Different. Existing manual planned-buy rows still override this selector. Bot3 production default is now `OPEN_BROADCAST_DELAY_MS=18850`, buy `GAS_PRICE_GWEI=1.1`, sell `AUTO_SELL_GAS_PRICE_GWEI=0.15`, global `AUTO_SELL_STRATEGY=pre_start_exit`, `AUTO_SELL_STOP_LOSS_ENABLED=0`, plus `MARKET_BUY_QUESTION_ALLOWLIST_REGEX=a^` for ordinary non-Bot3-FIFA strategy buys. On 2026-06-26 the USA vs Bosnia and Herzegovina 5-outcome `10U` plan was added, funded, prepared, and bought with the then-current T+18.985s timing. On 2026-06-27, chain and journal evidence showed Bot3 broadcast started at T+18.988, first RPC accepted at T+19.049, and receipt landed in block `106577487`; the earlier buyer `0x3a92A09faA9C1aD28b629c681c850b99607E937d` paid only `0.5gwei` but landed in previous block `106577486` with the same second timestamp, so the miss was propagation/block-boundary timing rather than gas price. A one-off Bot3 OpenRouter Python June 28 planned buy bought `DeepSeek V4 Flash` at `2U` with T+18.900 and `1gwei`, and disabled auto-sell with `hold_to_settlement`. On 2026-06-28 Bot3 bought Mexico vs Ecuador (`MEX 1-0 ECU`, `MEX 2-0 ECU`, `MEX 2-1 ECU`, `5U` each) successfully after correcting the record to exact market `0x519F7d36E0ac447235A4d7E715739bF747D67D8d`; Bot3 then bought Australia vs Egypt at T+18.850 and `1gwei`, ranking `2` in the first 19s block behind a `1.3gwei` same-block buyer, and bought Argentina vs Cabo Verde at T+18.850 and `2.2gwei`, ranking `3` overall. On 2026-06-29 Bot3 bought the non-exact-score `2026 FIFA World Cup 3rd Place ?` market `0x86308B8059183eA443fd1885D5493cF6C5222F1f`, selecting France, Argentina, Spain, England, and Brazil at `10U` each, with T+18.850, `4.4gwei`, and `hold_to_settlement`, ranking `1/9`; later the same day Bot3 bought `2026 FIFA World Cup Runner-Up?` market `0x6B7F30fb52B26814BB49312442010450e43e226D`, selecting France, England, Argentina, and Spain at `30U` each, with T+18.850, `4.4gwei`, and `hold_to_settlement`, ranking `3/9`. On 2026-06-30 Bot3 bought `France vs. Sweden - Handicap` market `0x8f14974D980d971c606a9b2f5862285ADA77C55d`, outcome `France −1.5`, at `10U`, per-record `1.1gwei`, T+18.850, and `pre_start_exit` anchored to kickoff `2026-06-30T21:00:00Z`. The long-dated World Cup baskets do not enter the 10h pre-start auto-sell path. `npm run bot3:buy-rank` and `42space-bot3-buy-rank-evidence.timer` are deployed and write `/opt/42space/output/bot3-buy-rank/latest.{json,md}` only when a new Bot3 buy appears; the latest Runner-Up report is `bot3-buy-rank-evidence-2026-06-29T07-46-29-761Z`.
- Bot3 manual website position enrollment: on 2026-06-30, website-bought `France vs Sweden` positions were added to Bot3 retained auto-sell management without creating a new buy, then updated from `30%` to `10%` retention. The four outcomes `FRA >=4-1 SWE`, `FRA >=4-0 SWE`, `FRA 3-1 SWE`, and `FRA 3-0 SWE` retain `10%` of current chips and sell the remainder under Bot3's 10h `pre_start_exit` rule.
- Bot3 Runner-Up orderflow trigger: on 2026-06-30, `42space-bot3-runner-up-orderflow-sell.service` was enabled for `2026 FIFA World Cup Runner-Up?` market `0x6B7F30fb52B26814BB49312442010450e43e226D`. It watches Bot3-held outcomes France, England, Argentina, and Spain, uses a dedicated uncommitted Chainstack read RPC from `/etc/42space/profiles/42space-3-orderflow.env`, sells only the matched watched outcome when an external chain-confirmed buy on that outcome reaches at least `200U`, and keeps failed watched transactions retryable instead of marking them handled before execution completes.
- Bot3 exact-score auto-buy auto-sell: on 2026-07-06 the automatic exact-score path was aligned with planned-buy `pre_start_exit` semantics. Planned `kickoffAt` still wins; otherwise Sports/FIFA exact-score positions use market `endDate` as the match anchor, so 10h pre-start clearing is 10h before match time rather than 10h before open. Total Goals / Goal Differential / Handicap side markets do not use this fallback.
- Bot4: daily-template profile is created on `47.251.26.212` with dashboard port `4245`, `EVENT_DISPLAY_INCLUDE_RULES=daily_fixed_template`, buy-only allowlist for OpenRouter Python and BNB/USDT Futures Daily Volume templates. Active planned buys are OpenRouter Python `DeepSeek V4 Flash`, `Owl Alpha`, and `Hy3 preview` at `10U` each and BNB outcomes `$150M – $300M` plus `$300M – $450M` at `10U` each, so the current active daily-template batch cap is `50U`; Hermes OpenRouter token usage buying is paused after the template/outcome change and removed from the buy allowlist while remaining visible through daily-template display. Current execution is staggered single-market pre-signing with `BUNDLE_DUE_MARKETS=0`: OpenRouter Python uses T+19.900 and `0.5gwei`; BNB overrides to T+22.000, `0.15gwei`, and the secondary Bot4 RPC. `EVENT_OPEN_WINDOW_SECONDS=35` is only stale-buy protection so staggered buys are not rejected as old; sell gas remains `0.15gwei`; post-cutover daily buys use `open_timed_exit` at T+39600s, i.e. Beijing 19:00 for 08:00 daily opens, while old pre-cutover positions are excluded by `AUTO_SELL_APPLY_AFTER_ISO`. Bot4 is publicly reachable through direct `4245`, while the old port-80 fallback has been reassigned to Bot5. `npm run bot4:readiness` plus `scripts/bot4-first-buy-evidence.js` check per-target timing/gas.
- Bot5: production profile `42space-5` is configured on `47.251.26.212` with dashboard port `4246`, `EVENT_PROFILE_ROLE=bot2_like`, its own Chainstack RPC/WSS, wallet/private key, Feishu webhook, runtime config, follow state, fills, seen keys, auto-sell state, and dashboard action log. It now mirrors Bot2-like production selection and sell policy with the operator-adjusted buy broadcast: `DRY_RUN=0`, `EXECUTE=1`, middle-three `10U`, one market per open, `OPEN_BROADCAST_DELAY_MS=19900`, rebroadcast `5000ms`, buy gas `6gwei`, `open_timed_exit` T+25 full exit with sell gas still `0.15gwei`, fast randomized open-exit, and -10% full stop-loss. The profile dashboard is enabled on `127.0.0.1:4246`, public `http://47.251.26.212/` serves Bot5 through `42space-bot5-public-dashboard.service`, and direct public `http://47.251.26.212:4246/` is open through the lightweight cloud server SWAS firewall rule `42space-5-dashboard`. Doctor verified private-key loading, wallet match, HTTP RPC, WSS, and broadcast RPC; allowance is ready, but BUSDT and BNB funding are still blockers, so `42space-event@42space-5.service` is active in waiting-for-funds mode.
- Multi-region: record real broadcast evidence after the next live buy from US West.
- Execution: Bot1 default remains `pre_start_exit`, hold without take-profit sells, clear 10h before kickoff, stop-loss disabled, buy gas `2.2gwei`, sell gas `0.15gwei`, default `OPEN_BROADCAST_DELAY_MS=19850`, and buy broadcast RPC now pinned to single Ankr while Chainstack remains the read RPC. Bot1 bought Mexico vs Ecuador `15U` successfully at T+19.900 with tx `0xf0fa51021088100bec31a61dcbc7502257f6e0cee863b2a47351e9f7a8ffef2f`; Bot1 then bought Spain vs Austria (`ESP 1-0 AUT`, `ESP 2-0 AUT`, `ESP 2-1 AUT`, `5U` each) with a per-record T+19.700 override, tx `0x47c037da917b524a5d6e848ffe5cafd16102ad2a5912f95867b2052dec16948d`, first accepted by Ankr at `56ms`, receipt success in block `106845595`, and positions visible for all three outcomes. Bot1 Australia vs Egypt used the then-current per-record T+19.800 and `2gwei`, started broadcast at T+19.805, first accepted at T+19.851, but landed in block `106913364` with timestamp T+19.000; the same-market evidence ranked Bot1 `3` overall, after two lower-gas 19s-block buyers. Bot1 Argentina vs Cabo Verde used T+19.850 and `2.2gwei`, started broadcast at T+19.859, first accepted by Ankr at T+19.928, and landed in block `106945360` timestamp T+20.000 with txIndex `10`; same-market rank was `5`, behind one 18s tx, two 19s txs, and a lower-gas 20s tx at txIndex `1`. USA vs Bosnia Bot1 evidence showed T+19.985 started fanout at T+19.994, first RPC accepted at T+20.066, and landed in block `106577489` txIndex `9`; lower-gas `0x604C5E022a66c29194ce43C862D5096e7Ac2E99c` paid `1.1gwei` but landed in the same block txIndex `1`. Bot1 production now uses fixed T+19.850; fixed-delay mode cannot guarantee both avoiding 19s premium and ranking first in the first 20s block. Block-aware mode remains local-only and not deployed. After the Argentina buy, Bot1 status shows no future market batch and BUSDT below the generic `45U` funding guard until topped up. Bot2 buy defaults target the first 19s block with `OPEN_BROADCAST_DELAY_MS=18840`, buy gas `1.1gwei`, 2 broadcast RPCs, and `REBROADCAST_DURATION_MS=5000`; automatic buying is restored by clearing `MARKET_BUY_QUESTION_ALLOWLIST_REGEX`, while the old Bot2 planned-buy rows remain disabled. The first restored candidate `$FXI FDV by end of July 7th?` pre-signed but did not broadcast because a stale future seen key from the paused period prevented open-broadcast scheduling; this is patched so future eligible automatic buys clear old seen records across startup, discovery, scheduling, and pre-sign. A later same-wallet nonce-652 transaction `0x6a69e8339e74f06967fbea0f22a07993e9384e2197b1372491f1acf33416d0d1` used the nonce and left Bot2 with `0.8276 BUSDT`, so Bot2 is now waiting for BUSDT funding. Bot2 uses the profile-scoped `open_timed_exit` strategy, selling 100% at market-open T+25s while keeping the -10% stop-loss unchanged; the fast randomized open-exit path pre-signs from on-chain balances after buy receipt and targets T+24.5s to T+26.0s, but now respects per-planned-buy `hold_to_settlement`/disabled auto-sell overrides before scheduling. Bot2's 2026-07-03 GRASS one-off bought three middle outcomes at `10U` with `1.1gwei`, then exposed the pre-fix fast-open-exit override gap by selling all three at T+24.740; the row is disabled, GRASS positions show `remainingSize=0`, and the fix is deployed with local/remote `npm run verify` passing. Bot2's 2026-07-03 CCXI one-off bought `$17 – $18`, `$18 – $20`, and `$20– $22` at `5U` each with per-record `1.1gwei`, then auto-sold all three at T+24.822 with sell gas `0.15gwei`; the row is disabled and positions show `remainingSize=0`. Bot2's May 31 auto-sell succeeded after transient positions API 5xx recovery, and short positions API outages are retry-only warnings; the 2026-06-26 PENGU duplicate planned-buy incident is patched by making null per-record delays inherit the profile delay and blocking planned-buy seen overrides after market start; review the next realtime premium watcher close-open report for sub-3s samples after non-blocking REST hydration; verify non-presigned raw-tx rebroadcast and receipt-dropped evidence on the next live miss, add GTA, `$GENIUS`, and PENGU duplicate-prevention replays.
- Bot1 latest planned buy: `2026 FIFA World Cup 3rd Place ?` bought `France`, `Argentina`, `Spain`, `England`, and added `Brazil` at `10U` each with per-record buy gas `4.4gwei`, fixed `openBroadcastDelayMs=19850`, and single-Ankr broadcast. Tx `0xc0ba3f918ddea2b5058fe0057490c5264e96c57d077d6b391dcc3d92d621b632` started broadcast at `2026-06-29T07:30:19.853Z`, first accepted at `07:30:19.901Z`, succeeded in block `107021346`, and ranked `6/9` in the first 60s.
- Bot1 latest planned buy: `2026 FIFA World Cup Runner-Up?` bought `France`, `England`, `Argentina`, and `Spain` at `20U` each with per-record buy gas `4.4gwei`, fixed `openBroadcastDelayMs=19850`, and single-Ankr broadcast. Tx `0xb3d60f7209d115c65e26f22a82861b1115433e0639ae891f38258df012d775f7` started broadcast at `2026-06-29T07:45:19.853Z`, first accepted at `07:45:19.900Z`, succeeded in block `107023346`, and ranked `6/9` in the first 60s.
- Bot1 staged buy: `France vs. Sweden - Handicap` is configured for `France −1.5` at `10U`, per-record buy gas `1.1gwei`, fixed `openBroadcastDelayMs=19850`, and single-Ankr broadcast. The plan is prepared for `2026-06-30T09:15:00Z` but funding-blocked with BUSDT `5.130428114356515073` vs required `10U`; BNB is sufficient.
- Bot1 next single-Ankr test: Colombia vs Ghana exact-score market `0xe3AF7421974F09A340B3B821B8927e73b0C729a6` is staged in Bot1 planned buys for `COL 2-0 GHA`, `COL 1-0 GHA`, and `COL 2-1 GHA` at `5U` each, `openBroadcastDelayMs=19850`, buy gas `2.2gwei`, `pre_start_exit`, stop-loss disabled, and 10h-before-kickoff clearing. Production status readback shows required `15U`, wallet `18.06401318494609335 BUSDT`, `0.015771905447891948 BNB`, allowance/BUSDT/BNB ready, broadcast RPC count `1`, and doctor blockers empty. `42space-bot1-buy-rank-evidence.timer` is enabled to collect the post-buy rank report under `/opt/42space/output/bot1-buy-rank/`.
- Test tooling: make `event:bench` use the same REST fallback as production discovery.
- Discovery: monitor real `rest-discovery-poll` rows after the raw REST discovery fix, especially `knownFutureEligible` for future eligible markets that are known to REST but not yet pending.
- Dashboard: add simple source labels: Website, Chain, Both; verify follow-controlled buy gating, deduplicated project holdings/history expansion, and Bot2 non-basic event display on the next live opening.
- Event intelligence: review the next live Bot2/Bot3/Bot5 non-filtered-event Feishu card after the UX revision; configurable display filters default to BTC Price-only, daily fixed templates, Total Goals/Total Score, and Goal/Score Differential, and an empty filter list displays all monitored data-complete events; non-BTC Meme price-range events remain visible/notifiable; strategy question allowlists block auto-buy but do not hide display rows; configure `TWITTERAPI_IO_KEY` and web search credentials on the server; Bot2 and Bot5 buy focus defaults only Meme/Binance strong, excludes low-liquidity Tweet Count from default-follow, and Bot3 must keep that Meme default-follow buy filter disabled unless explicitly requested.
- Ops: keep using `scripts/health_check.sh` for quick status checks; the 2026-06-27 SWAS stall was traced to global OOM under dashboard/premium-watch load, recovered by reboot, and mitigated by persistent swap with `vm.swappiness=20`; add alerting only if manual checks become insufficient.

Update this file after code changes that change priorities or task status.
