# Execution And Risk Todo

## Done

- [x] Configure 5 selected outcomes per market.
- [x] Configure `5U` per selected outcome.
- [x] Use `lowest_odds` selection.
- [x] Add hard open-window skip behavior.
- [x] Add auto-sell policy concept: `2x` sell `50%`.
- [x] Add dry-run self-test coverage.
- [x] Add startup Router auto-approval before live monitoring.
- [x] Allow partial same-start batch execution when funds cover only some complete markets.
- [x] Remove fund-binding semantics: live execution recalculates wallet budget and discards stale pre-signed bundles.
- [x] Add buy/auto-sell transaction lock in the long-running process.
- [x] Pause auto-sell after hot buy pre-signing until the open window has passed.
- [x] Force manual sell quote path to dry-run even when dashboard inherits production execute env.
- [x] Preserve speed-first token-order fallback while keeping strict odds as an opt-in mode.
- [x] Make pre-sign, due, catch-up, and retry dry-run tests use the production discovery model: chain first, REST future fallback.
- [x] 2026-05-27 dry-run chain verification passed for pre-sign cache, due drain, catch-up, deadline skip, and retry classifier.
- [x] 2026-05-27 server deployment verification passed on `47.83.23.65`: local verify, remote verify, service restart, health check, dry-run pre-sign/due/catch-up/retry.
- [x] Move open snipe defaults to T-60s hot/pre-sign, T-750ms prebroadcast, dedicated Chainstack/Ankr broadcast, and short raw-tx rebroadcast.
- [x] Raise speed-first default gas to `2.0 gwei` after same-market audit showed first-block buyers at `1.55 gwei`.
- [x] Make partial funding BNB-aware so higher gas settings do not stop monitoring when at least one complete market can still execute.
- [x] Clean test decision-log artifacts after dry-run validation so dashboard activity does not present tests as real buys.
- [x] Persist broadcast timing, first accepted RPC timing, gas, and nonce in buy results for post-open speed audits.
- [x] 2026-07-09 add an optional 48Club builder bundle submission path for fast buys. When `BUILDER_BUNDLE_ENABLED=1` and `BUILDER_BUNDLE_TIP_BNB` is positive, the hot path signs a `nonce+1` BNB transfer to the builder control address, submits `[buyRawTx, tipRawTx]` through `eth_sendBundle`, keeps standard RPC fanout restricted to the original buy raw transaction, persists builder submission/tip fields in the buy result, includes the tip value plus tip-transfer gas in BNB funding reserve, and records the tip tx receipt/Gas ledger when it lands. Planned buys can override the builder settings with a nested `builderBundle` object. Local `npm run verify` and a no-transaction 48Club `eth_chainId=0x38` smoke test passed; the feature remains default-off until a profile or planned buy explicitly enables it.
- [x] 2026-07-09 run a Bot3 live builder test on `2026 FIFA World Cup Runner-Up?` / `Spain`: one-off temp planned buy selected only `Spain`, bought `1U`, submitted a `0.001 BNB` 48Club builder tip bundle, and kept Bot3 global builder defaults off. Public fanout succeeded first: tx `0xdd499389bef5d57dd80ed23b1f49c4a5f714cc01eb24ceafbb25982cf0ae5746`, nonce `152`, block `108965568`, txIndex `11`, gasUsed `389304`, effective gas `1.1gwei`, first accepted by Ankr in `63ms`; Chainstack accepted in `345ms`. 48Club accepted the bundle in `359ms` with tip tx `0xa13884e9bcfba3dc1dc819200b5670b6483cf550ffa065317feeb1ad3e048614`, but the tip tx was not found after the configured max block. The test exposed that concurrent public fanout does not guarantee builder inclusion or tip payment.
- [x] 2026-07-09 fix builder result persistence after the live test showed `builder-bundle-submitted` was printed but not present in the executed result/fills. `buyOutcomesBatch` and bundle execution now propagate builder submission fields from the broadcast result so future receipt tracking can record `builder-bundle-tip-receipt` automatically. Local and remote `npm run verify` passed, and the deployed Bot3 services were restarted and active.
- [x] 2026-07-09 add `builder_only` mode for 48Club tests and validate it with Bot2 ASML. `BUILDER_BUNDLE_MODE=builder_only` or planned-buy `builderBundle.mode="builder_only"` skips public RPC fanout and fails instead of silently falling back when the builder bundle is not submitted. Bot2's ASML planned buy used a per-record `0.002 BNB` builder tip, bought `$1700 - $1750`, `$1750 - $1800`, and `$1800 - $1850` at `10U` each, and recorded `broadcastMode=presigned_builder_bundle_only`. Buy tx `0x6bea5412dd963b8eeb983dbd96a14987569b0eb96391ff71755052a9ea031d6c` succeeded in block `108968007`, txIndex `1`, nonce `710`, gasUsed `1180591`, gas `1.1gwei`; tip tx `0xe0f7512296d56d3ff562b8f1fb1c46b15d402d38cbdabaf2718ebcdc71526d2c` succeeded in the same block at txIndex `2`, nonce `711`, gasUsed `21000`, gas `1gwei`, value `0.002 BNB`. Local and remote `npm run verify` passed.
- [x] 2026-07-09 add the safer hybrid `builder_then_fanout` mode after ASML proved builder-paid inclusion but missed the first open+19s block. The new mode submits the private buy+tip bundle first, waits `BUILDER_BUNDLE_FANOUT_DELAY_MS` / planned-buy `builderBundle.fanoutDelayMs`, and then broadcasts only the buy raw tx through the profile's existing public broadcast RPCs if builder acceptance is late. The builder max-block lookup now races the configured broadcast RPCs instead of depending on one read RPC, builder keepalive runs only for hot pending records with an enabled positive-tip builder plan, and buy results persist fanout-delay/timed-out audit fields. Local and remote `npm run verify` passed; Bot2 env now defaults explicit builder plans to `builder_then_fanout` with `120ms`, and the ASML builder row was updated from `builder_only` to `builder_then_fanout` without enabling global builder tips.
- [x] 2026-07-09 update the Bot2 `Micron Technology (MU) price range, end of July 15th?` planned buy to the operator's builder-hybrid parameters. Backup `/opt/42space/data/42space-2/planned-buys.json.bak-mu-builder-10u-20260709T112627Z` was created; the row now buys the middle five outcomes `$900 – $950`, `$950 – $1000`, `$1000 – $1050`, `$1050 – $1100`, and `$1100 – $1200` at `10U` each, pins `openBroadcastDelayMs=18840`, buy gas `3.1gwei`, and sets `builderBundle={enabled:true, mode:"builder_then_fanout", tipBnb:"0.002", fanoutDelayMs:120, maxBlocks:6, timeoutMs:800}`. Bot2 event/dashboard services were restarted and readback active with `NRestarts=0`.
- [x] 2026-07-09 optimize the builder hot path after MU showed `143ms` public acceptance but `360ms` builder acceptance. Builder-enabled pre-signed buys now also pre-sign the `nonce+1` builder tip transaction, reserve the tip nonce during the hot window, prebuild the bundle transaction skeleton, and reuse both at broadcast time. The new `BUILDER_BUNDLE_MAX_BLOCK_LOOKUP` / planned-buy `builderBundle.maxBlockLookup` switch defaults to `0`, relying on the 48Club optional `maxTimestamp` validity field instead of making a broadcast-time block-number RPC. Buy results now persist `builderBundleTipPreSigned`, `builderBundlePayloadPrebuilt`, `builderBundleRequestLatencyMs`, `builderBundleTipSignLatencyMs`, `builderBundlePayloadBuildLatencyMs`, and max-block lookup timing fields; receipt tracking resets the runtime nonce after a pre-signed builder tip is missing so public-fallback wins do not leave a durable nonce gap. Local `npm run verify` passed.
- [x] 2026-07-10 add timestamp-constrained builder pre-positioning for Bot4 without changing other profiles. New default-off fields `BUILDER_BUNDLE_EARLY_SUBMIT_LEAD_MS` and `BUILDER_BUNDLE_MIN_TIMESTAMP_OFFSET_MS` let an explicitly configured `builder_then_fanout` plan submit its already signed buy+tip bundle before the public action time while carrying a `minTimestamp` not-before floor. Bot4 OpenRouter uses a `900ms` lead from T+19.900, a T+20.000 floor, and `positionFirst=true`; if early submission fails, the code releases the reserved tip nonce and discards later pre-signed transactions before public fallback. Buy results persist early-submit time/lead and min-timestamp evidence, the Bot4 fallback uses both profile Chainstack endpoints plus 48Club privacy RPC, hot-window keepalive warms each planned record's actual override RPC list, and early-builder records use a dedicated target timer instead of re-entering the generic pending scan that added `64ms` on the July 10 buy.
- [x] 2026-07-10 generalize strict Builder timing across all profiles. The shared policy defaults to `0.001 BNB`, `builder_then_fanout`, `positionFirst`, T+18/T+19 pre-positioning for strict 19s/20s targets, and `minTimestamp=maxTimestamp`; planned buys can override the tip. Failed private submission falls back with the same pre-signed buy tx only, accepted bundles that miss the target second are abandoned with nonce/pre-sign recovery, off-target actions such as T+22 remain RPC-only, and one wallet admits only one strict Builder market per target second.
- [x] 2026-07-10 preserve `positionFirst` and `noMerge` in BlockRazor `eth_sendBundle` payloads while keeping `48spSign` restricted to 48Club. Updated the dual-provider self-test to require both ordering flags and the configured BlockRazor authorization header; local and production `npm run verify` passed, and only Bot3 execution code was deployed.
- [x] 2026-07-10 stage Bot3 `$CASHCAT FDV by July 11th, 12:00 UTC?` market `0x89fEbB51DEbed12cBE082443eb5634B3EdaAc5FC` as the funded dual-Builder production proof. The record buys `$125M - $150M`, `$150M - $200M`, and `$200M - $300M` at `10U` each, uses `2.1gwei`, `builder_only`, strict `first_19s_block`, `positionFirst=true`, `noMerge=true`, and a `0.003 BNB` mutually exclusive provider tip, with auto-sell disabled. Production preflight reported `30U` executable, `33.587824781431722205 BUSDT`, `0.031265234532185482 BNB`, Router allowance ready, and both broadcast RPCs healthy.
- [x] 2026-07-10 capture the CASHCAT live attempt. Atomic buy tx `0x0fdb7e2113d34382bd3b1ca9849e0e8b4caaed60ce1f39847abb0f07376a36d1`, nonce `162`, bought all three requested `10U` outcomes in block `109167936`, timestamp T+20, txIndex `19`, gas `2.1gwei`, gas used `1,084,596`, and market rank `7`. 48Club rejected the early bundle and BlockRazor rejected the equal min/max timestamp window; neither `0.003 BNB` provider tip mined. An unintended public fallback first reached Ankr at T+19.817, proving the requested T+19 Builder path did not succeed. Positions are `3063.7` chips each, the Gas ledger records `0.0022776516 BNB` / `1.31169956U`, latest/pending nonce are both `163`, auto-sell remains disabled, and the executed plan is disabled to prevent a duplicate buy.
- [x] 2026-07-10 fix the two defects exposed by CASHCAT. BlockRazor now receives `positionFirst`/`noMerge`, strips only `48spSign`, and converts an exact equal timestamp window to `maxTimestamp` only because the atomic executor supplies the lower bound. Strict timing presets now preserve `builder_only`; scheduled early submission supports and is reused by that mode, and its broadcast path never public-fans out. Provider-payload, strict-mode, and pre-submission self-tests cover the behavior. Local and production `npm run verify` passed, the two source files were backed up and deployed, and only Bot3's event worker was restarted.
- [x] 2026-07-10 capture the first post-fix strict Bot3 Builder proof with JUGGERNAUT. Buy tx `0x599a87003e898d8533050f0136e7c0ffdbb68c6e73d69320092e1758cc7f0906` landed in block `109169931`, timestamp T+19, txIndex `2`, market rank `1`, and `1.1gwei`; BlockRazor tip tx `0x0c56233df93dd65a137162817b3b24f77ad298de2774a34cc5299b5b04359bf5` paid `0.003 BNB` at txIndex `3`, the 48Club same-nonce tip was absent, and public RPC was skipped.
- [x] 2026-07-10 eliminate the JUGGERNAUT near-simultaneous timer race. The main strict `builder_only` path now registers and persists its in-flight submission promise before awaiting Builder I/O, and the early timer reuses that promise instead of sending duplicate bundles or releasing a mined tip nonce. Concurrent self-test coverage asserts one Builder request for two callers; local and production verification passed and only Bot3 was restarted.
- [x] 2026-07-10 record and fix the JUGGERNAUT auto-sell incident. Disabling its completed buy plan removed the `hold_to_settlement` override, so global `pre_start_exit` sold all four positions in tx `0x66d1032022548de8b0266145dfdc30013a80b01d25802bff5c1a59ed5635c372`, recovering about `4.233915 BUSDT` from the original `20U` stake before Gas. Added `preserveAutoSellAfterDisable`: disabled rows remain buy-ineligible while still supplying their explicit auto-sell override. The behavior is self-tested and deployed.
- [x] 2026-07-10 execute and verify Bot3 HOODRAT as the second strict proof. The plan selected tokenIds `2,4,8,16,32,64` at `10U` each, action T+18.840, target T+19, buy gas `1.1gwei`, and `0.003 BNB` tip with auto-sell disabled. Buy tx `0x2793e31a0aeb932f9614c79cff7c6b3345d69b4096dbb35d8648c34158b106e1` landed in block `109171931`, T+19, txIndex `1`, market rank `1`; BlockRazor tip `0x46088ba0d5d76f5c28e83b2778cbafdbdafc586ebf8a215e5ba5de8c17dd9837` landed at txIndex `2`, the 48Club tip was absent, and public RPC was skipped. The operator then manually sold all six `5686.76`-chip positions through the website in six separate transactions for about `92.028869 BUSDT` total before Gas; this was not Bot3 auto-sell.
- [x] 2026-07-10 deploy Bot2's profile-owned `TimedBuyExecutor` at `0x961E5954B4e1812d87955a2b7A614cCc8B1E7Cc1`. Deployment tx `0x4b5391b4e60e369e8d76ef1fa0c2c7d1bc44d6c5c7eeb2b28b267eb2afa84a11` succeeded in block `109190996`; max BUSDT approval tx `0x960e479a2cc10ee27782e538818dc7b724ef542fdb76a6ce46d885e1dfb1f26a` succeeded in block `109190998`. Runtime code hash, owner, operator, Router, collateral, and allowance all matched.
- [x] 2026-07-10 promote the proven atomic dual-Builder path to the Bot2 and Bot3 global default without touching Bot1/Bot4/Bot5. Both profiles now read `BUILDER_BUNDLE_ENABLED=1`, `builder_only`, `first_19s_block`, `0.001 BNB` tip, `1.1gwei` buy gas, `500ms` preposition lead, `positionFirst=true`, `noMerge=true`, max-block lookup disabled, authenticated BlockRazor enabled, timed executor enabled, and legacy timestamp Guard disabled. Existing per-record planned-buy overrides remain authoritative.
- [x] 2026-07-10 verify the Bot2/Bot3 default rollout. Backups are `/etc/42space/profiles/42space-2.env.bak-20260710145433` and `/etc/42space/profiles/42space-3.env.bak-20260710145433`; the BlockRazor token was copied secret-to-secret and never printed. Local and production `npm run verify` passed. Only Bot2/Bot3 event and dashboard services were restarted; all four are `active/running` with `NRestarts=0`, process-environment readback matches the target policy, executors pass chain verification, and final latest/pending nonces are Bot2 `844/844` and Bot3 `180/180`.
- [x] 2026-07-10 execute Bot2 `2026 World Cup Winner market volume on 42, July 19th?` through the strict Builder path. The plan selected `$60M - $62.5M`, `$62.5M - $65M`, `$65M - $67.5M`, `$67.5M - $70M`, `$70M - $72.5M`, and `$72.5M - $75M` at `20U` each, used `openBroadcastDelayMs=18850`, buy gas `3.1gwei`, `builder_then_fanout`, and a `0.004 BNB` tip, with per-market `autoSell.enabled=false` / `hold_to_settlement`. Production backup is `/opt/42space/data/42space-2/planned-buys.json.bak-world-cup-winner-volume-6x20u-builder-2026-07-10T025542240Z`; only Bot2 event/dashboard services were restarted. 48Club accepted the pre-positioned bundle in `254ms`; buy tx `0x640133d9a7606a0acaef7a49fb3a28ce23fa1328f37355f91c1bd3fa3559ca05` and tip tx `0x04bf61a91b998a9d79d7bf8329aad809c87db5dd87faf50bcbaa8b0a37c5c377` both succeeded in block `109095952` at txIndex `1/2`, with buy effective gas `3.1gwei`.
- [x] 2026-07-10 immediately exit all six Bot2 World Cup Winner volume outcomes after discovering that block `109095952` had chain timestamp open+18s despite the submitted open+19s min/max timestamp. REST had not indexed the new position, so the ordinary `event:sell --all` path returned no positions twice; the emergency path read tokenIds `2,4,8,16,32,64` directly on-chain, quoted them, approved the market operator, and sold them in one Router multicall. Approval tx `0x7eb04536963bcaa2a2dd01dff9771da16b776c84267dfde3879faf7db29c41fe` and sell tx `0x30941d9e9c2b998aa9a3b30063e1012dae557506f8d6242897282db4fc2f7eb2` succeeded; all six balances read back `0`, BUSDT read back `91.485111783702887843`, the plan was disabled with backup `/opt/42space/data/42space-2/planned-buys.json.bak-world-cup-winner-urgent-exit-2026-07-10T030531240Z`, Gas ledger rows were appended, and Bot2 services returned active with `NRestarts=0`.
- [x] 2026-07-10 identify the T+18 root cause from receipt, block, and fill evidence. The old preset submitted at T+18.006 because it applied a `1000ms` lead to the T+19 boundary, 48Club accepted at T+18.260, and the Builder's own marker/buy/tip occupied txIndex `0/1/2`; public RPC was not used. `minTimestamp` did not delay inclusion and `maxTimestamp=T+19` kept the bundle valid.
- [x] 2026-07-10 preserve the speed-first buy path. The proposed Lens quote and hard `minOut` patch was restored before any production restart; direct, planned, automatic, and bundled fast buys remain prebuilt and use `minOut=1` without an opening-time quote RPC.
- [x] 2026-07-10 implement targeted Builder submission with a `300ms` boundary lead. T+19 maps to T+18.700 and T+20 maps to T+19.700; payloads use `maxTimestamp` expiry only. The configured public fallback broadcasts immediately when the same Builder request is still in flight, reuses that promise to prevent a duplicate Builder submission, and records private/public start times separately. Local `npm run verify` passed with 44 event self-tests.
- [x] 2026-07-10 add and execute `scripts/builder-timing-stress-test.js`. The 24-round, `0.003932942 BNB` state-free test used six Lens simulations per transaction, two public RPCs, the production 48Club endpoint, exact socket/receipt/block evidence, a `0.005 BNB` hard cap, and no 42 position changes. The proposed hybrid `300/150ms` path still produced one T-1 Builder inclusion in six rounds.
- [x] Do not deploy the fixed-300ms prototype after the failed hybrid boundary proof. Restore Bot2 worker with profile `BUILDER_BUNDLE_ENABLED=0`, `BUILDER_BUNDLE_TIP_BNB=0`, public double-RPC execution, `latestNonce=pendingNonce`, and `NRestarts=0`.
- [x] 2026-07-10 deploy and verify the enforceable timestamp guard. Contract `0x376ba9bF428F62350256f9aD4f3B5eF48Ae81557` was deployed in block `109107036`; runtime code hash `0x9cf91347050a558485154e83d83a918f40af5e081fe2e9ba7ef46eff126400a5` matches the pinned artifact. The guarded bundle order is `[guard,buy,tip]`, the guard is not revert-allowlisted, and pre-sign verifies deployed bytecode before reserving its nonce.
- [x] 2026-07-10 complete guarded Builder and fallback pressure tests. Three single-attempt probes showed early simulation rejects the guard until Builder chain time is eligible. The retry test then ran 12 heavy canaries from `T-500ms` at `100ms` intervals: `10/12` landed in the exact target second with 48Club marker plus consecutive guard/canary/tip receipts, `2/12` missed with zero paid cost, and T-1 was `0`. Four public-fallback canaries proved both RPCs accept the nonce-gapped buy before target; after chain-time guard release all `4/4` mined (`2` target, `2` target+1), T-1 `0`.
- [x] 2026-07-10 deploy the guarded transport code and Bot2-only capability. Bot2 profile uses `timingMode=auto`, `prepositionLeadMs=500`, `100ms` retry, `50ms` chain-time polling, and the verified guard address, while profile Builder/tip remain `0/0`. Only a planned buy that explicitly sets positive-tip Builder enabled can activate it. The fallback records guard Gas, resets an unused tip nonce, and clears dependent later pre-signs. Local and remote `npm run verify` passed; Bot1-Bot5 workers and Bot2 dashboard are active.
- [x] Retire the pending first-live-proof task for Bot2's legacy separate-nonce Guard. That transport is superseded by Bot2's atomic timed executor and is disabled in production; no new capital should be used to prove the obsolete path.
- [x] 2026-07-06 add profile-local Gas ledger recording for mined buy/sell/operator-approval/manual-chain receipts, and add `gas:backfill` to rebuild historical Gas from tx hashes in fills and dashboard action logs.
- [x] 2026-07-06 close the remaining live Gas recording gaps: manual `event:sell --execute` now writes sell and temporary operator-approval Gas directly, async buy receipt timeout classification writes Gas when it finds a receipt, and the Bot3 Runner-Up orderflow-trigger sidecar writes startup approval plus triggered sell-batch Gas into the same profile-local ledger.
- [x] 2026-07-06 include Router BUSDT approval/reset transactions in live Gas accounting for both manual `event:approve` and startup auto-approval, so allowance maintenance Gas is not left for historical backfill only.
- [x] 2026-07-06 fix Bot2 `$TCC (TCryptochicks) FDV by July 13th, 00:00 UTC?` Gas accounting gap and same-class live gaps. Fast open-exit operator approvals now write Gas from `execution.approval`, manual minimal approve+buy writes Router approval and buy Gas, live receipt pricing and `gas:backfill` both fall back from block hash to block number for BNB/USDT pricing, and duplicate tx summary prefers priced rows. Bot2 backfill wrote three corrected TCC entries: buy `0.0011452991 BNB / 0.66874014U`, fast-open-exit approval `0.0000069441 BNB / 0.00405466U`, and sell `0.00009419985 BNB / 0.05500329U`; Bot2 dashboard now shows total Gas `0.0462023986 BNB / 27.497576U`, `561` Gas tx, and `0` unpriced BNB.
- [x] 2026-07-09 include builder tip transfer value in Gas accounting. `builder_tip` ledger entries now record the normal tip-transfer gas plus `extraFeeBnb` / `totalFeeBnb`, backfill upgrades old tip rows that only had `21000 * gasPrice`, and dashboard/project/aggregate PnL dedupe prefers total fee fields. Production backfill corrected Bot2 ASML tip tx `0xe0f7512296d56d3ff562b8f1fb1c46b15d402d38cbdabaf2718ebcdc71526d2c` to `0.002021 BNB / 1.1523742U` and MU tip tx `0x9db2bd9581a17590dcb2ed7c1b74a8f04bd5345ce45bea84b1ca090e83c16227` to `0.002021 BNB / 1.1503532U`; today's paid builder-tip cost is `0.004042 BNB / 2.3027274U`. The Bot3 Runner-Up concurrent-mode tip tx `0xa13884e9bcfba3dc1dc819200b5670b6483cf550ffa065317feeb1ad3e048614` was still `not_found`, so it paid no tip value.
- [x] Add hot-window background broadcast RPC keepalive so sendRawTransaction does not hit cold RPC connections at open.
- [x] Add single-writer risk rule for US West primary and Hong Kong standby.
- [x] 2026-05-27 US West dry-run flow passed: self-test, presign cache, due drain, catch-up, retry classifier, deadline skip, and RPC warmup; no broadcast.
- [x] 2026-05-27 update stake to `20U` per selected outcome, 5 outcomes per market, `100U` max per market and per same-start batch.
- [x] 2026-05-27 update stake to `50U` per selected outcome, 5 outcomes per market, `250U` max per market and per same-start batch.
- [x] 2026-05-27 switch current production policy back to `20U` per selected outcome, 5 lowest-odds outcomes per market, `100U` max per market.
- [x] Keep `ARM_WAIT_FOR_FUNDING=0` so the bot continues monitoring new markets even when the current wallet cannot fund a `100U` market.
- [x] Put single-market buys and pre-sign nonce reservation behind the same transaction lock used by bundle buys, and recheck auto-sell pause before any auto-sell execution.
- [x] Block Dashboard manual sell execution during the pre-sign/open-buy hot window so a separate sell process cannot steal the buy nonce.
- [x] Block manual real `event:buy` and bundle buy execution after `EVENT_OPEN_WINDOW_SECONDS`; the 60s deadline now applies at the final broadcast entrypoint, not only in the watcher.
- [x] 2026-05-27 tighten `EVENT_OPEN_WINDOW_SECONDS` to `5`; any market not bought within 5 seconds after open is marked skipped and all live buy entrypoints refuse it.
- [x] Keep pre-open raw-tx broadcast as the first-buyer strategy; the GTA failure was OOG after entering the opening block, not proof that prebroadcast should be disabled.
- [x] 2026-05-27 GTA incident trace: failed buy was `txIndex=11` in the opening block, before the first successful buyer at `txIndex=191`, but reverted from `out of gas`; raise 9-outcome single-market dynamic gas from `3.15M` to `4.8M` and add self-test coverage.
- [x] Make funding-blocked logs distinguish BUSDT, allowance, and BNB gas reserve instead of always claiming higher-priority BUSDT allocation.
- [x] Add auto stop-loss: if full-exit quote is down at least 10% from cost basis, sell 100%; stop-loss and take-profit use separate dedupe keys.
- [x] 2026-05-27 raise fast dynamic gas again for OOG safety: 9-outcome single-market path now computes `7.0M`, and 3-market/15-outcome bundle computes `11.7M` under the new caps.
- [x] Block Dashboard manual sell quotes as well as execution during the hot window; default guard is `PRE_SIGN_WINDOW_MS + 15s` before open through `EVENT_OPEN_WINDOW_SECONDS + 15s` after open.
- [x] 2026-05-27 change fast buy gas policy to wallet-budget mode: pre-sign and fallback signing use pending BNB balance / configured gas price as the effective gas limit, so fixed `FAST_GAS_LIMIT` values no longer cap opening buys when BNB can cover more.
- [x] 2026-05-27 replace `2x sell 50%` with new-position ladder exit: buy+30s start, sell 10% of initial outcome tokens every 15s, single-outcome -10% stop-loss sells that outcome fully, and due sells use `minOut=1` without price binding.
- [x] 2026-05-28 `$光源` incident: wallet-budget gas signed `18,012,201`, but BSC rejected tx gas above `16,777,216` before mempool entry; add `FAST_GAS_TX_LIMIT=16777216` and self-test coverage.
- [x] 2026-05-28 tighten new-position ladder exit to buy+10s start and sell 10% of initial outcome tokens every 10s.
- [x] 2026-05-28 `$GENIUS` incident: T-750ms pre-open broadcast landed in the previous BSC block and reverted before market open; make pre-open broadcast opt-in/off by default and discard mined reverted pre-signed raw tx instead of reusing it.
- [x] 2026-05-28 optimize post-open first broadcast: add exact open-time scheduler, short final spin, and bypass the due-time funding RPC when a valid pre-signed raw tx already exists.
- [x] 2026-05-28 detach receipt waiting from the buy transaction lock: fast buy marks RPC-accepted raw tx as submitted, releases the lock immediately, and records receipt success/failure in the background.
- [x] 2026-05-28 fix `presign-test` and `due-test` to use a budget-capped same-start batch and fail hard if no reusable pre-signed transaction exists; tests now cover both single and bundled pre-sign shapes.
- [x] 2026-05-29 harden auto-sell around buys: known pending openings pause operator approvals and sells, operator approval is prewarmed separately, due sells are cross-market chunked, and sell state updates only on successful receipt.
- [x] 2026-05-29 include broadcast-accepted buys in auto-sell eligibility; the actual open-position pull still gates real sell actions, so failed broadcasts do not create sellable positions.
- [x] 2026-05-29 make cancelled follow state a live buy blocker; pending records are dropped and pre-signed raw buys are discarded before due execution.
- [x] 2026-05-30 document the anti-sniping premium probe contract: read-only `simulateMint`, all markets in the next same-start upcoming batch, one lowest-odds outcome per market, and `500ms` sampling around `T+15s` to premium expiry.
- [x] 2026-05-30 add `event:premium-probe`, a read-only anti-sniping premium sampler that writes JSONL and Markdown reports without touching trading state.
- [x] 2026-05-31 document the realtime premium watcher contract: standalone read-only process, continuous WSS/REST discovery, all outcomes, `1U + configured stake` ladder, `500ms` sampling through chain `T+22s`, and baseline-adjusted estimated premium reporting.
- [x] 2026-05-31 add `event:premium-watch`, a standalone read-only realtime premium watcher with raw JSONL, enriched JSONL, Chinese CSV, and Chinese Markdown output.
- [x] 2026-05-31 deploy and start `42space-premium-watch.service` on US West as a separate read-only systemd service; local and remote `npm run verify` passed, and existing event/dashboard services stayed active.
- [x] 2026-05-31 enforce the premium watcher invariant that every observed current/future market gets a probe timer or an explicit `not-scheduled` audit row; quote incompatibility is now recorded inside probe output instead of blocking startup.
- [x] 2026-06-23 add premium watcher notification-only mode (`PREMIUM_WATCH_PROBES_ENABLED=0` / `--no-probes`) so event discovery and Feishu notifications can continue without opening-time `simulateMint` quote sampling.
- [x] 2026-05-31 add `event:premium-chart` and wire `event:premium-watch` to automatically render completed enriched JSONL into a Chinese HTML curve report.
- [x] 2026-05-31 make transient 42 positions API 5xx/429/network/invalid-JSON errors retryable auto-sell skips instead of `自动卖出监控异常`; Bot2 still sold the May 31 BTC Daily Volume position successfully after the API recovered.
- [x] 2026-05-31 set Bot2 Daily Volume anti-snipe timing to strict speed-first `T+19s` fanout with a `T+20s` hard buy cutoff; keep partial-funding behavior.
- [x] 2026-06-03 move current Bot1/Bot2 anti-snipe entry to `T+19.5s` with the existing `T+20s` hard buy-start cutoff after the MU price-range premium audit showed `T+19s` can still sit above the `0.0012` price gate while `T+20s` is effectively normal pricing.
- [x] 2026-06-03 disable Bot2's price gate for the current phase; keep single-market mode and T+19.5 direct pre-signed broadcast.
- [x] 2026-06-06 change Bot2 buy sizing to `middle` outcome selection, 3 selected outcomes, `6U` per outcome, so FDV/market-cap events buy the middle three ranges.
- [x] 2026-06-17 raise Bot2 normal default buy sizing to `10U` per selected outcome with `30U` normal market/batch caps; profile-local planned buys still carry their own explicit stakes and caps.
- [x] 2026-06-06 gate Bot2 ladder auto-sell on 100% profit before selling 10% every 10s; keep the existing single-outcome `-10%` stop-loss unchanged.
- [x] 2026-06-11 make just-in-time `fanout_raw` buys schedule the same short raw-tx rebroadcast loop as pre-signed fanout, and add non-blocking per-RPC fanout settlement logs.
- [x] 2026-06-11 classify async receipt watch failures as `receipt-dropped`, `receipt-timeout`, or `receipt-error` market decisions after probing transaction and receipt state.
- [x] 2026-06-11 move Bot2 production broadcast timing from `T+19.5s` to `T+19.0s` after the FOMA audit showed competing buyers at T+18/T+19 and the operator chose earlier chain position over waiting for the lower T+20 premium window.
- [x] 2026-06-15 add per-market planned buys that carry exact outcome names and stake through prepare, pre-sign, funding checks, and broadcast without changing the global `middle` default.
- [x] 2026-06-16 fix planned-buy stale seen handling so old filtered decisions cannot block future scheduled broadcasts, while past/submitted markets remain duplicate-protected.
- [x] 2026-06-16 fix planned-buy cap propagation so a manual per-outcome stake such as `10U` raises `maxStakeUsdt` for that specific planned execution instead of failing pre-sign under Bot2's normal `6U` cap.
- [x] 2026-06-16 add prepare-time live execution validation for pending plans so stake/cap/risk-ack/private-key mismatches surface before the pre-sign hot window.
- [x] 2026-06-17 add profile-scoped auto-sell controls for sell-only gas price, limited take-profit ladder steps, and pre-market-start remaining-position exit; support per-planned-buy `kickoffAt` and retain `endDate - 45m` as the FIFA exact-score fallback instead of market-open `startDate`.
- [x] 2026-06-17 add per-planned-buy auto-sell overrides so Meme FDV positions can sell half at 100% profit and leave the rest manual while FIFA/World Cup planned buys keep the global pre-start exit.
- [x] 2026-06-17 add `open_timed_exit` as a separate auto-sell strategy so Bot2 can keep T+19s buys and switch to market-open T+36s 100% exits without deleting the ladder strategy or changing Bot1.
- [x] 2026-06-18 apply `autoSellLadderProfitPercent` from runtime config so Bot1 can disable the profit gate for fixed-time half exits while preserving the pre-start remaining-position exit.
- [x] 2026-06-18 fix auto-sell stop-loss return math to use remaining-position cost after partial sells, preventing T+36 half exits from being immediately cleared by a false full-cost stop-loss.
- [x] 2026-06-20 track auto-sell remaining size locally after successful ladder sells, so stale positions API balances cannot make stop-loss compare a half-position quote against full-position cost.
- [x] 2026-06-20 add per-planned-buy `hold_to_settlement` auto-sell opt-out that skips ladder/stop-loss/pre-start exits and sell operator preapproval for settlement/claim tests without changing the profile-level Bot1/Bot2 strategies.
- [x] 2026-06-21 add per-planned-buy `openBroadcastDelayMs` and `pre_start_exit` so selected match markets can buy at T+20s, hold without take-profit sells, keep -10% stop-loss, and clear 10h before kickoff.
- [x] 2026-06-21 stage Bot3 exact-score config by copying current Bot1 runtime config and planned buys into an isolated `42space-3` profile while keeping the event worker disabled.
- [x] 2026-06-22 add six operator-selected Bot3 exact-score planned buys at `10U` per outcome, approve Router allowance, and start the Bot3 worker with the dashboard reporting six upcoming planned markets.
- [x] 2026-06-23 preserve sell-only gas price through dashboard runtime-config saves so `AUTO_SELL_GAS_PRICE_GWEI` is not cleared and auto-sell gas guards do not fall back to buy gas.
- [x] 2026-06-26 add Bot4 daily-template timing/risk guardrails: keep the operator-specified `OPEN_BROADCAST_DELAY_MS=19985` as the real T+19.985s action time, set `EVENT_OPEN_WINDOW_SECONDS=21` only as stale-buy protection, buy OpenRouter `DeepSeek V4 Flash` at `5U` plus BNB/USDT `$150M – $300M` and `$300M – $450M` at `5U` each, and use stop-loss-only auto-sell with no take-profit/scheduled sell.
- [x] 2026-06-27 set Bot4 production buy and sell gas defaults to `0.15gwei`; `/etc/42space/profiles/42space-4.env` and `/opt/42space/data/42space-4/runtime-config.json` were backed up, Bot4 event/dashboard services were restarted, and local dashboard readback showed `gasPriceGwei=0.15` plus `autoSellGasPriceGwei=0.15`.
- [x] 2026-06-27 widen Bot4 stale-buy protection to `EVENT_OPEN_WINDOW_SECONDS=25` without changing `OPEN_BROADCAST_DELAY_MS=19985`. The first live batch proved the BNB/USDT buy at T+20 succeeded, while the OpenRouter second transaction was rejected by Chainstack with `in-flight transaction limit reached for delegated accounts` and then skipped at T+21.001; the wider guard gives same-batch retries room after the first nonce confirms.
- [x] 2026-06-27 enable Bot4 same-action bundle buys by setting `BUNDLE_DUE_MARKETS=1` and keeping `EVENT_PRICE_GATE_ENABLED=0` in `/etc/42space/profiles/42space-4.env`. Backed up the profile as `42space-4.env.bak-bundle-20260627T024719Z`, restarted Bot4 event/dashboard services, and verified the running event process plus startup log show `bundleDueMarkets=true`, `openBroadcastDelayMs=19985`, and buy gas `0.15`. If both daily-template markets are funded, Bot4 now broadcasts one bundled transaction; if only one is affordable, the single-market fallback still buys the affordable record.
- [x] 2026-06-27 switch Bot4 back to independent single-market pre-signing and stagger the two daily templates: disable `BUNDLE_DUE_MARKETS`, keep `EVENT_PRICE_GATE_ENABLED=0`, set profile default `OPEN_BROADCAST_DELAY_MS=19900` and buy gas `3gwei` for OpenRouter, give the BNB/USDT planned-buy record `openBroadcastDelayMs=30000` plus `gasPriceGwei=0.15`, and widen `EVENT_OPEN_WINDOW_SECONDS=35` only so the T+30 BNB buy is not rejected by stale-window protection. Local verify covers planned-buy gas propagation into execution config and funding reserve, and Bot4 readiness/evidence scripts now check per-target timing/gas instead of the old same-action bundle schema.
- [x] 2026-06-27 add Bot4 Hermes OpenRouter token usage planned buy: match `Total Daily Token Usage by Hermes Agent via OpenRouter on ...`, buy outcome `> 900B` at `10U`, broadcast at T+21.000 with `0.15gwei`, keep OpenRouter Python at T+19.900/`3gwei` and BNB/USDT at T+30.000/`0.15gwei`, and raise Bot4 `MAX_BATCH_STAKE_USDT` / runtime `maxBatchStakeUsdt` to `25` for the combined daily-template batch.
- [x] 2026-06-27 raise Bot4 OpenRouter Python planned buy from `5U` to `10U` for `DeepSeek V4 Flash`, keeping T+19.900 and `3gwei`; raise Bot4 `MAX_BATCH_STAKE_USDT` / runtime `maxBatchStakeUsdt` from `25` to `30` for the combined OpenRouter Python + Hermes + BNB/USDT daily-template batch.
- [x] 2026-06-27 lower Bot4 Hermes OpenRouter token usage planned-buy gas from `3gwei` to `0.15gwei` while keeping its `10U` size and T+21.000 broadcast timing; OpenRouter Python remains T+19.900/`3gwei`, and BNB/USDT remains T+30.000/`0.15gwei`.
- [x] 2026-06-27 raise Bot4 BNB/USDT Futures Daily Volume planned buy from `5U` per selected outcome to `10U` per selected outcome for `$150M – $300M` and `$300M – $450M`; raise Bot4 `MAX_MARKET_STAKE_USDT` / runtime `maxMarketStakeUsdt` to `20`, and `MAX_BATCH_STAKE_USDT` / runtime `maxBatchStakeUsdt` to `40`.
- [x] 2026-06-27 pause Bot4 stop-loss clear-out by setting `AUTO_SELL_STOP_LOSS_ENABLED=0` and runtime `autoSellStopLossEnabled=false`; keep buy strategy, staggered timing, sell gas `0.15gwei`, no take-profit ladder, and no scheduled pre-start exit unchanged.
- [x] 2026-06-28 adjust Bot4 daily-template execution: OpenRouter Python now buys `DeepSeek V4 Flash` and `Owl Alpha` at `10U` each, T+19.900 and `3gwei`; Hermes covers the current concrete `>900B` ranges `900B - 950B`, `950B - 1T`, `1T - 1.05T`, and `≥ 1.05T` at `2.5U` each, total `10U`, T+19.900 and `3gwei`, using a per-record secondary Chainstack HTTP broadcast RPC to avoid sharing the OpenRouter Python broadcast path; BNB/USDT now buys the same two bands at `10U` each, T+25.000 and `0.15gwei`; `MAX_BATCH_STAKE_USDT` / runtime `maxBatchStakeUsdt` is `50`.
- [x] 2026-06-28 pause Bot4 Hermes OpenRouter token usage buying after the template/outcome change; the Hermes planned-buy record is disabled and Hermes is removed from the buy allowlist while still visible through daily-template display. Active Bot4 daily-template buys are now OpenRouter Python `20U` plus BNB/USDT `20U`, and `MAX_BATCH_STAKE_USDT` / runtime `maxBatchStakeUsdt` is `40`.
- [x] 2026-06-29 add `Hy3 preview` to Bot4 OpenRouter Python planned buys. Bot4 now buys `DeepSeek V4 Flash`, `Owl Alpha`, and `Hy3 preview` at `10U` each, total `30U`, while BNB/USDT remains `20U`; active `MAX_MARKET_STAKE_USDT` / runtime `maxMarketStakeUsdt` is `30` and active `MAX_BATCH_STAKE_USDT` / runtime `maxBatchStakeUsdt` is `50`.
- [x] 2026-06-29 fix Bot4 non-bundle gas reserve selection so staggered same-open markets sum per-market gas instead of estimating one combined 5-outcome `3gwei` fast tx. This keeps the BNB/USDT `0.15gwei` and OpenRouter Python `3gwei` transactions independently fundable under the current Bot4 BNB balance.
- [x] 2026-06-30 move Bot4 BNB/USDT Futures Daily Volume from T+25.000 to T+20.000 now that Hermes buying is paused. BNB remains a separate non-bundle planned buy at `0.15gwei` and now uses the secondary Bot4 Chainstack RPC, while OpenRouter Python remained at the then-current T+19.900 and `3gwei` on the primary/default broadcast path.
- [x] 2026-06-30 move Bot4 BNB/USDT Futures Daily Volume from T+20.000 to T+22.000 to reduce same-wallet nonce proximity to the OpenRouter Python T+19.900 buy. BNB remains `0.15gwei`, uses the secondary Bot4 Chainstack RPC, and stays non-bundled.
- [x] 2026-07-02 switch Bot4 daily-template auto-sell from no scheduled sell to `open_timed_exit` T+39600s, so daily 08:00 Beijing buys sell 100% at 19:00 Beijing by default. Stop-loss remains disabled, sell gas remains `0.15gwei`, `AUTO_SELL_APPLY_AFTER_ISO` protects older Bot4 positions from immediate overdue selling, and runtime-config validation now accepts open-exit delays up to one day.
- [x] 2026-07-02 lower Bot4 OpenRouter Python buy gas from `3gwei` to `0.5gwei`. Updated profile default gas, runtime gas, the OpenRouter Python planned-buy record, and Bot4 readiness/evidence expectations; BNB/USDT remains T+22.000 at `0.15gwei` on the secondary Bot4 RPC, sell gas remains `0.15gwei`, and the 19:00 Beijing timed exit is unchanged.
- [x] 2026-06-27 undo the accidental Bot4 OpenRouter Python June 28 one-off override and restore Bot4 planned buys from `/opt/42space/data/42space-4/planned-buys.json.bak-openrouter-python-jun28-2u-2026-06-27T15-11-58-224Z`; Bot4 is back to the generic OpenRouter Python `10U` rule, Hermes `10U`, and BNB `20U` combined daily-template setup.
- [x] 2026-06-27 add the OpenRouter Python June 28 one-off buy to Bot3 instead of Bot4: market `0x932623bf6ea88afd90e3c762824DB454322De3D7`, outcome `DeepSeek V4 Flash`, stake `2U`, `openBroadcastDelayMs=18900`, `gasPriceGwei=1`, and `autoSell.enabled=false` / `hold_to_settlement`. Backed up Bot3 planned buys as `/opt/42space/data/42space-3/planned-buys.json.bak-openrouter-python-jun28-bot3-2u-2026-06-27T15-15-59-259Z`, restarted Bot3 event/dashboard services, and verified status/doctor show the June 28 OpenRouter market as the only next batch with `requiredBusdt=2`, wallet BUSDT/BNB/allowance ready, broadcast RPC `2/2`, and no blockers.
- [x] 2026-06-28 add Bot3 planned buys for Australia vs Egypt and Argentina vs Cabo Verde without touching Bot1/Bot4. Australia vs Egypt buys `AUS 0-1 EGY`, `AUS 0-2 EGY`, and `AUS 1-2 EGY` at `10U` each against market `0x2D47979DD16367726aF21A171DF37a691Ddeb41f`; Argentina vs Cabo Verde buys `ARG 1-0 CPV`, `ARG 2-0 CPV`, `ARG 2-1 CPV`, `ARG 3-0 CPV`, and `ARG 3-1 CPV` at `10U` each against market `0xCE263162852C42991432EC17A73B69Ab42DeA29a`. Both records pin Bot3 execution defaults `openBroadcastDelayMs=18900` and `gasPriceGwei=1`, use `pre_start_exit` with stop-loss disabled and 10h-before-kickoff clearing, and were installed after backup `/opt/42space/data/42space-3/planned-buys.json.bak-aus-egy-arg-cpv-bot3-2026-06-28T05-13-52-945Z`. Restarted Bot3 event/dashboard services and verified status/doctor show Australia as the next `30U` batch with BNB/BUSDT/allowance ready, broadcast RPC `2/2`, and no blockers.
- [x] 2026-06-28 reduce Bot1/Bot3 auto-sell quote RPC load by gating full-exit quote calls by strategy. `pre_start_exit` and `open_timed_exit` no longer quote before due time when stop-loss is disabled; ladder no longer quotes before due time when stop-loss is disabled, and only quotes at due time when a profit gate is configured. Added self-test coverage that fails if pre-start or ladder reintroduces pre-due quotes. Deployed `/opt/42space/src/event-sniper.js` after backup `event-sniper.js.bak-autosell-rpc-20260628T054259Z`, changed Bot1/Bot3 `AUTO_SELL_POLL_MS=30000`, changed Bot3 default to `AUTO_SELL_STRATEGY=pre_start_exit`, `AUTO_SELL_STOP_LOSS_ENABLED=0`, and runtime `autoSellStrategy=pre_start_exit`. Restarted only Bot1 event, Bot3 event, and Bot3 dashboard; remote `npm run verify` passed, services were active, warning logs were empty, and readback estimated Bot1/Bot3 quote-driving positions at `0` with `0` estimated full-exit quote RPC/min before strategy due time.
- [x] 2026-06-26 fix Bot2 planned-buy duplicate execution incident from `$PENGU FDV by end of July 1st?`: pending records with `openBroadcastDelayMs=null` now inherit the profile `OPEN_BROADCAST_DELAY_MS` instead of scheduling at T+0, and planned-buy stale-seen override is blocked once market start has arrived so a successful buy cannot be requeued by the discovery feed before the configured action time. Local `npm run verify` passed.
- [x] 2026-06-26 switch Bot2 `open_timed_exit` production runtime from T+36s to T+25s full exit while preserving T+19.0 buy timing and the -10% full stop-loss.
- [x] 2026-06-26 add Bot2 fast randomized open-exit path: after a successful buy receipt, pre-sign a direct sell batch from on-chain balances and broadcast in the T+24.5s to T+26.0s random window while pausing the old poll-based monitor.
- [x] 2026-06-26 add Bot3 USA vs Bosnia and Herzegovina 5-outcome exact-score planned buy in `/opt/42space/data/42space-3/planned-buys.json` after backup `planned-buys.json.bak-us-bih-2026-06-26T15-27-54-782Z`; dry-run selected the five named outcomes, funding/status/doctor showed `requiredBusdt=50`, `prepared=true`, `fundingState=funded`, and `blockers: []`.
- [x] 2026-06-27 change Bot3 default planned-buy entry gas/timing to T+18.985s with buy gas `1gwei` while preserving sell-only gas `0.15gwei`; production env and runtime backups were created, event/dashboard services restarted, startup logs showed `openBroadcastDelayMs=18985`, buy gas `1`, auto-sell gas `0.15`, and status/doctor confirmed the USA vs Bosnia planned buy remains funded/prepared with `blockers: []`.
- [x] 2026-06-27 set Bot3 planned-buy-only execution boundary by adding `MARKET_BUY_QUESTION_ALLOWLIST_REGEX=a^` to the production profile. Ordinary non-planned strategy buys are fail-closed; planned-buy records still override the non-matching allowlists and status/doctor confirmed the USA vs Bosnia planned buy remains the next funded/prepared 50U batch with broadcast RPC `2/2`.
- [x] 2026-06-27 diagnose why Bot3 ranked behind `0x3a92A09faA9C1aD28b629c681c850b99607E937d` on USA vs Bosnia despite `1gwei`: Bot3 broadcast started at T+18.988, first accepted at T+19.049, and landed in block `106577487`, while the earlier buyer used `0.5gwei` and landed in previous block `106577486` with the same timestamp second. Updated Bot3 production `OPEN_BROADCAST_DELAY_MS=18900`, kept buy `GAS_PRICE_GWEI=1` and sell `AUTO_SELL_GAS_PRICE_GWEI=0.15`, restarted only Bot3 event/dashboard services, and verified active services, public dashboard `ok:true`, current doctor broadcast RPC `2/2`, and current RPC latency around `102ms` to `103ms`.
- [x] 2026-06-27 add and deploy `npm run bot3:buy-rank`, a read-only Bot3 buy-rank evidence collector that reads fills, fetches market logs/receipts, ranks same-market mint transactions by `blockNumber + transactionIndex`, and writes `/opt/42space/output/bot3-buy-rank/latest.{json,md}`. Local and remote `npm run check` passed. USA vs Bosnia replay generated `conclusion=ranked_after_first`, Bot3 rank `2/6`, Bot3 gas `1gwei`, first buyer `0x3a92A09faA9C1aD28b629c681c850b99607E937d` gas `0.5gwei`, block `106577486` vs Bot3 block `106577487`.
- [x] 2026-06-27 make Bot3 buy-rank evidence automatic for the next buy. Added `--only-new` skip logic to `scripts/bot3-buy-rank-evidence.js`, installed `42space-bot3-buy-rank-evidence.service` and `.timer`, enabled the timer, and verified the first timer run skipped the already collected USA tx with `reason=already-collected` instead of rewriting the report.
- [x] 2026-06-27 switch Bot1 default auto-sell from the old `17s` one-step ladder to `pre_start_exit`: hold without take-profit sells, clear 10h before kickoff, disable stop-loss by default, keep buy gas `3gwei`, sell gas `0.15gwei`, and normalize Bot1 `pre_start_exit` planned buys to `stopLossEnabled=false` while preserving `$SPACEMOON` as an explicit `ladder` special case. Production readback showed USA vs Bosnia funded/prepared with `requiredBusdt=50`, `requiredBnbGasReserve=0.0138`, and dashboard text `持有不分批卖 / 赛前 600min 清仓 / 止损关闭`.
- [x] 2026-06-27 diagnose why Bot1 was not first among the T+20 USA vs Bosnia buys despite `3gwei`: Bot1 scheduled T+19.985, started fanout at T+19.994, first RPC accepted at T+20.066, and landed in block `106577489` txIndex `9`; `0x604C5E022a66c29194ce43C862D5096e7Ac2E99c` paid only `1.1gwei` but landed in the same block txIndex `1`, while three earlier same-market buys landed in `08:00:19` blocks. The cause is propagation/block-boundary and validator ordering, not gas price alone. Updated only Bot1 production default to `OPEN_BROADCAST_DELAY_MS=18900`, wrote `openBroadcastDelayMs=18900` to runtime config, kept buy `GAS_PRICE_GWEI=3` and sell `AUTO_SELL_GAS_PRICE_GWEI=0.15`, restarted Bot1 event/dashboard services, verified watchConfig `openBroadcastDelayMs=18900`, broadcast RPC `2/2`, BNB ready, and current BUSDT below the `45U` next-batch requirement.
- [x] 2026-06-27 adjust Bot1 timing from `18900ms` to `19900ms` after the operator clarified the target is still the 20s block because 19s blocks can retain premium. Updated only `/etc/42space/profiles/42space.env` and `/opt/42space/data/42space/runtime-config.json` for Bot1, backed both up with `bak-bot1-delay-19900-*`, kept buy gas `3gwei`, sell gas `0.15gwei`, and `pre_start_exit` unchanged, restarted Bot1 event/dashboard services, and verified `watchConfig.openBroadcastDelayMs=19900`, broadcast RPC `2/2`, BNB ready, BUSDT below the `45U` next-batch requirement, and Bot2/Bot3/Bot4/Bot5 delays unchanged at `18985`/`18900`/`19985`/`19985`.
- [x] 2026-06-27 add Bot1 planned buys for Australia vs Egypt and Argentina vs Cabo Verde after backup `/opt/42space/data/42space/planned-buys.json.bak-aus-egy-arg-cpv-2026-06-27T15-06-04-840Z`. Australia vs Egypt buys `AUS 0-1 EGY`, `AUS 0-2 EGY`, and `AUS 1-2 EGY` at `10U` each against market `0x2D47979DD16367726aF21A171DF37a691Ddeb41f`; Argentina vs Cabo Verde buys `ARG 1-0 CPV`, `ARG 2-0 CPV`, `ARG 2-1 CPV`, `ARG 3-0 CPV`, and `ARG 3-1 CPV` at `10U` each against market `0xCE263162852C42991432EC17A73B69Ab42DeA29a`. Each record pins `openBroadcastDelayMs=19900`, buy gas `3gwei`, and `pre_start_exit` with stop-loss disabled and 10h-before-kickoff clearing. Restarted only Bot1 event/dashboard services; status showed `busdtBalance=94.474336533687133375`, the next Australia batch funded, broadcast RPC `2/2`, and doctor `blockers: []`; dry-run plan selected all named outcomes with no fallback.
- [x] 2026-06-28 set Bot1 default buy gas to `6gwei` and keep sell gas at `0.15gwei`. Updated only `/etc/42space/profiles/42space.env`, `/opt/42space/data/42space/runtime-config.json`, plus the two pending Bot1 planned-buy records for Australia vs Egypt and Argentina vs Cabo Verde that had explicit `gasPriceGwei=3`; backups were created with `bak-buygas-6-2026-06-28T05-01-55-053Z`. Restarted Bot1 event/dashboard services and verified `watchConfig.gasPriceGwei=6`, both planned records read back `gasPriceGwei=6`, `openBroadcastDelayMs=19900`, `pre_start_exit`, stop-loss disabled, BUSDT/BNB/allowance ready, and doctor `blockers: []`. The next Australia 3-outcome batch needs about `0.0204 BNB` at `6gwei` while the wallet has about `0.020512 BNB`, so the BNB margin is thin and the later 5-outcome Argentina plan likely needs a BNB top-up after the first buy.
- [x] 2026-06-28 add and execute urgent Bot1 Spain vs Austria planned buy after backup `/opt/42space/data/42space/planned-buys.json.bak-spain-austria-19700-5u-2026-06-28T09-30-16-250Z`. The exact-score market is `0x48Fe5A947c31fD9A1E4465a027b72cb308dB8271`, with outcomes `ESP 1-0 AUT`, `ESP 2-0 AUT`, and `ESP 2-1 AUT` at `5U` each, total `15U`; record pins `openBroadcastDelayMs=19700`, buy gas `6gwei`, `pre_start_exit`, stop-loss disabled, kickoff `2026-07-02T19:00:00.000Z`, and 10h-before-kickoff clearing. Restarted Bot1 event/dashboard services before the `2026-06-28T09:31:31Z` market open; status/doctor showed Spain as the next `15U` batch with BUSDT/BNB/allowance ready, broadcast RPC `2/2`, and no blockers; dry-run selected the three named outcomes at `5U` each with no fallback. The worker pre-signed tx `0x47c037da917b524a5d6e848ffe5cafd16102ad2a5912f95867b2052dec16948d` at `2026-06-28T09:30:51.277Z`, scheduled `targetAt=2026-06-28T09:31:50.700Z`, started fanout at `09:31:50.704Z`, first accepted via Ankr at `09:31:50.760Z`, and receipt succeeded in block `106845595` with txIndex `52` and gas used `1173487`. Positions now show all three Spain outcomes, and post-buy status moved the next batch to Australia vs Egypt `15U` but flags insufficient BNB because Bot1 balance is `0.016309242591798533 BNB` below the `0.0204 BNB` 6gwei fast-buy reserve.
- [x] 2026-06-28 lower Bot1 Australia vs Egypt planned-buy size from `10U` to `5U` per selected outcome after backup `/opt/42space/data/42space/planned-buys.json.bak-aus-egypt-5u-2026-06-28T05-53-10-492Z`. The selected outcomes remain `AUS 0-1 EGY`, `AUS 0-2 EGY`, and `AUS 1-2 EGY`; total stake is now `15U`, with `openBroadcastDelayMs=19900`, buy gas `6gwei`, `pre_start_exit`, stop-loss disabled, and 10h-before-kickoff clearing unchanged. Restarted Bot1 event/dashboard services and verified status funding now requires `15U`, dry-run selected the three named outcomes at `5U` each with no fallback, BUSDT/BNB/allowance ready, and doctor `blockers: []`.
- [x] 2026-06-28 retune Bot1 production from fixed T+19.900 to fixed T+19.800 after the operator chose a 200ms lead instead of deploying block-aware mode. Updated only Bot1 `/etc/42space/profiles/42space.env`, `/opt/42space/data/42space/runtime-config.json`, and the pending Australia vs Egypt plus Argentina vs Cabo Verde planned-buy records from `openBroadcastDelayMs=19900` to `19800`; backups were created as `bak-bot1-delay-19800-2026-06-28T12-37-57Z`. Restarted only `42space-event@42space.service` and `42space-dashboard@42space.service`; program readback showed `watchConfig.openBroadcastDelayMs=19800`, buy gas `6gwei`, sell gas `0.15gwei`, `pre_start_exit`, stop-loss disabled, broadcast RPC `2/2`, and both pending records at `19800`. Dashboard readback showed `广播 T+19.8s / 超窗 25s`; Australia vs Egypt remains the next `15U` batch but is BNB-blocked with `0.016309242591798533 BNB` below the fast-buy reserve, while BUSDT is `52.094590996865505018`.
- [x] 2026-06-28 lower Bot1 buy gas from `6gwei` to `2gwei`. Updated only Bot1 `/etc/42space/profiles/42space.env`, `/opt/42space/data/42space/runtime-config.json`, and the pending Australia vs Egypt plus Argentina vs Cabo Verde planned-buy records; backups were created with `bak-bot1-buygas-2-*`. Restarted only Bot1 event/dashboard services. Program readback showed `watchConfig.gasPriceGwei=2`, `openBroadcastDelayMs=19800`, sell gas `0.15gwei`, `pre_start_exit`, stop-loss disabled, broadcast RPC `2/2`, both pending records at `2gwei`, and doctor `blockers: []`. The next Australia `15U` batch is now funded; gas reserve is `0.0068 BNB` and wallet BNB is `0.016309242591798533`.
- [x] 2026-06-28 retune Bot1 from fixed T+19.800 to T+19.900 after Australia vs Egypt proved the 200ms lead was too early. Journal evidence showed target `2026-06-28T18:00:19.800Z`, broadcast start T+19.805, first accepted by Ankr T+19.851, tx `0x3002b25bcb1249ed82d09a3cfcd2449f77897698151c724f2a5ba1f90cb23bbb`, receipt block `106913364` timestamp T+19.000, and same-market rank `3` after two lower-gas 19s-block buyers. Updated only Bot1 profile env/runtime config and the unfilled Argentina vs Cabo Verde planned-buy record to `OPEN_BROADCAST_DELAY_MS=19900`; kept buy gas `2gwei`, sell gas `0.15gwei`, `pre_start_exit`, and stop-loss disabled. Backups were created with `bak-bot1-delay-19900-after-19800-19s-*`, restarted only Bot1 event/dashboard services, and readback showed Argentina `50U` funded with no blockers.
- [x] 2026-06-28 retune Bot1 default and the unfilled Argentina vs Cabo Verde planned-buy record to `OPEN_BROADCAST_DELAY_MS=19850` with buy gas `2.2gwei`. Updated only Bot1 `/etc/42space/profiles/42space.env`, `/opt/42space/data/42space/runtime-config.json`, and `/opt/42space/data/42space/planned-buys.json`; backups were created with `bak-bot1-delay-19850-gas-2p2-*`. Restarted only Bot1 event/dashboard services. Readback showed env/runtime/Argentina all at `19850` and `2.2`, sell gas still `0.15gwei`, `pre_start_exit`, stop-loss disabled, status `requiredBusdt=50`, required BNB gas reserve `0.01012`, wallet BNB `0.014068421191798533`, broadcast RPC `2/2`, and no blockers.
- [x] 2026-06-29 analyze Bot1 Argentina vs Cabo Verde buy rank and pin Bot1 buy broadcast to single Ankr. Evidence tx `0x45c40860cb8a955f6e54ed97e07021191f62d8da926c1cc40626f1fc03d99721` used T+19.850 and `2.2gwei`, started broadcast at T+19.859, first accepted by Ankr at T+19.928, landed in block `106945360` timestamp T+20.000 with txIndex `10`, and ranked `5`: one T+18 tx, two T+19 txs, plus a lower-gas T+20 tx at txIndex `1` were ahead. Chainstack accepted the same raw tx later at `381ms`, so dual RPC did not create the miss; remaining miss was ordering/propagation. Backed up Bot1 env as `42space.env.bak-bot1-single-ankr-broadcast-*`, changed only `BROADCAST_RPC_URLS` to the Ankr endpoint, restarted only Bot1 event/dashboard services, and verified `broadcastRpcCount=1`, doctor `broadcastRpc.rpcCount=1`, `okCount=1`, `bestProvider=rpc.ankr.com`.
- [x] 2026-06-29 add Bot1 Colombia vs Ghana as the next real single-Ankr validation buy after backup `/opt/42space/data/42space/planned-buys.json.bak-colombia-ghana-5u-single-ankr-2026-06-28T23-54-41-220Z`. The exact-score market is `0xe3AF7421974F09A340B3B821B8927e73b0C729a6`, start `2026-06-29T01:30:00Z`, with outcomes `COL 2-0 GHA`, `COL 1-0 GHA`, and `COL 2-1 GHA` at `5U` each, total `15U`; the record pins `openBroadcastDelayMs=19850`, buy gas `2.2gwei`, `pre_start_exit`, stop-loss disabled, kickoff/end `2026-07-04T02:15:00.000Z`, and 10h-before-kickoff clearing. Restarted only Bot1 event/dashboard services and verified both active; status shows the Colombia batch prepared with no prepare error, wallet `18.06401318494609335 BUSDT`, `0.015771905447891948 BNB`, required `15U`, required gas reserve `0.00748 BNB`, allowance/BUSDT/BNB ready, broadcast RPC count `1`, and doctor blockers empty. Dry-run plan selection resolves exactly the three named token IDs `1024`, `32`, and `2048` with no fallback. Installed and enabled `42space-bot1-buy-rank-evidence.timer`; its first run skipped the already collected Argentina tx, so the next distinct Bot1 buy should update `/opt/42space/output/bot1-buy-rank/latest.{json,md}`.
- [x] 2026-06-29 add and execute Bot1 `2026 FIFA World Cup 3rd Place ?` planned buy after backups `/opt/42space/data/42space/planned-buys.json.bak-worldcup-3rd-place-4x10u-gas4p4-2026-06-29T07-16-25-750Z` and `/opt/42space/data/42space/planned-buys.json.bak-worldcup-3rd-place-add-brazil-2026-06-29T07-24-27-051Z`. The market is `0x86308B8059183eA443fd1885D5493cF6C5222F1f`, start `2026-06-29T07:30:00Z`, with outcomes `France`, `Argentina`, `Spain`, `England`, and added `Brazil` at `10U` each, total `50U`; the record pins `openBroadcastDelayMs=19850`, per-record buy gas `4.4gwei`, `pre_start_exit`, stop-loss disabled, and uses market end `2026-07-18T21:00:00.000Z` as the clearing anchor to avoid immediate open-time pre-start exit. Restarted only Bot1 event/dashboard services and verified both active; pre-buy status showed BUSDT/BNB/allowance ready, required `50U`, broadcast RPC count `1`, and doctor blockers empty. The worker pre-signed at `2026-06-29T07:29:20.295Z`, started single-Ankr broadcast at `2026-06-29T07:30:19.853Z`, first accepted at `07:30:19.901Z`, and receipt succeeded in block `107021346` with tx `0xc0ba3f918ddea2b5058fe0057490c5264e96c57d077d6b391dcc3d92d621b632`, gas used `1656164`, effective gas `4.4gwei`. Buy-rank evidence ranked Bot1 `6/9` in the first 60s: four earlier transactions landed in the `07:30:19` timestamp window and one same-block `07:30:20` transaction had txIndex `1` before Bot1's txIndex `39`.
- [x] 2026-06-29 add and execute Bot1 `2026 FIFA World Cup Runner-Up?` planned buy after backup `/opt/42space/data/42space/planned-buys.json.bak-worldcup-runner-up-4x20u-gas4p4-2026-06-29T07-35-48-278Z`. The market is `0x6B7F30fb52B26814BB49312442010450e43e226D`, start `2026-06-29T07:45:00Z`, with outcomes `France`, `England`, `Argentina`, and `Spain` at `20U` each, total `80U`; the record pins `openBroadcastDelayMs=19850`, per-record buy gas `4.4gwei`, `pre_start_exit`, stop-loss disabled, and uses market end `2026-07-19T19:00:00.000Z` as the clearing anchor to avoid immediate open-time pre-start exit. Restarted only Bot1 event/dashboard services and verified both active; status/doctor showed required `80U`, BUSDT `85.115490998654546941`, BNB `0.024097711636391488`, allowance/BUSDT/BNB ready, broadcast RPC count `1`, and doctor blockers empty. The dry-run plan selected exactly the four named outcomes with no fallback. The worker pre-signed at `2026-06-29T07:44:20.494Z`, started single-Ankr broadcast at `2026-06-29T07:45:19.853Z`, first accepted at `07:45:19.900Z`, and receipt succeeded in block `107023346` with tx `0xb3d60f7209d115c65e26f22a82861b1115433e0639ae891f38258df012d775f7`, gas used `1357501`, effective gas `4.4gwei`. Buy-rank evidence ranked Bot1 `6/9` in the first 60s: three earlier transactions landed in the `07:45:19` timestamp window, and two same-block `07:45:20` transactions had txIndex `1` and `3` before Bot1's txIndex `9`.
- [x] 2026-06-30 stage Bot1 `France vs. Sweden - Handicap` planned buy after backup `/opt/42space/data/42space/planned-buys.json.bak-france-sweden-handicap-france-minus1p5-10u-gas1p1-2026-06-30T08-59-35-491Z`. The market is `0x8f14974D980d971c606a9b2f5862285ADA77C55d`, start `2026-06-30T09:15:00Z`, with outcome `France −1.5` at `10U`; the record pins `openBroadcastDelayMs=19850`, per-record buy gas `1.1gwei`, `pre_start_exit`, stop-loss disabled, and `kickoffAt=2026-06-30T21:00:00.000Z` for the 10h pre-start exit anchor. Restarted only Bot1 event/dashboard services and verified both active; dry-run selected exactly tokenId `1` with no fallback. Current readback shows the plan is prepared but funding-blocked: BUSDT `5.130428114356515073` vs required `10U`, so it needs about `4.87U` more before the open hot window; BNB `0.018117763136391488` and broadcast RPC count `1` are sufficient.
- [x] 2026-06-28 add optional block-aware 20s broadcast timing. `OPEN_BROADCAST_MODE=fixed` remains the default; `block_aware_20s` starts a WSS block-head clock, treats `OPEN_BROADCAST_DELAY_MS` as the arm time, normally falls back to `T+20s - OPEN_BROADCAST_BLOCK_AWARE_LEAD_MS`, and can release after repeated 19s timestamp heads near the 20s boundary but not before the arm time. Current defaults are `OPEN_BROADCAST_BLOCK_AWARE_LEAD_MS=95`, `OPEN_BROADCAST_BLOCK_AWARE_PRE_TARGET_COUNT=2`, and `OPEN_BROADCAST_BLOCK_AWARE_PRE_TARGET_SEND_MS=120`, so Bot1's `19900ms` arm prevents another T+19.700 style send while the nominal target becomes T+19.905 and the block-aware early path can fire at T+19.900 after two fresh 19s timestamp heads. The mode logs its timing reason in `open-broadcast-scheduled`, applies to due timers, feed/ws/rest/chain loops, pre-sign grouping, and direct execute checks, uses stable action keys for bundle grouping instead of merging records by the current due instant, and falls back without blocking when WSS heads are absent or late. Local `npm run event:self-test` covers fixed timing, nominal block-aware fallback, target-head release, repeated pre-target heads, late per-record delay preservation, and stable block-aware grouping.
- [x] 2026-06-28 prepare Bot1 block-aware post-buy evidence. Successful single and bundle buy fills now preserve `result.openBroadcastTiming` when `block_aware_20s` is active, including reason, target time, arm time, nominal target, latest WSS head offset, and WSS errors. The buy-rank evidence script now reports profile block-aware config and per-buy timing snapshots, and `npm run bot1:buy-rank` plus optional `42space-bot1-buy-rank-evidence.service/.timer` can write Bot1 reports under `/opt/42space/output/bot1-buy-rank` after the next new Bot1 buy.
- [x] 2026-06-28 strengthen Bot1/Bot3 buy-rank evidence for the 20s-boundary goal. The evidence script now filters `eth_getLogs` by the MarketTrade topic, reports the configured target boundary, broadcast/first-accepted lead to that boundary, receipt block timestamp delta, rank inside the target `T+20s` timestamp second, and rank at/after the 20s boundary. Remote smoke runs against recent Bot1 fills showed Spain first-accepted `240ms` before the 20s boundary and landed in a `T+19s` block, while Mexico first-accepted `17ms` before the boundary, landed in `T+20s`, but ranked second within the 20s timestamp second.
- [x] 2026-06-28 add `npm run bot1:block-calibrate`, a read-only calibration report for Bot1 block-aware timing. It reads Bot1 fills, optionally fetches receipts/blocks through RPC, summarizes first-accepted RPC latency, first-accepted lead to the 20s boundary, 19s/20s/post-20s block classifications, and emits recommended `OPEN_BROADCAST_BLOCK_AWARE_LEAD_MS`, pre-target head count, and arm lead. It does not read private keys, broadcast transactions, mutate fills/seen/runtime state, or print RPC secrets. The optional `42space-bot1-block-aware-calibration.timer` writes `/opt/42space/output/bot1-block-aware-calibration/latest.json` every five minutes with quiet journal output so the next buy can be retuned from evidence.
- [x] 2026-06-28 add a guarded Bot1 block-aware activation script. `scripts/bot1-block-aware-activate.sh` defaults to dry-run, refuses non-Bot1 profile env paths, runs local `npm run verify`, reads current remote Bot1 timing config without printing secrets, and requires `APPLY=1 CONFIRM_BOT1_BLOCK_AWARE=enable-bot1-block-aware` before it backs up production files, deploys only the relevant code/evidence files with directory-preserving rsync, updates only `/etc/42space/profiles/42space.env`, optionally installs the Bot1 buy-rank evidence timer plus block-aware calibration timer, and restarts only `42space-event@42space.service`.
- [x] 2026-06-28 add and execute Bot3 Mexico vs Ecuador planned buy without touching Bot1/Bot4. The final record buys `MEX 1-0 ECU`, `MEX 2-0 ECU`, and `MEX 2-1 ECU` at `5U` each against market `0x519F7d36E0ac447235A4d7E715739bF747D67D8d`, with kickoff `2026-07-01T01:00:00.000Z`, Bot3 defaults `openBroadcastDelayMs=18900`, buy gas `1gwei`, `pre_start_exit`, stop-loss disabled, and 10h-before-kickoff clearing. Removed the temporary broad regex after status showed it also matched Goal Differential and Total Goals. Bot3 pre-signed at `2026-06-28T09:18:41.263Z`, broadcast at `2026-06-28T09:19:40.903Z`, first accepted via Ankr at `09:19:40.988Z`, and receipt succeeded in block `106843974` with tx `0x2c0d9265f77c1f90fd91fd70bfc2a64243380ea0ffff8f42e330ec7f0d0d1d6c`.
- [x] 2026-06-28 retune Bot3 production default and current unexecuted target records to T+18.850 without touching Bot1/Bot2/Bot4. Updated `/etc/42space/profiles/42space-3.env`, `/opt/42space/data/42space-3/runtime-config.json`, and the current Australia vs Egypt plus Argentina vs Cabo Verde target records to `openBroadcastDelayMs=18850`; kept buy gas `1gwei`, sell gas `0.15gwei`, `pre_start_exit`, stop-loss disabled, and 10h-before-kickoff clearing unchanged; left already bought records unchanged as historical evidence. Backups use `bak-bot3-delay-18850-20260628T152609Z`. Restarted only Bot3 event/dashboard services and verified active services, status `openBroadcastDelayMs=18850`, next batch Australia vs Egypt `15U`, doctor `blockers: []`, `requiredBusdt=15`, and broadcast RPC `2/2`.
- [x] 2026-07-04 implement Bot3-only FIFA exact-score automatic execution config. The selector converts the lower price win-side tier into the existing `EVENT_OUTCOME_SELECTION=names` buy path, uses `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT=1` per selected outcome, preserves Bot3's existing pre-sign/open-broadcast/gas/receipt/auto-sell chain, and is profile-gated so Bot1/Bot2/Bot5 cannot activate it by display filtering alone. Self-test covers profile gating and planned-buy priority.
- [x] 2026-07-05 raise Bot3 exact-score automatic execution stake to `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT=5`, so the auto-selected three-outcome basket requires `15U` before known-batch execution. This does not change planned-buy records or their per-record stake.
- [x] 2026-07-06 fix Bot3 automatic exact-score pre-start auto-sell anchoring after the successful `France vs Morocco` auto-buy sold immediately. Root cause: auto-buy receipts are intentionally not planned-buy rows, so no `plannedBuy.kickoffAt` existed and `autoSellMarketStartDate` fell back to market-open `startDate`, making the 10h pre-start due time already stale. The fix keeps planned-buy `kickoffAt` first, then uses market `endDate` only for Sports/FIFA exact-score positions; side markets remain excluded. Local and remote `npm run verify` passed, and Bot3 event/dashboard services were restarted after production backup `/opt/42space/src/event-sniper.js.bak-bot3-auto-exact-autosell-enddate-20260706T035119Z`.
- [x] 2026-07-06 raise Bot3 exact-score automatic execution stake to `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT=10`, so the auto-selected three-outcome basket now requires `30U`. Kept planned-buy overrides, T+18.850 broadcast timing, buy `4.4gwei`, sell `0.15gwei`, `pre_start_exit`, stop-loss disabled, and `45U` market/batch caps unchanged. Production backup `/etc/42space/profiles/42space-3.env.bak-bot3-auto-stake-10-20260706T045532Z`; Bot3 event/dashboard services restarted active. Startup readback shows the next known exact-score batch `Norway vs England` requires `30U`, but BNB gas reserve is still short for the `4.4gwei` fast-buy reserve.
- [x] 2026-06-29 raise Bot3 buy gas from `1gwei` to `2.2gwei` after Australia vs Egypt ranked `2` in the first 19s block behind a same-block `1.3gwei` buyer. Kept `OPEN_BROADCAST_DELAY_MS=18850`, sell gas `0.15gwei`, `pre_start_exit`, stop-loss disabled, and 10h-before-kickoff clearing unchanged. Updated only Bot3 env/runtime and the next Argentina vs Cabo Verde planned-buy record; backups use `bak-bot3-buygas-2p2-20260628T181751Z`. Restarted only Bot3 event/dashboard services and verified status `openBroadcastDelayMs=18850`, `gasPriceGwei=2.2`, next batch Argentina vs Cabo Verde `50U`, doctor `blockers: []`, broadcast RPC `2/2`, and gas reserve `0.01012 BNB`.
- [x] 2026-06-29 execute Bot3 `2026 FIFA World Cup 3rd Place ?` country-basket planned buy and retune Bot3 buy gas to `4.4gwei`. The record selected France, Argentina, Spain, England, and Brazil at `10U` each on market `0x86308B8059183eA443fd1885D5493cF6C5222F1f`, used T+18.850, `presigned_fanout_raw`, two broadcast RPCs, and `hold_to_settlement` to avoid Bot3's default 10h `pre_start_exit` selling a long-dated non-exact-score market immediately. Pre-open status/doctor showed `requiredBusdt=50`, gas reserve `0.02024 BNB`, BNB/BUSDT/allowance ready, and blockers empty. The worker pre-signed nonce `75` at `2026-06-29T07:29:19.561Z`, started broadcast at `2026-06-29T07:30:18.852Z`, first accepted through Ankr at `07:30:18.914Z`, both RPCs accepted, and the receipt succeeded in block `107021344` at txIndex `7` with tx `0xd0408563a7ec51052d7eecfef16b41ce9a045e367758dc40c4695ec910f9475f`, gas used `1,906,474`, effective gas `4.4gwei`, and selector `0x60fc8466`. The Bot3 buy-rank evidence report generated at `2026-06-29T07:35:12.267Z` concluded `rank1` with `ourRank=1` of `topCount=9`; the next same-block buyers were `0x23d6004B93019bEf4E882f3aE60CAbEEd2efbB34` at txIndex `43` and `0x3a92A09faA9C1aD28b629c681c850b99607E937d` at txIndex `81`. Post-buy status leaves Bot3 below the generic `45U` next-buy BUSDT/BNB guard until topped up.
- [x] 2026-06-29 execute Bot3 `2026 FIFA World Cup Runner-Up?` country-basket planned buy and final sizing adjustment to `30U` per selected outcome. The record selected France, England, Argentina, and Spain on market `0x6B7F30fb52B26814BB49312442010450e43e226D`, used T+18.850, `4.4gwei`, `presigned_fanout_raw`, two broadcast RPCs, and `hold_to_settlement`. It initially blocked on BNB after being staged at `20U`; after funding and the operator's update, the final preflight was `120U` required, `0.0176 BNB` gas reserve, BUSDT/BNB/allowance ready, and no blockers. The worker pre-signed nonce `80` at `2026-06-29T07:44:19.604Z`, started broadcast at `2026-06-29T07:45:18.852Z`, first accepted through Ankr at `07:45:18.929Z`, both RPCs accepted, and the receipt succeeded in block `107023344` at txIndex `22` with tx `0x12251caa136916077f829bcb876a0a1265a38cc21cf47923e733705e3e898f03`, gas used `1,542,221`, effective gas `4.4gwei`, and selector `0x60fc8466`. The Bot3 buy-rank evidence report generated at `2026-06-29T07:46:29.761Z` concluded `ranked_after_first` with `ourRank=3` of `topCount=9`; rank 1 was `0x3a92A09faA9C1aD28b629c681c850b99607E937d` at `5gwei` txIndex `1`, and rank 2 was `0x23d6004B93019bEf4E882f3aE60CAbEEd2efbB34` at `1.3gwei` txIndex `2`. Post-buy status leaves Bot3 below the generic `45U` next-buy BUSDT guard until topped up.
- [x] 2026-06-28 add and execute urgent Bot1 Mexico vs Ecuador planned buy after backup `/opt/42space/data/42space/planned-buys.json.bak-mex-ecu-2026-06-28-5u-2026-06-28T09-10-47-449Z`. The exact-score market is `0x519F7d36E0ac447235A4d7E715739bF747D67D8d`, with outcomes `MEX 1-0 ECU`, `MEX 2-0 ECU`, and `MEX 2-1 ECU` at `5U` each, total `15U`; record pins `openBroadcastDelayMs=19900`, buy gas `6gwei`, `pre_start_exit`, stop-loss disabled, kickoff `2026-07-01T01:00:00.000Z`, and 10h-before-kickoff clearing. Restarted Bot1 event/dashboard services before the `2026-06-28T09:19:22Z` market open; status/doctor showed Mexico as the next `15U` batch with BUSDT/BNB/allowance ready, broadcast RPC `2/2`, and no blockers; dry-run selected the three named outcomes at `5U` each with no fallback. The worker pre-signed tx `0xf0fa51021088100bec31a61dcbc7502257f6e0cee863b2a47351e9f7a8ffef2f` at `2026-06-28T09:18:42.419Z`, broadcast at `2026-06-28T09:19:41.917Z` via `presigned_fanout_raw`, both RPCs accepted with Ankr first at `66ms`, receipt succeeded in block `106843976` with gas used `1018963`, and post-buy status moved the next batch to Australia vs Egypt `15U`.
- [x] 2026-06-27 tune Bot2 from `OPEN_BROADCAST_DELAY_MS=18985` to `18900` to target the first open+19s timestamp block. Backed up `/etc/42space/profiles/42space-2.env` as `42space-2.env.bak-bot2-delay-18900-20260627T063251Z`, changed only Bot2, restarted `42space-event@42space-2.service` and `42space-dashboard@42space-2.service`, verified both active, program readback `openBroadcastDelayMs=18900`, dashboard text `广播 T+18.9s / 超窗 20s`, buy gas `1gwei`, sell gas `0.15gwei`, broadcast RPC `2/2`, and doctor `blockers: []`.
- [x] 2026-06-30 retune Bot2 default buy broadcast and buy gas to `OPEN_BROADCAST_DELAY_MS=18840` and `GAS_PRICE_GWEI=3.3`. Backed up `/etc/42space/profiles/42space-2.env` as `42space-2.env.bak-bot2-delay-18840-gas-3p3-2026-06-30T08-07-46-307Z` and `/opt/42space/data/42space-2/runtime-config.json` with the same suffix, updated the runtime gas override from `1` to `3.3`, restarted only `42space-event@42space-2.service` and `42space-dashboard@42space-2.service`, verified program readback `openBroadcastDelayMs=18840`, `gasPriceGwei=3.3`, dashboard text `广播 T+18.84s / 超窗 20s`, sell gas still `0.15gwei`, `open_timed_exit` T+25 unchanged, broadcast RPC `2/2`, wallet BUSDT/BNB/allowance ready, and doctor `blockers: []`.
- [x] 2026-06-30 pause Bot2 automatic buying without stopping auto-sell. Backed up `/etc/42space/profiles/42space-2.env` as `42space-2.env.bak-bot2-autobuy-pause-2026-06-30T09-03-48-149Z` and `/opt/42space/data/42space-2/planned-buys.json` as `planned-buys.json.bak-bot2-autobuy-pause-2026-06-30T09-03-48-149Z`; set `MARKET_BUY_QUESTION_ALLOWLIST_REGEX=a^` so ordinary/default/manual-follow buys cannot pass the hard buy boundary, disabled all 25 Bot2 planned-buy rows with `pauseReason=bot2-auto-buy-paused`, restarted only Bot2 event/dashboard services, and verified active services, env readback, planned-buy enabled count `0`, dashboard hard-boundary summary, `autoSellEnabled=true`, `open_timed_exit` T+25, `AUTO_SELL_GAS_PRICE_GWEI=0.15`, status `live.count=0`, and no next-batch markets.
- [x] 2026-07-03 execute Bot2 one-off `$GRASS FDV by end of July 7th?` planned buy and patch the fast open-exit hold override gap. Added market `0x50Ed7FcC8Dc69EC10FDEF2d42FA9738748c38E4B` with outcomes `$400M - $450M`, `$450M - $550M`, and `$550M - $650M`, `10U` each, per-record buy gas `1.1gwei`, `openBroadcastDelayMs=18840`, and `autoSell: { enabled: false, strategy: "hold_to_settlement" }`; backups are `/opt/42space/data/42space-2/planned-buys.json.bak-grass-10u-gas1p1-hold-2026-07-03T03-51-59-447Z`, `/opt/42space/data/42space-2/seen-markets.json.bak-grass-10u-gas1p1-hold-2026-07-03T03-51-59-447Z`, and `/opt/42space/src/event-sniper.js.bak-fast-open-exit-hold-2026-07-03T04-04-00Z`. The buy pre-signed nonce `646` at `2026-07-03T03:59:19.580Z`, broadcast at `2026-07-03T04:00:18.847Z`, first accepted by Ankr at `75ms`, and succeeded in block `107761004` with tx `0xcef71a6f52b2ac473b385b628ed9c81b7a587c94fa52a519e3d7e6f08f2ff46c`, gas used `1180759`, effective gas `1.1gwei`. The pre-fix receipt-triggered `fast_open_timed_exit` path ignored the planned `hold_to_settlement` override and sold all three outcomes at target `2026-07-03T04:00:24.740Z`, tx `0x526ad776070f2098a06920fd6d33d08dc421c8113888b8395cb71f5dbe5e099b`, block `107761017`, sell gas `0.15gwei`; auto-sell state now shows the GRASS positions completed with `remainingSize=0`. Patched `src/event-sniper.js` so single-buy receipt context carries `plannedBuy` and fast open-exit checks planned `autoSell` before scheduling, added self-test coverage for context and planned-buy-file fallback, deployed only Bot2-relevant code to production, `npm run verify` passed locally and remotely, restarted `42space-event@42space-2.service` and `42space-dashboard@42space-2.service`, disabled the GRASS row with backup `/opt/42space/data/42space-2/planned-buys.json.bak-grass-executed-disable-2026-07-03T04-06-30-636Z`, and verified Bot2 still has zero enabled planned buys plus `MARKET_BUY_QUESTION_ALLOWLIST_REGEX=a^`.
- [x] 2026-07-03 execute Bot2 one-off `Churchill Capital Corp XI (CCXI) price range, end of July 10th?` planned buy with auto-sell enabled. Added market `0x25994afDc1ec987Ae9a123A0e10750821518bfb7` with outcomes `$17 – $18`, `$18 – $20`, and `$20– $22`, `5U` each, `openBroadcastDelayMs=18840`, and `open_timed_exit` auto-sell; initial backups were `/opt/42space/data/42space-2/planned-buys.json.bak-ccxi-5u-auto-sell-2026-07-03T09-23-17-206Z` and `/opt/42space/data/42space-2/seen-markets.json.bak-ccxi-5u-auto-sell-2026-07-03T09-23-17-206Z`. Operator changed per-record buy gas to `1.1gwei` at `2026-07-03T09:28:22Z`, before the T-60s pre-sign; backup `/opt/42space/data/42space-2/planned-buys.json.bak-ccxi-gas1p1-2026-07-03T09-28-22-152Z`. Status verified next batch `requiredBusdt=15`, `prepared=true`, gas reserve `0.00374 BNB`, and `gasPriceGwei=1.1`. The buy pre-signed nonce `649` at `2026-07-03T09:29:19Z`, target `2026-07-03T09:30:18.840Z`, broadcast at `2026-07-03T09:30:18.844Z`, first accepted by Ankr at `64ms`, and succeeded in block `107804995` with tx `0x6a2496a69c28b7886424623d4523734729e2377440a012f3052c3897bd6e09a2`, gas used `1178641`, effective gas `1.1gwei`. Fast open-exit sold all three outcomes at target `2026-07-03T09:30:24.822Z`, first accepted by Ankr at `33ms`, receipt success in block `107805009` with tx `0xd58d9727925b802d96cbdbc9a8c8f64531bbe412eb7b36ca1294c7a19c1a22bb`, gas used `627987`, effective sell gas `0.15gwei`; auto-sell state shows all three CCXI positions completed with `remainingSize=0`. Disabled the CCXI row after execution with backup `/opt/42space/data/42space-2/planned-buys.json.bak-ccxi-executed-disable-2026-07-03T09-33-40-133Z`, restarted only Bot2 event service, and verified Bot2 has zero enabled planned buys.
- [x] 2026-07-03 restore Bot2 automatic buying and keep automatic selling enabled, with global buy gas lowered to `1.1gwei`. Backed up `/etc/42space/profiles/42space-2.env` as `42space-2.env.bak-bot2-autobuy-resume-gas1p1-2026-07-03T13-52-26-989Z` and `/opt/42space/data/42space-2/runtime-config.json` with the same suffix; set env/runtime `GAS_PRICE_GWEI=1.1`, cleared `MARKET_BUY_QUESTION_ALLOWLIST_REGEX`, kept `AUTO_SELL_ENABLED=1`, `AUTO_SELL_STRATEGY=open_timed_exit`, `AUTO_SELL_OPEN_EXIT_DELAY_SECONDS=25`, `AUTO_SELL_GAS_PRICE_GWEI=0.15`, `AUTO_SELL_STOP_LOSS_ENABLED=1`, and `AUTO_SELL_FAST_OPEN_EXIT_ENABLED=1`. Restarted only Bot2 event/dashboard services; readback shows `marketBuyQuestionAllowlistRegex=null`, `plannedBuysEnabled=0`, `openBroadcastDelayMs=18840`, middle-three `10U`, one market per open, fast open-exit T+24.5s to T+26.0s, and doctor `blockers: []`. The restored strategy immediately found `$FXI FDV by end of July 7th?` as the next funded/prepared `30U` Meme batch for `2026-07-03T14:00:00Z`, with gas reserve calculated at `1.1gwei`.
- [x] 2026-07-07 switch Bot2 automatic outcome selection from centered three outcomes to first three display/token-order outcomes by adding `EVENT_OUTCOME_SELECTION=first` support, updating Bot2 production env to `EVENT_OUTCOME_SELECTION=first` with `EVENT_OUTCOME_COUNT=3`, and keeping the existing `10U` per outcome, `30U` market/batch caps, T+18.840 timing, `1.1gwei` buy gas, and `open_timed_exit` T+25 sell policy. Bot5 was not retuned by this change.
- [x] 2026-07-03 patch Bot2 restored-auto-buy stale seen bug after the `$FXI FDV by end of July 7th?` miss. `$FXI` pre-signed tx `0x4f735979d0636de427cc454e04b776838bed1ade3fe1700a01dd4dae0bf91268` with nonce `652` at `2026-07-03T13:59:19Z`, but no `open-broadcast-scheduled` or fill appeared because the market already had a future `seen` key from the paused period; pre-sign did not check seen while scheduler did, so the raw tx was never broadcast. The tx is not found on-chain, and nonce `652` was later used by same-wallet tx `0x6a69e8339e74f06967fbea0f22a07993e9384e2197b1372491f1acf33416d0d1` at `2026-07-03T14:02:04Z` with `0.055063559gwei`, leaving Bot2 with `0.827589937189638326 BUSDT`. Patched `src/event-sniper.js` so startup, chain/REST/feed discovery, open-broadcast scheduling, and pre-sign clear stale seen keys only for markets that are still future, currently strategy-eligible, and not manually blocked; planned-buy-only override remains available for planned records. Added self-test coverage for non-planned future eligible global buys. Local and remote `npm run verify` passed; deployed backup `/opt/42space/src/event-sniper.js.bak-future-seen-autobuy-2026-07-03T14-12-47Z`, restarted Bot2 event/dashboard services, and readback shows buy gas `1.1`, buy allowlist `null`, auto-sell `open_timed_exit`, sell gas `0.15`, planned buys enabled `0`, but Bot2 is waiting for BUSDT top-up.
- [x] 2026-06-27 stage Bot5 as an independent Bot2-like execution profile in code/templates: middle-three `10U`, one market per open, `OPEN_BROADCAST_DELAY_MS=19900`, buy gas `6gwei`, `open_timed_exit` T+25 full exit with sell gas still `0.15gwei`, fast randomized open-exit enabled, and -10% full stop-loss. Production activation still waits for Bot5-specific funding and first-buy verification.
- [x] 2026-06-27 retune Bot5 default buy broadcast from `19985ms` to `19900ms` after comparing against current Bot2 production `18900ms`; restarted Bot5 event/dashboard/public-dashboard services, verified direct `4246` plus port-80 dashboard show `广播 T+19.9s / 超窗 20s`, and kept buy gas `6gwei`, sell gas `0.15gwei`, T+25 full exit, and fast randomized T+24.5s to T+26.0s open-exit unchanged.
- [x] 2026-05-31 lower premium watcher system priority and move it to a dedicated read-only RPC/WSS env overlay so it does not contend with Bot2's sniper RPC path.
- [x] 2026-05-31 set Bot1 to the same strict speed-first `T+19s` fanout with `T+20s` hard buy cutoff.
- [x] 2026-06-01 remove REST hydration from the premium watcher hot sampling path: WSS/receipt markets with outcomes now begin `simulateMint` immediately and hydrate metadata in the background, so close-open markets do not lose the first seconds to REST 404 lag.
- [x] 2026-06-01 add Bot2 single-market price gate mode: with bundles disabled and `EVENT_MAX_DUE_MARKETS_PER_OPEN=1`, the configured post-open action time checks selected outcomes via `simulateMint` and broadcasts the existing pre-signed transaction as soon as any effective price is below `0.0012`; the fixed-delay strategy remains available but disabled for this phase.

## Next

- [x] Deploy startup auto-approval to `47.83.23.65`.
- [x] Confirm production logs show `event-router-approval-startup`.
- [ ] Re-run live buy-path verification on the next eligible opening after the speed-first fallback correction.
- [ ] Compare next live buy against same-market first-block transactions and tune post-open broadcast/gas from evidence.
- [ ] Verify the Colombia vs Ghana Bot1 buy under fixed T+19.850, `2.2gwei`, and single-Ankr broadcast: scheduled target, timer jitter, first accepted RPC, block number/index, gas price, same-market top-10 rank, and whether it lands in the intended 20s block without reverting to a 19s block.
- [ ] Verify the next funded Bot3 exact-score planned or automatic buy under T+18.850 and `1.1gwei` using the auto-generated `bot3-buy-rank` report: scheduled target, timer jitter, first accepted RPC, block number/index, gas price, same-market top-10 rank, and whether Bot3 reaches rank 1.
- [x] 2026-07-10 deploy Bot3's dual-Builder atomic timed-buy capability under the initial default-off rollout. The profile initially defaulted explicit Builder plans to `builder_then_fanout`, `auto` target timing, a `500ms` preposition lead, and `0.001 BNB` tip; this baseline was later superseded by the global strict Bot2/Bot3 rollout recorded above. Per-event overrides remain authoritative.
- [x] 2026-07-10 verify the optimized builder hot path on Bot2 World Cup Winner volume. Readback showed `builderBundleTipPreSigned=true`, `builderBundlePayloadPrebuilt=true`, `builderBundleMaxBlockLookupEnabled=false`, payload-build latency `0ms`, and 48Club request/acceptance latency `254ms`, down from the prior MU `360ms` baseline. Buy and `0.004 BNB` tip receipts both succeeded in block `109095952` at txIndex `1/2`.
- [x] 2026-07-10 stage Bot3 `World Cup: Final Matchup` market `0x951B04c71Ff284C35e7C46554CbD1D7C9FA8F44b` for six named outcomes at `20U` each. The profile-local record uses `T+20`, buy gas `2.1gwei`, guarded Builder retries from `T+19.500` every `100ms`, `0.002 BNB` tip, and full 10-hour pre-start exit anchored to the official final kickoff. Only `42space-event@42space-3.service` was restarted; readback showed the plan uniquely matched, required `120U`, and BUSDT/BNB/allowance/RPC checks ready.
- [x] Audit the Bot3 Final Matchup opening. Pre-sign and `T+19.500` Guarded Builder scheduling worked, but wallet-budget mode signed the buy with Gas limit `10,870,554`, consuming nearly the entire BNB balance without preserving Guard/tip costs. Builder rejected the bundle as underfunded; only Guard nonce `156` mined, while buy/tip were dropped and no positions or auto-sell rows were created. The expired plan is disabled. The later nonce-157 `130 BUSDT -> 129.8071924 USDC` swap was separate from Bot3.
- [x] Fix all three guarded Builder funding checks: single-market funding preflight resolves the planned-buy's final Builder config instead of global defaults; `resolveExecutionGasLimit` reserves Builder tip, tip-transfer Gas, and Guard maximum Gas before sizing buy Gas; and pre-sign hard-checks total maximum signed cost against pending BNB. Add planned-Builder reserve coverage plus an incident-balance/`2.1gwei` regression, run local and remote `npm run verify`, deploy matching `src/fortytwo.js` and `src/event-sniper.js`, and restart only Bot3.
- [x] 2026-07-10 implement and deploy `TimedBuyExecutor` for Bot3 at `0x14e3f8172388FDDb85918792a63294F4C740343B`. Deployment tx `0xa623c035ef1888d7cd7bc24826e17d4e68fe5a7de36ade493b92d367bd189da8` and max BUSDT approval tx `0x1eff93faddc6dee12c59b15c39c8aece32126b3b039f57d4af3c192086758ed7` succeeded. Runtime code hash, owner, operator, Router, collateral, and allowance readback all matched.
- [x] 2026-07-10 implement provider-specific dual Builder pre-sign/submission. One common atomic buy is paired with separate 48Club and BlockRazor tips at the same nonce; only one tip can mine. BlockRazor strips 48Club-only payload fields and supports optional auth. Receipt/Gas tracking checks all candidate tips and records the winning mined tip.
- [x] 2026-07-10 enforce atomic public fallback and unsafe-path blocking. The public transaction is held until observed chain timestamp equals the target, target-second misses abort, and a missing atomic pre-sign cannot downgrade to a direct Router buy. Local and remote `npm run verify` passed, including dual-target, anonymous BlockRazor payload, same-nonce tip, and atomic calldata tests.
- [x] 2026-07-10 complete live no-transaction readback: 48Club and unauthenticated BlockRazor both returned BSC `0x38` through provider-appropriate warmup probes, and a future-target executor simulation decoded `TooEarly`. All Bot3 services were active with zero configuration ambiguity after restart.
- [x] 2026-07-10 obtain and install the BlockRazor auth token for Bot3 only. Stored the 96-character token in `~/.codex/secrets/42space-blockrazor.env` and `/etc/42space/profiles/42space-3.env` without printing or committing it; production backup is `/etc/42space/profiles/42space-3.env.bak-blockrazor-auth-20260710T112235Z`. Restarted only Bot3 event/dashboard services. Both services are active, `status` and `doctor` exit `0` with `authConfigured=true`, and an authenticated already-mined bundle probe reached nonce validation over HTTP 200 in `260ms` with the expected `nonce_too_low`, so no transaction or tip could execute.
- [ ] On the next Bot2 or Bot3 buy that uses the new global default, capture timed-buy/tip receipts, exact block timestamp/index, winning Builder, losing same-nonce tip `not_found`, Gas ledger rows, final nonce, and same-market rank. Confirm the transaction lands in T+19 and that no public RPC fallback occurs.
- [ ] After Bot2 automatic buying is re-enabled, verify the next T+18.840 direct first-three or planned-buy records scheduled timer latency, broadcast start, first accepted RPC, fanout settlement, raw-tx rebroadcast, selected outcomes, gas `1.1gwei`, and async receipt result.
- [ ] After Bot5 secrets/funding/allowance are installed, verify its first same-event Bot2-like buy path independently: profile role readback, T+19.900 schedule, first accepted RPC, receipt result, and auto-sell startup.
- [ ] Record next post-open scheduled timer latency, broadcast start, first accepted RPC, and async receipt result after the broadcast-only lock deploy.
- [ ] After US West cutover, compare first accepted RPC timing against the prior Hong Kong baseline.
- [ ] Fix `event:bench` to use the production discovery model. Current benchmark command is chain-future-only and can miss REST future markets.
- [x] Confirm current production env now matches `5 outcomes * 20U`.
- [x] Confirm current production env now matches `5 outcomes * 50U`.
- [x] Confirm current production env now matches `5 lowest-odds outcomes * 20U`.
- [x] Confirm current Bot2 production env now matches `3 middle outcomes * 10U`, `30U` market/batch caps, T+18.840 entry, buy gas `3.3gwei`, and profile-scoped T+25s timed exit.
- [x] Confirm current Bot2 production automatic buying is restored while auto-sell remains active.
- [ ] Add explicit per-market max cost display to dashboard.
- [ ] Make failed broadcast and receipt-drop reasons visible in the dashboard activity UI.
- [ ] Add a live incident replay for out-of-gas opening revert + expired-window manual rescue to prevent repeating the GTA failure mode.
- [ ] Add a replay for previous-block pre-open revert so `ALLOW_PREOPEN_BROADCAST=0` and terminal pre-signed discard cannot regress.
- [ ] Add a replay for over-cap gas signing so wallet-budget mode cannot regress past the BSC single-transaction gas cap.
- [ ] Add a production-log replay for the `$PENGU` duplicate planned-buy incident covering T+0/T+8/T+17 repeat execution and confirming the stale-seen override cannot requeue after market start.
- [x] Confirm auto-sell records partial sells clearly.
- [x] 2026-06-26 decouple auto-sell monitor startup from `ARM_WAIT_FOR_FUNDING`: `event:arm` now starts auto-sell before the buy-funding wait, reuses the same runtime/tx lock in `watch()`, pauses/waits around watch startup after funding recovery, syncs nonce after startup Router approval, and local plus remote `npm run verify` pass. Deployed the single-file code change to `/opt/42space/src/event-sniper.js`, backed up the prior remote file as `event-sniper.js.bak-autosell-funding-2026-06-26T10-25-21Z`, restarted only `42space-event@42space-3.service`, and verified Bot3 sold Norway vs France retained pre-start exits while still waiting for buy funding: tx `0x02c765b7501584048da7feffd8c2a028076ce6bcba6b9e1caec5ec14074b4b0b`, status `success`, block `106469110`, remaining chips `1065.22`, `1065.22`, and `560.22`.
- [x] 2026-06-26 add `ARM_FUNDING_NOTIFY_WINDOW_MS` so profiles can send Feishu waiting-for-funds cards earlier than the default 30-minute window without changing retry timing or buy logic.
- [x] 2026-06-26 install `scripts/bot4-first-buy-evidence.js` plus `42space-bot4-first-buy-evidence.timer` on `47.251.26.212`; the evidence schema records Bot4 targets independently, so OpenRouter Python expects T+19.900/`0.5gwei`, BNB/USDT expects T+22.000/`0.15gwei`, and Hermes is paused. Complete proof requires scheduled target, pre-sign, broadcast within each target window, RPC first-accepted time, selected outcomes, receipt, auto-sell monitor startup, current stop-loss-disabled config, and unintended-buy checks. If the 30m window expires without full proof, the latest report writes `conclusion=timeout`.
- [ ] Promote the current transaction lock into a full priority Tx Scheduler after the next live buy/sell proof, so all chain-writing actions share explicit priority state instead of direct function calls.
- [x] Add a builder-only mode for 48Club tests. `builder_only` keeps concurrent mode available as the default, but planned buys can now require builder-paid inclusion; Bot2 ASML proved buy+tip in the same block on 2026-07-09.
- [ ] Make one-off manual test helpers preserve profile-local output paths such as `GAS_LEDGER_FILE` even when `RUNTIME_CONFIG_FILE` points to a temporary file.
- [ ] Add a small replay covering duplicate prevention after restart.
- [ ] Review the next completed `event:premium-watch` close-open report and confirm sub-3s samples appear after the non-blocking hydration fix.
- [ ] Verify the next live central discovery feed event reaches each `EVENT_DISCOVERY=feed` bot and still schedules the normal pre-sign/open-broadcast path.
- [x] After the 2026-06-27 00:00 UTC daily-template opening, verify Bot4's scheduled T+19.985 buy evidence: BNB/USDT bought `$150M – $300M` and `$300M – $450M` successfully with `0.15gwei` gas in tx `0xe514fd3715886fa974532effe170caa0dd67210d5860fcd25adc87533fe352ad`; OpenRouter failed on the second nonce due the Chainstack in-flight delegated-account limit and was skipped at T+21.001 before retry.

## Bot3 Retained Settlement Auto-Sell

Scope: implement the Bot3-only retained-chip pre-start auto-sell behavior described in `docs/plans/03-execution-risk.md`. This task must preserve Bot1 and Bot2 behavior unless the operator separately asks for those profiles.

- [x] Add planned-buy `autoSell.retainPositions` parsing for Bot3 planned-buy records.
- [x] Normalize each retain entry by outcome name and retain percentage.
- [x] Reject invalid retain percentages, missing outcomes, duplicate normalized outcomes, and retained outcomes that are not in the planned-buy selected outcome list.
- [x] Keep whole-match retention out of scope unless explicitly requested; every retained outcome must be listed.
- [x] Extend `autoSellConfigForPosition` or adjacent helpers so each position can resolve its retained target from its planned-buy record.
- [x] Add a helper that computes retained target chips from `entry.initialSize * retainPercent / 100`.
- [x] Make rounding conservative so the sell amount never reduces the position below the requested retained target.
- [x] Update `buildPreStartExitAutoSellAction` so retained outcomes sell only `currentSize - retainedTargetSize`.
- [x] Keep non-retained outcomes on the current full `pre_start_exit` behavior.
- [x] When current chips are already less than or equal to the retained target, return a no-sell retained-to-settlement result instead of selling.
- [x] Preserve Bot3 default `stopLossEnabled=false`; do not re-enable stop-loss while adding retained settlement behavior.
- [x] If a future record enables stop-loss with retained positions, ensure stop-loss cannot sell below the retained target unless a separate explicit override exists.
- [x] Update auto-sell state marking so retained partial pre-start sells do not set `remainingSize` to `0`.
- [x] Add retained-to-settlement state metadata after successful retained partial sell or no-sell retained skip.
- [x] Make fills/activity logs distinguish full pre-start exits from retained partial pre-start sells.
- [x] Include market, outcome, tokenId, retain percent, retained target chips, sold chips, current chips before sell, due time, tx hash, and status in retained sell logs.
- [x] Keep operator preapproval, batching, gas checks, buy-window pauses, nonce lock, and transient positions API retry behavior on the existing auto-sell path.
- [x] Add self-tests for 1000 initial chips with 10% retained producing a 900-chip sell.
- [x] Add self-tests for a non-retained outcome in the same match still selling 100%.
- [x] Add self-tests for current chips already below target producing no sell and marking retained-to-settlement.
- [x] Add self-tests for `stopLossEnabled=false` on the Bot3 retained-settlement path.
- [x] Add self-tests for invalid retained outcome names, duplicate names, and invalid retain percentages.
- [x] Run `npm run verify`.
- [x] Before any production Bot3 edit, read `/opt/42space/data/42space-3/planned-buys.json` and create a timestamped backup next to it. Backups created on 2026-06-26: planned-buy backup `/opt/42space/data/42space-3/planned-buys.json.bak-retain-2026-06-26T09-09-10-475Z`; code backup `/opt/42space/src/event-sniper.js.bak-retain-2026-06-26T09-08-05Z`.
- [x] Apply retained positions only to the operator-specified Bot3 matches/outcomes. Applied `10%` retain positions to all planned outcomes for Norway vs France, Uruguay vs Spain, New Zealand vs Belgium, and Egypt vs IR Iran; applied only `CPV 0-1 KSA` and `CPV 1-1 KSA` for Cabo Verde vs Saudi Arabia; applied only `JOR 0-1 ARG` and `JOR 0-2 ARG` for Jordan vs Argentina.
- [x] Restart only `42space-event@42space-3.service` after planned-buy config changes unless dashboard rendering code also changes. Restarted `42space-event@42space-3.service`; dashboard rendering code did not change, so `42space-dashboard@42space-3.service` was not restarted.
- [x] Verify production with `systemctl is-active`, recent journal logs, and planned-buy config readback. Remote `npm run verify` passed, event/dashboard services were active, planned-buy readback showed the expected `retainPositions`, and no new `planned-buys-load` warning appeared after the helper fix and final Bot3 restart.
- [x] Report which Bot3 outcomes retain chips, each retain percentage, and which outcomes still fully exit 10 hours before kickoff. Retain `10%`: `NOR 1-2 FRA`, `NOR 1-1 FRA`, `NOR 0-2 FRA`, `URU 1-2 ESP`, `URU 0-1 ESP`, `URU 0-2 ESP`, `CPV 0-1 KSA`, `CPV 1-1 KSA`, `NZL 0-2 BEL`, `NZL 0-3 BEL`, `NZL 1-2 BEL`, `EGY 1-0 IRN`, `EGY 1-1 IRN`, `EGY 2-1 IRN`, `JOR 0-1 ARG`, `JOR 0-2 ARG`. Full 10-hour pre-start exit remains for `CPV 0-2 KSA` and `JOR 1-2 ARG`.
- [x] 2026-06-30 enroll the Bot3 manual website-bought `France vs Sweden` exact-score positions into retained auto-sell management, then update them from `30%` to `10%` retention after the operator correction. The planned-buy-style auto-sell override `bot3-france-sweden-web-retain-30-2026-06-30` on market `0xA3653aDdD6928Eb0De992EE245b6c4337500447C` now has explicit `kickoffAt=2026-06-30T21:00:00.000Z`, `pre_start_exit`, stop-loss disabled, and `10%` retention for `FRA >=4-1 SWE`, `FRA >=4-0 SWE`, `FRA 3-1 SWE`, and `FRA 3-0 SWE`. Seeded `/opt/42space/data/42space-3/auto-sell-positions.json` from live positions because the website buy had no Bot fill log; initial/current sizes are `7634.86`, `7634.86`, `6442.71`, and `6442.71` chips, so retained targets are `763.486`, `763.486`, `644.271`, and `644.271` chips. Backups include `/opt/42space/data/42space-3/planned-buys.json.bak-france-sweden-retain30-web-2026-06-29T16-36-18-668Z`, `/opt/42space/data/42space-3/auto-sell-positions.json.bak-france-sweden-retain30-web-2026-06-29T16-36-18-670Z`, `/opt/42space/data/42space-3/planned-buys.json.bak-france-sweden-handicap-retain10-20260630T091049898Z`, and `/opt/42space/data/42space-3/auto-sell-positions.json.bak-france-sweden-handicap-retain10-20260630T091049898Z`. Restarted only `42space-event@42space-3.service`; service is active, readback shows four seeded entries with `seededForRetainPercent=10`, and planned-buy retain config is `10%`.
- [x] 2026-06-30 add and execute Bot3 `France vs. Sweden - Handicap` buy without touching Bot1. The Bot3 planned-buy record `bot3-france-sweden-handicap-france-minus-1p5-2026-06-30` targets market `0x8f14974D980d971c606a9b2f5862285ADA77C55d`, outcome `France −1.5`, `10U`, `openBroadcastDelayMs=18850`, per-record buy gas `1.1gwei`, `pre_start_exit`, stop-loss disabled, and `kickoffAt=2026-06-30T21:00:00.000Z`; the 10h pre-start auto-sell due time is `2026-06-30T11:00:00Z`. Bot3 pre-signed tx `0x733a7e9e49362a2cbbb559b2205658e83f4d16e73b2692c72df45a1a6786f57c` with nonce `84`, broadcast at `2026-06-30T09:15:18.852Z`, first accepted via Ankr at `49ms`, Chainstack accepted at `333ms`, and receipt succeeded in block `107227206` with gas used `455056` and effective gas price `1.1gwei`. Positions readback shows tokenId `1` `France −1.5`, size `5686.76`, cost basis `9.9402U`; fills and seen state contain the tx, and `autosell` readback reports the market operator approval ready.

## Bot3 Runner-Up Orderflow Trigger Sell

- [x] Add `scripts/orderflow-trigger-sell.js` as a Bot3 sidecar that polls a single market's chain `MarketTrade` logs, filters watched token IDs, ignores Bot3's own wallet, and triggers only on positive watched-outcome buys at or above the configured market threshold.
- [x] Keep the trigger sell scoped to `matched_outcomes` by default, so a large external buy of France sells Bot3's France position only; a multi-outcome triggering transaction sells only the matched watched outcomes from that transaction.
- [x] Use Bot3's existing direct sell helpers, preapproved market operator, `minOut=1`, and Bot3 sell gas default `0.15gwei`; do not change Bot3 buy timing, planned buys, retained pre-start exits, or other bot profiles.
- [x] Add `ops/42space-bot3-runner-up-orderflow-sell.service` with market `0x6B7F30fb52B26814BB49312442010450e43e226D`, watched token IDs `32768,134217728,65536,2`, threshold `200U`, poll interval `1000ms`, future-only lookback `0`, and profile-local state/log files under `/opt/42space/data/42space-3`.
- [x] Verify locally with `node --check scripts/orderflow-trigger-sell.js`, `node scripts/orderflow-trigger-sell.js --self-test`, and `npm run check`.
- [x] Deploy to `47.251.26.212` after backups, run remote `node --check` plus self-test, run a short dry-run against Bot3 production RPC/state, then enable `42space-bot3-runner-up-orderflow-sell.service`.
- [x] Verify production service state: systemd active, Bot3 event/dashboard still active, startup JSONL records execute mode and `0.15gwei` sell gas, operator approval succeeded in block `107175438`, and state advances from `lastProcessedBlock` with no trigger yet.
- [x] 2026-06-30 isolate the orderflow monitor read RPC from Bot3's normal profile RPC. Added `ORDERFLOW_TRIGGER_RPC_URL` support to `scripts/orderflow-trigger-sell.js`, added an optional `EnvironmentFile=-/etc/42space/profiles/42space-3-orderflow.env` to the service template, wrote the operator-provided Chainstack HTTP RPC only to the root-only production env file, restarted the monitor, and verified the service/env file are active, Bot3 event/dashboard stayed active, recent journal has no warnings, and state continues advancing.
- [x] 2026-06-30 harden orderflow transaction state so a failed watched transaction is not skipped. `seenTxs` now means handled, not merely observed; failed watched transactions are stored in `failedTxs` with serialized trade rows, retry attempts, failure message, and `nextRetryAt`; retry success moves the tx to handled and clears the failed queue. Local `npm run check` and `git diff --check` passed, the script was deployed after backup, remote `node --check` plus self-test passed, the service was restarted, Bot3 event/dashboard stayed active, and production state shows empty `failedTxs`/`processingTxs` with advancing `lastProcessedBlock`.
- [x] 2026-07-07 expand orderflow-trigger sell from Bot3 Runner-Up single-market coverage to four profile/market services: Bot3 Runner-Up, Bot3 3rd Place, Bot1 Runner-Up, and Bot1 3rd Place. Each service keeps independent state/log files, watches only current held token IDs, uses the configured external-buy threshold, `matched_outcomes` mode, and `100%` sell percent. Bot3 Runner-Up watched token IDs were refreshed from live positions to include Norway and drop Spain. Production verification passed: all four services are enabled/running, Bot1/Bot3 event and dashboard services remain active, startup approvals read `ready` with no new approval tx needed, state files advanced to current blocks, and all failed/processing queues were empty.
- [x] 2026-07-09 lower both Bot1 and Bot3 Runner-Up orderflow-trigger sell thresholds for market `0x6B7F30fb52B26814BB49312442010450e43e226D` from `200U` to `150U`.
- [x] 2026-07-09 lower both Bot1 and Bot3 3rd Place orderflow-trigger sell thresholds for market `0x86308B8059183eA443fd1885D5493cF6C5222F1f` from `200U` to `150U`. Production readback after restart showed both 3rd Place services active with `ORDERFLOW_TRIGGER_THRESHOLD_USDT=150`, startup logs using threshold `150`, and empty failed/processing queues.
- [x] 2026-07-07 add Bot1's dedicated uncommitted orderflow RPC env file at `/etc/42space/profiles/42space-orderflow.env`. The file is root-only `600`, stores only `ORDERFLOW_TRIGGER_RPC_URL` and `ORDERFLOW_TRIGGER_WS_URL`, both Bot1 orderflow services were restarted and confirmed to load the EnvironmentFile, state continued advancing, failed/processing queues stayed empty, and a no-secret `eth_blockNumber` health check against the dedicated HTTP RPC succeeded.
- [x] 2026-07-07 make orderflow-trigger sell recognize website/manual re-buys for watched markets. Services can set `ORDERFLOW_TRIGGER_WATCH_CURRENT_POSITIONS=1` to merge live held token IDs into the watched set, and `soldTokenIds` no longer blocks a token that has a fresh on-chain balance. This covers Bot3 Runner-Up Spain after it was previously sold and then manually bought again, plus manually added 3rd Place England exposure. Production verification after deploy showed Bot3 Runner-Up dynamically added Spain tokenId `134217728` back into `watchedTokenIds` while retaining the old `soldTokenIds` audit entry, all four World Cup orderflow services active, failed/processing queues empty, and remote `npm run check` passing.
- [x] 2026-07-11 stop the Bot3 Spain-vs-Belgium orderflow sidecar's historical BigInt retry loop without changing sell behavior. The no-position audit now stores a JSON-safe trigger summary instead of the raw viem receipt, so the two stale transactions moved from `2688` failed attempts each to handled `no_position`; production state now has empty `failedTxs`/`processingTxs`, a cursor within two blocks of head, and no post-restart errors.
- [x] 2026-07-11 add zero-network-overhead per-method RPC telemetry to the orderflow sidecar's existing read transport. The first production window covered `61.214s` with `104` requests and `0` errors: `51 eth_blockNumber`, `51 eth_getLogs`, and the two final historical `eth_getTransactionReceipt` retries. The next two steady windows each used `102` requests with no receipt calls or errors. Metrics contain counts and latency only, never RPC URLs or credentials. Local and remote `npm run verify` passed; only `42space-bot3-spain-belgium-orderflow-sell.service` restarted, while all five event workers, all five dashboards, and all five orderflow services remained active.
- [x] 2026-07-11 build and deploy `scripts/shared-rpc-observer.js` plus `42space-shared-rpc-observer.service` in shadow-only mode. It owns no profile env, wallet, signer, nonce, threshold, webhook, or execution helper. WSS subscribes only to `newHeads` and the production raw `MarketTrade` topic for three markets; canonical HTTP reads each full block once, audits markets every 60 seconds, and scans six multi-address Transfer topic shapes every 3 seconds with independent cursors.
- [x] 2026-07-11 validate the shared observer locally and in production without restarting existing readers or trading workers. Raw historical replay reproduced one known direct-address hit and one known Transfer hit exactly. A 5-second observer-only stop/start backfilled `129` market-audit blocks in `272ms`, `35` Transfer blocks in `298ms`, and the direct block cursor returned to a 2-3 block lag with zero errors. The first full production window used `274 RPC/min` with zero errors; current readers are approximately `1329 RPC/min`, so post-gate replacement projects a `79.4%` reduction rather than counting temporary shadow traffic as savings.
- [x] 2026-07-11 deploy `shared-rpc-observer-report.js` with a five-minute systemd timer. The report compares observer feed keys and first-seen times against all five orderflow logs and all three address-watcher logs, checks health freshness, subscriptions, errors, duplicate keys, the 24-hour window, sample counts, and the `100ms` p95 latency gate. Initial production report correctly returns `ready=false` only for incomplete duration and missing live orderflow/address samples.
- [x] 2026-07-11 reconcile live RPC endpoint fingerprints with the operator-provided Chainstack organization. All 17 Chainstack HTTP/WSS endpoints loaded by running `42space` processes matched platform nodes. Bot1/Bot3 Event nodes remain distinct from the two transaction-monitor nodes used by orderflow/address readers; no API key or raw endpoint was written to the repo or logs.
- [x] 2026-07-11 add persistent JSONL feed input to `orderflow-trigger-sell.js` and `address-tx-watch.js` while retaining `rpc` as the default. The orderflow feed path expands every target-market trade from the receipt before applying the unchanged threshold/sell path; the address path hydrates the transaction and receipt before applying the unchanged direction, transfer, 42-event, and Feishu logic. Empty feed polls do not write state.
- [x] 2026-07-11 pass production-data, no-execution feed canaries. The orderflow canary recovered the real token `65536` buy at block `109318774`, measured `9.961846U`, stayed below the artificial `999999999U` threshold, and produced no sell/action/error. The address canary reproduced all compared fields for one existing hit, including direction, transfer count, receipt status, and one 42 event, with Feishu disabled and zero errors.
- [x] 2026-07-11 deploy the feed-capable scripts and checkpoint writer after matching local/remote hashes and passing local plus production `npm run verify`. No source override exists in any production profile, so all five orderflow and three address readers remain on RPC mode. Restart only the shared observer; Bot1/Bot3 Event, orderflow, value-sell, and address PIDs remained unchanged. The observer returned with `2/2` subscriptions, zero WSS errors, and a dry-run feed cursor advanced from block `0` to `109322161` using checkpoints with zero trades/actions/errors.
- [x] 2026-07-11 pause address monitoring at operator request. Disable and stop all three address-watcher services, then verify all five Event workers, all five orderflow services, and the Bot3 value watcher remain active with unchanged PIDs.
- [x] 2026-07-11 deploy orderflow-only observer mode with `SHARED_OBSERVER_ADDRESS_ENABLED=0`. It retains `newHeads` and `MarketTrade` WSS subscriptions but performs no direct-block or Transfer scan; health exposes null address cursors and the report sets `minAddressSamples=0`. Two consecutive full production windows each used exactly `2 RPC/min` (`1 eth_blockNumber + 1 eth_getLogs`) with zero errors.
- [x] 2026-07-11 audit the five main auto-sell workers. Bot1/Bot3/Bot4 currently make no recurring Chainstack auto-sell quote; Bot2/Bot5 have no positions and also make none. Keep Bot2/Bot5's position-time 1-second stop-loss quotes and all due/trigger execution reads unchanged.
- [x] 2026-07-11 add method telemetry and a process-local operator cache to the Bot3 value watcher. It keeps its 5-second balance and redeem simulation; when a position exists, steady quote cost falls from three to two `eth_call` requests per cycle. The first full production window confirmed current `no_position` state at `0 RPC/min` and zero errors.
- [x] 2026-07-11 deploy the main auto-sell positive operator-approval hint without restarting production workers. Once a market is confirmed in `autoSellOperatorReadyMarkets`, stop-loss quotes skip only the repeated `isOperator`; the 1-second cadence, balance read, redeem simulation, trigger logic, direct-plan read, and execution-time approval check remain unchanged. Local and remote `npm run verify` passed, hashes matched, and all five Event PIDs stayed unchanged. Bot4 was `armed` with two prepared buys, so a restart was correctly rejected.
- [x] 2026-07-11 load the main auto-sell approval hint through a cold one-profile-at-a-time restart. Bot1/Bot2/Bot3/Bot5 had zero pending/prepared buys; Bot4's two prepared markets opened 950 minutes later and every profile had no transaction lock. Restart order was Bot1, Bot2, Bot3, Bot5, then Bot4, with fresh cgroup-matching health before each next step. Bot4 rebuilt both prepared plans and funding readiness. Strategies, position counts, and poll settings stayed unchanged; all startup guards cleared into successful auto-sell scans with zero errors; all five wallets kept identical contiguous latest/pending nonces (`241`, `845`, `183`, `103`, `13`); all Event units remained active with `NRestarts=0`; and orderflow/value/observer PIDs did not change. A live read-only Bot1 quote canary then confirmed the active path: baseline `balanceOf + isOperator + redeem simulation = 3` calls, cached `balanceOf + redeem simulation = 2`, with identical sell amount and expected collateral.
- [x] 2026-07-11 make the first RPC-to-feed reader restart gapless. A reader without a feed cursor replays from one block before its persisted RPC cursor, ignores only older backlog during that initial batch, and then clears the temporary boundary so delayed audit/backfill rows remain valid. An isolated production canary started from RPC block `109327792`: replay boundary `109327791`, four rows read, three target-market rows, two overlap/gap rows retained, one older row skipped, cursor advanced to block `109327794`, zero RPC, zero failed/processing transactions, and no trigger/execution/error. Local and remote `npm run verify` passed; all five formal reader PIDs remained unchanged in RPC mode.
- [x] 2026-07-11 prepare per-reader source switching and rollback without restarting readers. All five base units now explicitly set `SOURCE=rpc`, the shared feed path, `100ms` feed polling, and no historical replay. `ops/42space-orderflow-feed-source.conf` overrides only `SOURCE=feed` when installed as one unit's drop-in; removing it restores RPC. Remote `systemd-analyze verify` passed apart from unrelated cloudmonitor warnings, daemon-reload left all five original PIDs active with `NRestarts=0`, effective source is `rpc`, and no drop-ins are installed.
- [x] 2026-07-14 close the failed Ankr shadow window without touching trading execution. The 57.6-hour report contained `53/53` matched existing orderflow events, zero misses, and zero duplicate dispatches, but Ankr WSS began returning `401` at `2026-07-13T06:56:13Z`; 12 events then arrived through the 60-second HTTP audit and p95 regressed by `52.514s`. No reader was cut over under that failed window.
- [x] 2026-07-14 remove Ankr from the shared observer and switch it to the existing Bot1 transaction-monitor Chainstack WSS. A no-secret preflight received three consecutive heads, observer restart returned `2/2` subscriptions, and the new window advanced more than 1,000 heads with zero WSS errors. Install `ops/42space-shared-rpc-observer-fast-audit.conf` during burn-in so canonical Chainstack HTTP audits all three markets every second; the first full window used `120 RPC/min` with zero errors.
- [x] 2026-07-14 cut all five orderflow readers to feed one at a time. Preflight showed Bot1/Bot3 had no pending/prepared buys or transaction locks and every orderflow failed/processing queue was empty. Spain-Belgium moved first, then Bot3 3rd Place, Bot3 Runner-Up, Bot1 3rd Place, and Bot1 Runner-Up. Every reader completed its overlap boundary, retained an advancing feed cursor, stayed active with empty failed/processing queues, and emitted a full `0 RPC/min`, zero-error window. Event worker PIDs were unchanged.
- [ ] Keep the one-second shared HTTP audit for a clean Chainstack-only production burn-in. After at least 24 hours with fresh WSS heads, zero observer errors, successful real orderflow dispatch, and no feed-reader errors, remove `10-fast-audit.conf`; verify the observer returns to the base 60-second audit and approximately `2 RPC/min` before closing RPC consolidation.
- [x] 2026-07-04 add Bot3 exact-score auto-buy funding fallback so the first `1U * 3` practice strategy can arm without waiting for the older generic unknown-batch upper bound. Planned buys and known next batches still compute their real selected stake before execution.
- [x] 2026-07-04 production readback confirms the fallback is live for Bot3: `requiredBusdt=3`, generic upper bound remains `45`, wallet BUSDT/allowance/BNB are ready, and the event worker entered discovery feed watch after restart.
- [x] 2026-07-05 production target for Bot3 exact-score auto-buy funding fallback is now `requiredBusdt=15` when no concrete next batch is known. Readback after restarting only Bot3 event/dashboard shows `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT=5`, `reason=bot3_fifa_exact_score_auto_single_market_fallback`, `genericUpperBoundRequiredBusdt=45`, wallet BUSDT/allowance/BNB ready, and discovery feed watch active.
- [x] 2026-07-06 production target for Bot3 exact-score auto-buy is now `requiredBusdt=30` for the three-outcome basket after raising `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT=10`. Readback after restarting only Bot3 event/dashboard shows the next known batch `Norway vs England`, `nextBatchRequiredBusdt=30`, `genericUpperBoundRequiredBusdt=45`, BUSDT above the requirement, and BNB below the `4.4gwei` fast-buy reserve.
- [x] 2026-07-06 fix Bot3 live Gas ledger USDT valuation after the France vs Morocco buy/approval/auto-sell showed `gasFeeBnb` recorded but `gasFeeUsdt=null`. The real-time receipt and execution-action paths now fetch the block-minute BNB/USDT close and pass `bnbUsdtPrice`/`bnbUsdtSource` into `buildGasLedgerEntry`, matching the historical backfill behavior. Deployed `src/event-sniper.js` and `src/gas-ledger.js` to `/opt/42space` after backups, remote `npm run verify` passed, restarted `42space-event@42space-3.service` and `42space-dashboard@42space-3.service`, and ran Bot3 `gas:backfill --limit 100`. The France vs Morocco entries are now priced: buy tx `0xeef535ef6896fbf4c27e9f11eeb449df537b4c5fa63a390e3ede168f94063594` `0.0052147172 BNB / 3.05582428U`, approval tx `0xf385f542430523f98121f83baebbad4b416658e295bd5699aa0e8445e7ffbf82` `0.0000069441 BNB / 0.00406855U`, and sell tx `0x46b7bbbef8a7a19dd592c5cafebdacc36463374d7cb99350389bb6c15b62ebf0` `0.00009388125 BNB / 0.05500502U`.
- [x] 2026-07-06 lower Bot3 global/default buy gas, including the FIFA/Sports exact-score automatic buy path, from `4.4gwei` to `1.1gwei` without touching Bot1/Bot2/Bot4/Bot5 or per-record planned-buy gas overrides. Backups were created at `/etc/42space/profiles/42space-3.env.bak-buygas-1p1-20260706T050807Z` and `/opt/42space/data/42space-3/runtime-config.json.bak-buygas-1p1-20260706T050807Z`; production env/runtime now read `GAS_PRICE_GWEI=1.1`, `gasPriceGwei=1.1`, `OPEN_BROADCAST_DELAY_MS=18850`, and sell gas `0.15gwei`. Restarted `42space-event@42space-3.service` and `42space-dashboard@42space-3.service`; program status readback shows next known batch `Norway vs England`, `requiredBusdt=30`, gas reserve `0.00374 BNB` at `1.1gwei`, and current wallet BNB/BUSDT ready.
- [x] 2026-07-07 expand Bot3 FIFA/Sports exact-score automatic buy selection from three outcomes to five outcomes per selected win side. The side decision still uses the original three-score tier rows (`1-0`, `2-0`, `2-1` versus `0-1`, `0-2`, `1-2`); after the side is chosen, the lower-price away side now buys `0-1`, `0-2`, `0-3`, `1-2`, and `1-3`, while the lower-price home side buys the mirrored `1-0`, `2-0`, `3-0`, `2-1`, and `3-1`. The funding fallback, Router approval threshold, and wallet-budget cap now use `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT * 5`, so the current `10U` auto stake requires `50U` instead of the old `30U`; planned buys still override this selector.

- [x] 2026-07-11 deploy and verify Bot4-owned atomic timed executor `0x6034a8672020114673e320203b1bA6692F53Ec50`, approve BUSDT, enable authenticated BlockRazor plus 48Club for Bot4, and configure only the OpenRouter recurring plan as strict `builder_only` T+20. The plan privately submits at T+19.500, uses `positionFirst`/`noMerge`, `0.001 BNB` tip, `0.5gwei` buy gas, and no public fallback. Readiness, both Builder probes, local/remote verification, service health, market/outcome presence, funding, allowance, and nonce checks pass. Bot4 BNB/USDT remains RPC T+22 at `0.15gwei`.
- [x] 2026-07-13 widen only Bot4 OpenRouter's private Builder retry window from `500ms` to `700ms`, moving first private submission from T+19.500 to T+19.300 while retaining the T+20 target, dual Builder, `builder_only`, `positionFirst`/`noMerge`, `0.001 BNB` tip, `0.5gwei` buy gas, and no public fallback. Keep Bot4 BNB/USDT unchanged at RPC T+22/`0.15gwei` and align readiness/evidence expectations with the new lead.
- [x] 2026-07-14 implement the deferred Bot4 exact-second Roadmap after both Builders rejected the old zero-width T+20 payload and the failed-buy nonce gap blocked BNB/USDT. Deploy exact executor `0xC2B2F78C620228Ea8d1B2E155664ceBbc7212148`, use `[T+20,T+21)` Builder hints, reconcile all dependent pre-signs from the failed buy nonce, make dedicated retries honor `EXECUTION_RETRY_MS`, and abandon strict buys at T+21. Contract semantics, ownership/config/allowance, local/remote verification, Bot4 readiness, service health, contiguous `120/120` nonce, and a no-broadcast July 15 dual-Builder real-plan pre-sign all pass.
- [ ] Capture the first post-repair Bot4 OpenRouter receipt proof: provider acceptance/rejection timing, exact target timestamp, block timestamp/index, winning tip, no public fallback, both outcome balances, nonce continuity before BNB/USDT, and market rank. If the exact-second buy misses, confirm no late buy and that BNB/USDT re-signs from the recovered chain nonce.
- [x] 2026-07-11 replace Bot4 OpenRouter's 70% full-exit quote gate with plan-local 42 REST outcome price targets: `DeepSeek V4 Flash >= 0.0017` and `Non-listed Model >= 0.0020`, each selling `100%`. The monitor uses one positions REST request per second for ten minutes after a matching buy and then restores the profile's 60-second interval; no full-exit quote RPC is made below target. A plan-local `priceApplyAfterIso` cutover excludes older positions unless the operator explicitly enables existing-position application. Self-tests cover no-RPC below target, terminal sell behavior at target, cutover behavior, and 1s-to-60s scheduler restoration.
- [x] 2026-07-12 add and deploy generic planned-buy `stakeByOutcomeUsdt` overrides while keeping `stakePerOutcomeUsdt` as the fallback. Direct and quoted plans, funding selection, execution risk caps, receipts, and Gas-ledger allocation now resolve each selected outcome independently. Bot4 OpenRouter resolves to `Non-listed Model=20U` and default `DeepSeek V4 Flash=10U`, total `30U`; BNB/USDT remains unchanged at `10U` each. Local and remote `npm run verify` passed, production plan construction returned per-outcome stakes `20/10`, runtime caps read `20/30/50`, Bot4 Event/Dashboard are active with `NRestarts=0`, and the current sell monitor retained three checked positions with zero errors after restart.
- [x] After the next Bot4 OpenRouter opening, capture receipt-level proof: private submission and provider acceptance times, target timestamp, block timestamp/number, transaction index, winning Builder and paid tip, absence of public broadcast, selected outcomes, fill status, and same-market rank. Completed on 2026-07-11 with the rank-1 proof below.
- [x] 2026-07-11 capture Bot4's strict T+20 proof. OpenRouter tx `0xb1d3f3a42dd2fb1f00ac63f9fe2dba5f6400732cfc18624d8c91cac5386e57b8` landed in first T+20 block `109263919`, txIndex `1`, and ranked first overall. 48Club accepted in `239ms` and received the only mined `0.001 BNB` tip; BlockRazor timed out, its same-nonce tip was absent, and no public fallback occurred. Fix the evidence false timeout so the stored report now concludes `complete`.
- [x] 2026-07-11 deploy and verify Bot1 executor `0xe40b5c53d2C6566219f85431a334BA9692d0d6E4` and Bot5 executor `0xe8e714bE74480788cABe470935DEb82236793bc3`, including runtime hash, owner/operator, Router/collateral, max BUSDT allowance, and contiguous nonce checks.
- [x] 2026-07-11 enable Bot1 and Bot5 globally on strict dual Builder T+20: T+19.500 private submission, `builder_only`, `0.001 BNB`, `1.1gwei`, authenticated BlockRazor plus 48Club, `positionFirst`/`noMerge`, and no public fallback. Preserve both profiles' selection, stake, and sell policies.
- [x] 2026-07-11 validate shared BlockRazor auth for the current load. Official sendBundle limits are unlimited for BPS and accepted bundles per block; six simultaneous authenticated empty-bundle probes passed `6/6` in `473-558ms`.
- [x] 2026-07-11 harden `deploy-timed-buy-executor.js` against cross-RPC read-after-write lag by polling deployed bytecode and BUSDT allowance after successful receipts. Bot1's successful deploy/approval exposed the stale immediate read; the repaired helper completed Bot5 without false failure.
- [x] 2026-07-12 retune Bot1 production defaults only: strict T+20 Builder remains unchanged, buy gas is `2.1gwei`, winning-provider tip is `0.005 BNB`, and automatic selling is RPC-only `open_timed_exit` at a random T+23-27 for `100%` with `0.15gwei` sell gas and stop loss disabled. Back up env/runtime config, restart only Bot1 Event/Dashboard, and verify process env, Strategy API, heartbeat, five-position scan with `triggered=0`/`errors=0`, and unchanged Bot3 config. Existing Runner-Up/3rd Place holdings keep event-level ten-hour `pre_start_exit` overrides.
- [x] 2026-07-13 raise Bot1 exact-score market/batch caps from `45U` to `50U`, restoring preparation of the five-outcome x `10U` selector, and raise Bot3's global Builder tip from `0.001` to `0.003 BNB`. Backups were created before both production edits; Bot1 and Bot3 returned `prepared=2/2`, all four Event/Dashboard units stayed active, and Bot3's France-Spain plus England-Argentina buys both mined in strict T+19 with paid `0.003 BNB` winning tips.
- [x] 2026-07-13 fix the Bot1 fast-exit/future-buy nonce collision exposed by adjacent France-Spain and England-Argentina openings. France-Spain buy nonce `244` mined, but the post-buy market approval consumed nonce `246`, invalidating England-Argentina's already pre-signed buy nonce `246`; the fast sell at nonce `248` then had a nonce gap and fell back to the normal monitor, which sold all five France-Spain outcomes successfully in tx `0xff081a82085bc4663f843d2b62f481dc26ebc975d5b687a1b36ee8b365056cc2`. The repair pre-approves markets before buy signing, serializes the fast-exit buy lane, skips precision sell when an earlier pending buy has priority, and refuses nonce-consuming post-buy approval. Local and production `npm run verify` pass with a dedicated regression check; the production source hash matches local, only Bot1 Event restarted, and its worker/buy/sell health is green with `NRestarts=0`.
- [x] Fund Bot1 above the conservative `0.02 BNB` five-outcome buy-plus-sell requirement. After the 2026-07-13 live buy, Builder tip, approval, and fallback sell, dashboard readback still shows `0.037947 BNB` and `105.49 BUSDT` with wallet state ready.
- [x] Capture Bot1's first strict T+20 live receipt/rank proof. France-Spain tx `0x34ebd4385ec9b7bf73c5dfac01d10a915f7cde77055c5070d92a1564eba4d295` landed exactly at T+20 in block `109668732`, txIndex `5`, gas `2.1gwei`, rank `4` overall and `3` in the target second/first T+20 block. BlockRazor's `0.005 BNB` tip mined in the same block, the 48Club same-nonce tip was absent, and public fallback was not used. Evidence is stored in `/opt/42space/output/bot1-buy-rank/latest.json`.
- [x] Capture Bot5's first strict T+20 live receipt/rank proof. CRCL tx `0x16e893838769b65a16fd0633e337ad8d1d95e992a872142a06ef01103b5fc97c` landed in the first T+20 block at txIndex `5`, market rank `4`, with 48Club's `0.001 BNB` tip at txIndex `6` and no public fallback. The buy lane passed; the timed-exit lane exposed the separate Builder/pause defect documented below.
- [x] 2026-07-11 keep Bot2/Bot5 Meme valuation outside the hot path: shared discovery locks explicit outcome names at first observation, persists them across restarts, and never performs an opening-time refresh or re-sign.
- [x] 2026-07-11 production acceptance for the Meme selector: premium watcher, Bot2, and Bot5 are active with zero restarts; both workers report `armed 3/3`, fresh PID-matching heartbeats, and auto-sell errors `0`; deployed source hashes match the local verified tree. Bot2 remains T+18.840 and Bot5 remains T+19.900 with their existing independent signing, nonce, Builder, and sell paths.
- [x] 2026-07-11 retune only Bot5 `open_timed_exit` from T+24.5-26 to randomized T+40-50. Keep Bot2 at T+24.5-26, both sell gas values at `0.15gwei`, and both -10% stop-loss rules unchanged. Production env/runtime/dashboard readback and the expanded 22-check strategy test confirm the profile boundary.
- [x] 2026-07-11 stage the dual-profile CRCL live test. Both Bot2 and Bot5 select `$50 - $60`, `$60 - $70`, and `$70 - $80` at `5U` each through planned-buy, inherit their profile Builder timing/gas, and preserve automatic timed exit plus stop loss. Both workers are `armed 4/4`; status shows the plan funded/prepared with no prepare error; profile-signer offline pre-sign produces reusable 3-outcome transactions; corrected due-path simulation drains pending to zero and marks seen without broadcast; Executor runtime code, operator, allowance, Router/collateral, and contiguous nonce checks pass. Pre-open market Router operator approvals mined successfully and were recorded in each Gas ledger.
- [x] 2026-07-11 isolate execution rehearsal side effects after production-profile `due-test --profile-signer` incorrectly sent two Feishu `买入成功` cards for CRCL. Chain readback proved both test hashes absent, both receipts absent, nonces unchanged, BUSDT balances unchanged, and CRCL positions zero. Test configs now hard-disable Feishu and redirect all mutable execution/decision/alert files to a temporary directory; self-test covers the boundary.
- [x] Capture CRCL buy/sell evidence after the `2026-07-11T11:30:00Z` open. Bot2 ranked `1` overall in T+19 and Bot5 ranked `4` overall / `3` in the first T+20 block; both bought the three named `5U` outcomes, winning tips mined, public fallback was absent, and nonces remained contiguous. Both fast exits failed before broadcast because sell inherited strict Builder `maxTimestamp` validation; the retained monitor pause delayed fallback sells to T+74/T+96. Both fallback sells succeeded and all six final outcome balances are zero.
- [x] Fix the CRCL fast-exit defects and evidence race. Fast sells now sign and broadcast with Builder/timed-executor/timestamp-guard disabled, failure immediately releases the normal sell monitor pause, dedicated open timers are protected from generic expiry cleanup, and the rank report resolves the actual per-fill Builder target second instead of defaulting Bot2 to T+20. Local self-test now covers all four boundaries.
- [x] 2026-07-11 obtain CASHCAT live acceptance for the repaired sell path. Bot2 and Bot5 each bought the locked three adjacent Meme ranges at `10U`; RPC-only fast sells succeeded at targets T+25.962 and T+45.601 with sell gas `0.15gwei`, no Builder timestamp error, zero final positions, and contiguous nonces `853/853` and `22/22`. Dashboard force-refresh reports Bot2 net `+11.39U` and Bot5 net `-8.1075U` after recorded Gas/tip costs.
- [x] 2026-07-11 pause Bot5 after CASHCAT ranked `8` overall / `5` in T+20. Four same-block tips (`0.0075898`, `0.005`, `0.005`, `0.0013253 BNB`) preceded Bot5's `0.001 BNB`. Stop and disable only `42space-event@42space-5.service`; keep Bot5 Dashboard active and Bot2 Event active. Reactivation requires an explicit operator decision.
- [x] 2026-07-11 raise only Bot2 Meme execution from three to five outcomes at `10U` each. Add `MEME_RANGE_SELECTION_OUTCOME_COUNT=5`, set production market/batch caps to `50U`, preserve first-observation locking and T+19/`1.1gwei`/`0.001 BNB` transport, and leave non-Meme first-three behavior unchanged. Local and production verification pass; HOODRAT is funded/prepared `5/5` for `50U`. JUGGERNAUT was safely skipped because restart completed after its open window.
- [x] 2026-07-11 explicitly re-enable Bot5 Event and raise only its Builder tip from `0.001` to `0.006 BNB`. Keep T+20, three outcomes x `10U`, buy gas `1.1gwei`, and T+40-50 exit unchanged. Service/dashboard are active and the Strategy UI reads tip `0.006`; live preflight is BNB-blocked at balance `0.00603565` versus required `0.009761`, a minimum shortfall of `0.00372535 BNB`.
- [x] 2026-07-11 complete HOODRAT live acceptance after funding. Bot2 bought five named ranges x `10U` in block `109363891`, txIndex `1`, rank `1` overall/T+19; BlockRazor tip `0.001 BNB` mined and public buy fallback was skipped. Bot5 bought three named ranges x `10U` in block `109363893`, txIndex `5`, rank `5` overall / `3` in T+20; BlockRazor tip `0.006 BNB` mined and public buy fallback was skipped. RPC-only fast sells succeeded at targets T+25.079 and T+40.702, all selected token balances are zero, nonces are contiguous at Bot2 `857/857` and Bot5 `31/31`, and auto-sell errors remain zero. Dashboard force-refresh reports Bot2 net `+19.07U` and Bot5 net `-9.368U` after recorded Gas/tip costs.
- [x] 2026-07-11 pause Bot5 again after the HOODRAT ranking review and widen only Bot2's private Builder retry window. Bot5 had already transferred `29.720062993975746076 BUSDT` and `0.036600071198020532 BNB` to Bot2 at nonces `31/32`, leaving balances `0/0` and nonce `33/33`; these transfers predated the pause. Stop and disable `42space-event@42space-5.service`, retain its Dashboard, and change only Bot2 `BUILDER_BUNDLE_PREPOSITION_LEAD_MS` from `500` to `700`. Bot2 remains T+19/`1.1gwei`/`0.001 BNB` with unchanged selection and T+25 sell policy; live readback shows buy/sell `watching`, pending/prepared `0/0`, no lock, sell errors `0`, and `NRestarts=0` after the startup guard cleared.
- [x] 2026-07-12 return Bot2 Meme execution from five outcomes to three and raise only its Builder tip. Set `MEME_RANGE_SELECTION_OUTCOME_COUNT=3`, market/batch caps `30U`, keep `10U` per outcome and buy gas `1.1gwei`, and set `BUILDER_BUNDLE_TIP_BNB=0.003`. Production doctor resolves T+18.300 private retries toward strict T+19, three outcomes/`30U`, required BNB `0.006761`, wallet `0.062190370444852729 BNB`, and `blockers=[]`; selection, timing, and RPC-only T+25 sell behavior otherwise remain unchanged. Chainstack is healthy, while the configured Ankr broadcast endpoint returns `401 API key disabled`, leaving paid-RPC readiness at `1/2` without blocking strict dual-Builder execution.
- [x] 2026-07-12 rotate only Bot2's disabled backup Ankr credential across its HTTP, WSS, and `BROADCAST_RPC_URLS` entries. The new endpoint passed direct HTTP/WSS probes, normalized env comparison proves only the credential changed, Event/Dashboard restarted cleanly, and production doctor restored Chainstack plus Ankr to `2/2` with `blockers=[]`; Builder, wallet, timing, selection, gas, tip, and sell settings were not changed.
- [x] 2026-07-12 complete the recreated GRVT split proof and align Bot5 execution with Bot2. Address-only plans excluded retired `0xFBB0...e302`; replacement `0xd283...bB4` bought outcomes 1-3 on Bot2 and 4-6 on Bot5, each `3 x 10U`. Both strict T+19 buys landed in block `109531855` at txIndex `1/3` with mined tips at `2/4`. Randomized T+25.337/T+24.532 RPC sells succeeded, all six balances are zero, final nonces are `863/863` and `39/39`, heartbeat returned to `pending=0/prepared=0`, and both plan rows are disabled.
- [x] 2026-07-12 move Bot2 and Bot5 default exits one second earlier. Back up both profile env and runtime configs, set `AUTO_SELL_FAST_OPEN_EXIT_MIN_DELAY_MS=23500`, `MAX_DELAY_MS=25000`, and `AUTO_SELL_OPEN_EXIT_DELAY_SECONDS=24`; remove Bot5's stale runtime fallback value `40`. Keep buy timing, `1.1gwei`, `0.003 BNB` tip, `0.15gwei` sell gas, 100% exit, and -10% stop loss unchanged. Effective-config readback matches on both profiles; all four services are active with `NRestarts=0` and no pending buys.
- [x] 2026-07-13 complete two ARROW split-plan executions. The July 14 market bought Bot2 outcomes 1-3 and Bot5 outcomes 4-6, each `3 x 10U`, in block `109703824` at T+19; all selected balances are zero after fallback stop-loss exits recovered a transient fast-sell RPC parameter error. The July 31 market bought only Bot2 outcomes 1-3 in block `109704489`, txIndex `1`; Bot5's outcomes 4-6 candidate did not mine because an unrelated nonce-43 wallet swap had converted `66 BUSDT` to USDC before open, leaving insufficient BUSDT. Bot5 latest/pending remained `45/45`, and all four one-off plans are disabled with terminal evidence.
- [x] 2026-07-13 move only Bot2's default exit another two seconds earlier. Back up Bot2 env/runtime, set `AUTO_SELL_FAST_OPEN_EXIT_MIN_DELAY_MS=21500`, `MAX_DELAY_MS=23000`, and `AUTO_SELL_OPEN_EXIT_DELAY_SECONDS=22`; keep Bot5 at T+23.5-25/fixed T+24 and leave every buy parameter unchanged. Bot2's July 31 sell target was T+21.979, first RPC acceptance was T+22.032, receipt `0x0c4c4e0f6f21995078dcd2428a64a9933e8aae2ae487b4c3459bdced845b54b9` succeeded, and Event/Dashboard readback shows the new window with `NRestarts=0`.
- [x] 2026-07-13 unify Bot2 and Bot5 global exits at randomized T+20.5-21.5 with fixed T+21 fallback, preserving all buy parameters, 100% sell sizing, `0.15gwei` sell gas, and -10% full stop loss. The Hakimi address-scoped split bought Bot2 outcomes 1-3 and Bot5 outcomes 4-6 at `3 x 10U`; both landed in T+19 block `109707822` with mined `0.003 BNB` BlockRazor tips and no public fallback. Both RPC-only sells landed in T+21 block `109707827`, all positions are closed, nonces are Bot2 `875/875` and Bot5 `50/50`, both plans are terminally disabled, and worker/sell health is green with zero errors.

- [x] 2026-07-14 retarget Bot4's recurring OpenRouter buy to exact outcomes `Hy3 (free)=20U` and `MiMo - V2.5=10U`, retaining total `30U`, T+19.300 private submission, exact T+20 execution, `0.5gwei`, and `0.001 BNB` tip. Pair REST price exits as `Hy3 (free) >= 0.0020` and `MiMo - V2.5 >= 0.0017`, each selling `100%`, while retaining the 1s-for-10min cadence, 60s normal cadence, Beijing 19:00 remainder exit, `0.15gwei` sell gas, and disabled stop loss. Exact labels were verified on July 15-19 markets; production plan/readiness/4245 readback, isolated profile-signer no-broadcast pre-sign, `120/120` nonce, local/remote verification, and Bot4-only service health all pass.

## Update Rule

Update this file after buy, sell, funding, caps, retry, receipt, or persistence changes.
