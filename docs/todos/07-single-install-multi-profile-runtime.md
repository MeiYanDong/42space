# Single Install Multi Profile Runtime Todo

## Phase 1: Documentation And Code Guard

- [x] Document the single-install multi-profile target model.
- [x] Document the isolation contract and rollback rule.
- [x] Add profile-local `DASHBOARD_ACTIONS_FILE` support.
- [x] Run local verification.
- [x] Confirm no secret values were committed.

## Phase 2: Server Profile Layout

- [x] Deploy canonical code to `/opt/42space`.
- [x] Create `/etc/42space/profiles`.
- [x] Create `/etc/42space/profiles/42space.env` from the current production env.
- [x] Create `/etc/42space/profiles/42space-2.env` from the current A/B env.
- [x] Convert profile data paths to absolute profile-local paths.
- [x] Document `MARKET_FOLLOW_FILE` as profile-local state for follow-controlled buy gating.
- [x] Document `ALERT_STATE_FILE` as profile-local state for Feishu alert dedupe.
- [x] Preserve production data under `/opt/42space/data/42space`.
- [x] Preserve A/B data under `/opt/42space/data/42space-2`.

## Phase 3: Templated Services

- [x] Install `42space-event@.service`.
- [x] Install `42space-dashboard@.service`.
- [x] Start `42space-dashboard@42space.service`.
- [x] Start `42space-event@42space.service`.
- [x] Start `42space-dashboard@42space-2.service`.
- [x] Start `42space-event@42space-2.service`.

## Phase 4: Legacy Retirement

- [x] Stop and disable `42space-dashboard.service`.
- [x] Stop and disable `42space-event-arm.service`.
- [x] Stop and disable `42space-2-dashboard.service`.
- [x] Stop and disable `42space-2-event-arm.service`.
- [x] Confirm no event worker is still running from `/opt/42space-2`.

## Phase 5: Verification

- [x] Run `npm run verify` on the server from `/opt/42space`.
- [x] Verify `42space-dashboard@42space.service` and `42space-event@42space.service` are active.
- [x] Verify `42space-dashboard@42space-2.service` and `42space-event@42space-2.service` are active.
- [x] Verify `http://47.251.26.212:4242/api/overview` returns `ok: true`.
- [x] Verify `http://47.251.26.212:4243/api/overview` returns `ok: true`.
- [x] Confirm dashboard processes and worker processes all use `/opt/42space` as cwd.

## Phase 6: Bot3 Exact-Score Profile

- [x] Create `/etc/42space/profiles/42space-3.env` from current Bot1 production env with Bot3 wallet, RPC, Feishu webhook, dashboard port `4244`, and profile-local paths.
- [x] Create `/opt/42space/data/42space-3` with fresh state/log files.
- [x] Copy current Bot1 `runtime-config.json` and `planned-buys.json` into the Bot3 data directory.
- [x] Start and enable `42space-dashboard@42space-3.service`.
- [x] Keep `42space-event@42space-3.service` inactive and disabled until the operator approves live buying.
- [x] Verify `http://127.0.0.1:4244/api/overview` returns `ok: true`.
- [x] Verify Bot3 read-only doctor checks: private key loads, wallet matches, HTTP RPC/WSS/broadcast RPCs work, and remaining blockers are BUSDT/BNB funding.
- [x] Open cloud security-group access for public `47.251.26.212:4244` and verify the Bot3 dashboard returns `ok: true`.
- [x] Correct Bot3 private key after operator update, set wallet fields to `0x406Fe5a3dfcE304474a698cB0C0Db835C4a9c4F4`, restart dashboard, and verify doctor still reports only allowance/funding blockers while the worker stays disabled.
- [x] 2026-06-22 after operator approval, fund Bot3, approve Router allowance, enable/start `42space-event@42space-3.service`, and confirm the worker prepares the six newly added exact-score planned buys.
- [x] 2026-06-26 apply Bot2's event display/notification filters to Bot3 without migrating Bot2's Meme/Binance strong default-follow buy filter; Bot3 ordinary strategy buys are disabled by a non-matching question allowlist and planned buys remain the explicit execution path.
- [x] 2026-06-26 add USA vs Bosnia and Herzegovina as a Bot3 profile-local planned buy for `USA 1-0 BIH`, `USA 2-0 BIH`, `USA 2-1 BIH`, `USA 3-0 BIH`, and `USA 3-1 BIH` at `10U` each with `openBroadcastDelayMs=18985`, stop-loss disabled, and full 10h pre-start auto-sell; restart only `42space-event@42space-3.service` and verify the record is funded and prepared.
- [x] 2026-06-27 update and verify Bot3 production defaults: `OPEN_BROADCAST_DELAY_MS=18985`, buy `GAS_PRICE_GWEI=1`, and sell `AUTO_SELL_GAS_PRICE_GWEI=0.15`. Backups were created for the profile env and runtime config, `42space-event@42space-3.service` plus `42space-dashboard@42space-3.service` were restarted, status/doctor showed the USA vs Bosnia plan funded and prepared with no blockers, and dashboard port `4244` returned `ok: true`.
- [x] 2026-06-27 make Bot3 explicitly planned-buy-only by setting `MARKET_BUY_QUESTION_ALLOWLIST_REGEX=a^` alongside the existing non-matching strategy question allowlist. Backed up `/etc/42space/profiles/42space-3.env`, restarted only Bot3 event/dashboard services, and verified status/doctor still show the USA vs Bosnia planned buy funded/prepared with no blockers.
- [x] 2026-06-27 retune Bot3 default planned-buy timing to `OPEN_BROADCAST_DELAY_MS=18900` after USA vs Bosnia evidence showed T+18.988 broadcast and T+19.049 first RPC acceptance landed one block behind a lower-gas buyer. Kept buy gas `1gwei`, sell gas `0.15gwei`, fanout broadcast `2` RPCs, and planned-buy-only allowlists; restarted Bot3 event/dashboard services and verified active services, public dashboard `ok:true`, and doctor broadcast RPC `2/2`.
- [x] 2026-06-27 deploy Bot3 buy-rank evidence collection. Added `scripts/bot3-buy-rank-evidence.js`, exposed `npm run bot3:buy-rank`, deployed script and `package.json` to `/opt/42space` after backups, ran remote `npm run check`, and replayed USA vs Bosnia into `/opt/42space/output/bot3-buy-rank/latest.{json,md}` showing Bot3 rank `2/6` and the lower-gas first buyer in the previous block.
- [x] 2026-06-27 enable automatic Bot3 buy-rank collection for the next new buy. Installed `42space-bot3-buy-rank-evidence.service` and `.timer`, enabled the timer, and verified the first run exited successfully with `reason=already-collected`; the next distinct Bot3 buy tx will update `/opt/42space/output/bot3-buy-rank/latest.{json,md}`.
- [x] 2026-06-28 add Bot3 profile-local planned buys for Australia vs Egypt and Argentina vs Cabo Verde after backup `/opt/42space/data/42space-3/planned-buys.json.bak-aus-egy-arg-cpv-bot3-2026-06-28T05-13-52-945Z`. Australia vs Egypt buys `AUS 0-1 EGY`, `AUS 0-2 EGY`, and `AUS 1-2 EGY` at `10U` each against market `0x2D47979DD16367726aF21A171DF37a691Ddeb41f`; Argentina vs Cabo Verde buys `ARG 1-0 CPV`, `ARG 2-0 CPV`, `ARG 2-1 CPV`, `ARG 3-0 CPV`, and `ARG 3-1 CPV` at `10U` each against market `0xCE263162852C42991432EC17A73B69Ab42DeA29a`. Both records pin `openBroadcastDelayMs=18900`, buy gas `1gwei`, `pre_start_exit`, stop-loss disabled, and 10h-before-kickoff clearing. Restarted only Bot3 event/dashboard services; status/doctor show the next Australia batch funded with `requiredBusdt=30`, BNB/BUSDT/allowance ready, broadcast RPC `2/2`, and no blockers.
- [x] 2026-06-28 remove Bot3's inherited global ladder/stop-loss default. Backed up Bot1/Bot3 env files, Bot3 runtime config, and `/opt/42space/src/event-sniper.js` with `bak-autosell-rpc-20260628T054259Z`; deployed strategy-gated auto-sell quote code; set Bot3 `AUTO_SELL_STRATEGY=pre_start_exit`, `AUTO_SELL_STOP_LOSS_ENABLED=0`, `AUTO_SELL_BEFORE_MARKET_START_SECONDS=36000`, and `AUTO_SELL_POLL_MS=30000`; set Bot1 `AUTO_SELL_POLL_MS=30000`; restarted only Bot1 event, Bot3 event, and Bot3 dashboard. Remote `npm run verify` passed, services are active, warning logs are empty, and readback estimated Bot1/Bot3 full-exit quote RPC/min at `0` before strategy due time.
- [x] 2026-06-28 adjust Bot3 Australia vs Egypt planned buy to `5U` per selected outcome for `AUS 0-1 EGY`, `AUS 0-2 EGY`, and `AUS 1-2 EGY`. Backed up `/opt/42space/data/42space-3/planned-buys.json` to `/opt/42space/data/42space-3/planned-buys.json.bak-australia-egypt-5u-20260628T055314Z`, restarted only Bot3 event/dashboard services, verified both services active, status shows Australia total stake `15U`, doctor shows `requiredBusdt=15`, broadcast RPC `2/2`, and no blockers.
- [x] 2026-06-28 add and execute Bot3 Mexico vs Ecuador planned buy at `5U` per selected outcome for `MEX 1-0 ECU`, `MEX 2-0 ECU`, and `MEX 2-1 ECU`. Initial backup `/opt/42space/data/42space-3/planned-buys.json.bak-mexico-ecuador-5u-20260628T091249Z` created a temporary regex record; after finding the exact market, backup `/opt/42space/data/42space-3/planned-buys.json.bak-mexico-ecuador-address-20260628T091608Z` set market `0x519F7d36E0ac447235A4d7E715739bF747D67D8d` and kickoff `2026-07-01T01:00:00.000Z`; backup `/opt/42space/data/42space-3/planned-buys.json.bak-mexico-ecuador-regex-fix-20260628T091825Z` removed the broad regex after status showed it also matched Goal Differential and Total Goals. Bot3 kept `openBroadcastDelayMs=18900`, buy gas `1gwei`, `pre_start_exit`, stop-loss disabled, and 10h-before-kickoff clearing. The worker pre-signed at `2026-06-28T09:18:41.263Z`, broadcast at `2026-06-28T09:19:40.903Z`, first accepted via Ankr at `09:19:40.988Z`, and receipt succeeded in block `106843974` with tx `0x2c0d9265f77c1f90fd91fd70bfc2a64243380ea0ffff8f42e330ec7f0d0d1d6c`.
- [x] 2026-06-28 retune Bot3 default planned-buy timing to `OPEN_BROADCAST_DELAY_MS=18850`. Backed up `/etc/42space/profiles/42space-3.env`, `/opt/42space/data/42space-3/runtime-config.json`, and `/opt/42space/data/42space-3/planned-buys.json` with `bak-bot3-delay-18850-20260628T152609Z`; updated the profile env and runtime config to `18850`; synchronized the current unexecuted target records to `openBroadcastDelayMs=18850`; left already bought records unchanged as historical evidence. Restarted only `42space-event@42space-3.service` and `42space-dashboard@42space-3.service`; verified both active, startup/status read `openBroadcastDelayMs=18850`, Australia vs Egypt and Argentina vs Cabo Verde target records read `18850`, doctor shows `blockers: []`, `requiredBusdt=15`, and broadcast RPC `2/2`.
- [x] 2026-06-29 raise Bot3 buy gas to `2.2gwei` while keeping default broadcast at `OPEN_BROADCAST_DELAY_MS=18850`. Backed up `/etc/42space/profiles/42space-3.env`, `/opt/42space/data/42space-3/runtime-config.json`, and `/opt/42space/data/42space-3/planned-buys.json` with `bak-bot3-buygas-2p2-20260628T181751Z`; updated Bot3 env/runtime `GAS_PRICE_GWEI=2.2`; updated the next Argentina vs Cabo Verde planned-buy record to `gasPriceGwei=2.2` and `openBroadcastDelayMs=18850`; left sell gas at `0.15gwei`. Restarted only Bot3 event/dashboard services and verified both active, status read `openBroadcastDelayMs=18850` plus `gasPriceGwei=2.2`, next batch Argentina vs Cabo Verde `50U`, doctor `blockers: []`, broadcast RPC `2/2`, and gas reserve `0.01012 BNB`.
- [x] 2026-06-29 add and execute Bot3 `2026 FIFA World Cup 3rd Place ?` one-off country basket. Added planned buy `bot3-worldcup-3rd-place-2026-06-29` for market `0x86308B8059183eA443fd1885D5493cF6C5222F1f`, outcomes France, Argentina, Spain, England, and Brazil at `10U` each, `openBroadcastDelayMs=18850`, and `gasPriceGwei=4.4`; set `autoSell.enabled=false` and `strategy=hold_to_settlement` because the non-exact-score market's `startDate` is the open time and Bot3's 10h `pre_start_exit` rule would otherwise already be due. Backups were `/opt/42space/data/42space-3/planned-buys.json.bak-worldcup-3rdplace-4p4-2026-06-29T072205489Z`, `/etc/42space/profiles/42space-3.env.bak-worldcup-3rdplace-gas-4p4-2026-06-29T072314865Z`, and `/opt/42space/data/42space-3/runtime-config.json.bak-worldcup-3rdplace-gas-4p4-2026-06-29T072314865Z`; Bot3 env/runtime now read `GAS_PRICE_GWEI=4.4`, sell gas stayed `0.15gwei`, and only Bot3 event/dashboard services were restarted. Status/doctor before open showed required `50U`, BNB/BUSDT/allowance ready, gas reserve `0.02024 BNB`, and broadcast RPC `2/2`. The worker pre-signed at `2026-06-29T07:29:19.561Z`, broadcast at `2026-06-29T07:30:18.852Z`, first accepted via Ankr at `07:30:18.914Z`, both RPCs accepted, receipt succeeded in block `107021344` with tx `0xd0408563a7ec51052d7eecfef16b41ce9a045e367758dc40c4695ec910f9475f`, txIndex `7`, gas limit `4,847,693`, gas used `1,906,474`, and effective gas price `4.4gwei`. `npm run bot3:buy-rank` wrote `/opt/42space/output/bot3-buy-rank/bot3-buy-rank-evidence-2026-06-29T07-35-12-267Z.{json,md}` and reported `conclusion=rank1`, `ourRank=1`, `topCount=9`; the next same-block buyers were `0x23d6004B93019bEf4E882f3aE60CAbEEd2efbB34` at txIndex `43` and `0x3a92A09faA9C1aD28b629c681c850b99607E937d` at txIndex `81`. Post-buy status shows wallet `14.328574147080778558 BUSDT` and `0.012941364923972602 BNB`; direct ERC1155 balance checks show all five selected outcome token balances present, and `auto-sell-positions.json` does not contain this market.
- [x] 2026-06-29 add and execute Bot3 `2026 FIFA World Cup Runner-Up?` one-off country basket. Added planned buy `bot3-worldcup-runner-up-2026-06-29` for market `0x6B7F30fb52B26814BB49312442010450e43e226D`, outcomes France, England, Argentina, and Spain, first at `20U` each and then updated per operator request to `30U` each after backup `/opt/42space/data/42space-3/planned-buys.json.bak-worldcup-runnerup-30u-2026-06-29T074102625Z`. The final record used `openBroadcastDelayMs=18850`, `gasPriceGwei=4.4`, and `autoSell.enabled=false` / `strategy=hold_to_settlement`; it required `120U` total and `0.0176 BNB` gas reserve. Funding briefly blocked on BNB, then recovered; pre-open status/doctor showed BUSDT `124.346183497273781037`, BNB `0.032613044413802237`, allowance ready, broadcast RPC `2/2`, and blockers empty. The worker pre-signed at `2026-06-29T07:44:19.604Z`, broadcast at `2026-06-29T07:45:18.852Z`, first accepted via Ankr at `07:45:18.929Z`, both RPCs accepted, receipt succeeded in block `107023344` with tx `0x12251caa136916077f829bcb876a0a1265a38cc21cf47923e733705e3e898f03`, txIndex `22`, gas limit `7,412,055`, gas used `1,542,221`, and effective gas price `4.4gwei`. `npm run bot3:buy-rank` wrote `/opt/42space/output/bot3-buy-rank/bot3-buy-rank-evidence-2026-06-29T07-46-29-761Z.{json,md}` and reported `conclusion=ranked_after_first`, `ourRank=3`, `topCount=9`; the two earlier same-block buyers were `0x3a92A09faA9C1aD28b629c681c850b99607E937d` at `5gwei` txIndex `1` and `0x23d6004B93019bEf4E882f3aE60CAbEEd2efbB34` at `1.3gwei` txIndex `2`. Post-buy status shows wallet `4.348883921636414101 BUSDT` and `0.025827272013802237 BNB`; direct ERC1155 balance checks show all four selected outcome token balances present at about `8986.95` each.
- [x] 2026-06-30 enroll Bot3 manual website-bought `France vs Sweden` positions into profile-local retained auto-sell, then update the operator-requested retention from `30%` to `10%`. Added Bot3-only planned-buy auto-sell override plus seeded auto-sell state entries for `FRA >=4-1 SWE`, `FRA >=4-0 SWE`, `FRA 3-1 SWE`, and `FRA 3-0 SWE`; each now retains `10%` of the current detected chips and sells the remainder under Bot3's 10h `pre_start_exit` rule. Current retained targets are `763.486`, `763.486`, `644.271`, and `644.271` chips. Backups were created for `/opt/42space/data/42space-3/planned-buys.json` and `/opt/42space/data/42space-3/auto-sell-positions.json`, including `planned-buys.json.bak-france-sweden-handicap-retain10-20260630T091049898Z` and `auto-sell-positions.json.bak-france-sweden-handicap-retain10-20260630T091049898Z`; restarted only `42space-event@42space-3.service` and verified active service plus readback.
- [x] 2026-06-30 add and execute Bot3 `France vs. Sweden - Handicap` planned buy. Added Bot3-only planned buy `bot3-france-sweden-handicap-france-minus-1p5-2026-06-30` for market `0x8f14974D980d971c606a9b2f5862285ADA77C55d`, outcome `France −1.5`, `10U`, per-record buy gas `1.1gwei`, `openBroadcastDelayMs=18850`, `pre_start_exit`, stop-loss disabled, and `kickoffAt=2026-06-30T21:00:00.000Z` so the 10h pre-start sell is due at `2026-06-30T11:00:00Z`. Bot3 pre-signed nonce `84`, broadcast tx `0x733a7e9e49362a2cbbb559b2205658e83f4d16e73b2692c72df45a1a6786f57c` at `2026-06-30T09:15:18.852Z`, first accepted via Ankr at `49ms`, both RPCs accepted, and receipt succeeded in block `107227206` with gas used `455056` and effective gas `1.1gwei`. Positions readback shows Bot3 holds tokenId `1` `France −1.5` with cost basis about `9.9402U`; fills and seen state contain the tx, and event/dashboard/orderflow services are active.
- [x] 2026-06-30 add and enable the Bot3 Runner-Up orderflow-trigger sell sidecar. Installed `scripts/orderflow-trigger-sell.js` and `42space-bot3-runner-up-orderflow-sell.service` on `/opt/42space`, watching Runner-Up market `0x6B7F30fb52B26814BB49312442010450e43e226D` for Bot3-held token IDs `32768,134217728,65536,2`; an external watched-outcome buy at the configured threshold triggers a matched-outcome `100%` sell using Bot3 sell gas `0.15gwei`. Remote `node --check`, self-test, short dry-run, operator approval, service active check, state/log readback, and Bot3 event/dashboard active checks all passed. Current Runner-Up threshold is `150U`.
- [x] 2026-06-30 move the Bot3 Runner-Up orderflow monitor read path to a dedicated uncommitted Chainstack RPC via `/etc/42space/profiles/42space-3-orderflow.env`. The service template now supports the optional env file, the script honors `ORDERFLOW_TRIGGER_RPC_URL`, and production restart/readback showed the env file loaded, service active, Bot3 event/dashboard active, no recent service warnings, and advancing state.
- [x] 2026-06-30 deploy retry-safe state handling for the Bot3 Runner-Up orderflow monitor. Failed watched transactions now remain retryable instead of being marked handled before execution completes; production readback shows the new state fields loaded and empty, with `lastProcessedBlock` advancing.
- [x] 2026-07-07 install multi-profile World Cup orderflow-trigger sell services. The existing Bot3 Runner-Up service now watches current Bot3 Runner-Up holdings, Bot3 3rd Place was added for market `0x86308B8059183eA443fd1885D5493cF6C5222F1f`, and Bot1 received Runner-Up plus 3rd Place services with profile-local data files under `/opt/42space/data/42space`. Remote `node --check`, `orderflow-trigger-sell --self-test`, and `npm run check` passed; systemd readback shows all four orderflow services enabled/running.
- [x] 2026-07-07 configure Bot1 orderflow services to use a dedicated uncommitted read RPC env file. Created `/etc/42space/profiles/42space-orderflow.env` with mode `600`, restarted `42space-bot1-runner-up-orderflow-sell.service` and `42space-bot1-third-place-orderflow-sell.service`, verified both services active and EnvironmentFile-backed, and confirmed the dedicated HTTP RPC responds to `eth_blockNumber` without printing the secret URL.
- [x] 2026-07-07 enable current-position watch on the World Cup orderflow services so website/manual buys inside the covered markets become watched without Bot fill records. Bot1/Bot3 Runner-Up and 3rd Place services now set `ORDERFLOW_TRIGGER_WATCH_CURRENT_POSITIONS=1`; remote state readback confirmed Bot3 Runner-Up Spain was dynamically re-added after the manual website buy.
- [x] 2026-07-09 set both Bot1 and Bot3 World Cup Runner-Up and 3rd Place orderflow-trigger sell thresholds to `150U`, keeping matched-outcome `100%` sell behavior and current-position watch. Remote `/api/orderflow-monitors` readback showed all four monitor services active/running with empty failed/processing queues.
- [x] 2026-07-09 deploy `42space-address-tx-watch-0x96fde.service` as an independent address transaction watcher for `0x96FDe227f3863812464dC1320B505016837a3650`. It uses Bot1 Feishu config plus the dedicated orderflow read RPC env, watches direct BSC transactions and token `Transfer` logs, writes profile-local state/log files, and has `ADDRESS_TX_WATCH_COOLDOWN_MS=600000`. Remote `node --check`, `address-tx-watch --self-test`, and `npm run check` passed; systemd readback shows enabled/running with `NRestarts=0`, and the state cursor advanced from block `108935974` to `108935987`.
- [x] 2026-07-09 add `42space-address-tx-watch-0x1bc7df.service` and `42space-address-tx-watch-0x51349f.service` for `0x1Bc7dF2AA0DBE1a489A7205f2D1fF92C3d51A80b` and `0x51349f0B9b8C21A34781273e37F16B0233239C41`. They reuse the Bot1 Feishu config and dedicated orderflow read RPC env, keep 10-minute alert cooldowns, and write separate state/log files under `/opt/42space/data/42space/`.
- [x] 2026-07-10 upgrade all three address watcher services to decode 42 buy/sell context before Feishu delivery. The shared sidecar now reads receipts, resolves market/outcome through 42 REST, uses a transaction-hash activity fallback for Binance Router sells, preserves the 10-minute cooldown, and lets a meaningful 42 trade upgrade a just-sent generic alert once.
- [x] 2026-07-04 add Bot3-only FIFA/Sports exact-score auto-buy code path. `BOT3_FIFA_EXACT_SCORE_AUTO_BUY_ENABLED` gates the feature, `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT=1` sets the first live practice size, planned buys remain higher priority, and `npm run event:self-test` covers lowest-price-tier selection, side-market skip, profile gating, and planned-buy override.
- [x] 2026-07-04 keep Bot3 exact-score auto-buy from being blocked by the old unknown-batch funding upper bound. When Bot3's auto selector is enabled and `WATCH_FUNDING_MODE=next_batch` has not yet identified a concrete next batch, the funding fallback now uses `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT * 3`; once a planned buy or known next batch exists, the actual selected stake still takes priority.
- [x] 2026-07-04 deploy and verify the Bot3 exact-score auto-buy strategy on production. Remote `npm run verify` passed, `42space-event@42space-3.service` was restarted, event/dashboard/orderflow services are active, Bot3 profile readback has `BOT3_FIFA_EXACT_SCORE_AUTO_BUY_ENABLED=1` and `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT=1`, `event:funding` reports `reason=bot3_fifa_exact_score_auto_single_market_fallback`, `requiredBusdt=3`, BUSDT/allowance/BNB ready, and `npm run bot3:fifa-preview` reports `enabled=true`.
- [x] 2026-07-05 raise Bot3 FIFA/Sports exact-score auto-buy stake from `1U` to `5U` per selected outcome. Only Bot3's profile env value `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT` changed; planned-buy priority, T+18.850 broadcast timing, `4.4gwei` buy gas, `0.15gwei` sell gas, `pre_start_exit`, and stop-loss disabled remain unchanged. Production backup: `/etc/42space/profiles/42space-3.env.bak-bot3-auto-stake-5-20260705T045930Z`; readback shows the switch enabled, stake `5`, funding fallback `requiredBusdt=15`, BUSDT/allowance/BNB ready, and Bot3 event/dashboard/orderflow services active.
- [x] 2026-07-06 align Bot3 automatic exact-score auto-sell timing with planned-buy `pre_start_exit`. The successful `France vs Morocco` auto-buy ranked 1 but then sold immediately because the unplanned auto-buy receipt had no `plannedBuy.kickoffAt`, so the old fallback used market-open `startDate=2026-07-06T03:06:05Z` and computed stale `dueAt=2026-07-05T17:06:05Z`; the market's match anchor was `endDate=2026-07-09T20:45:00Z`. Patched `src/event-sniper.js` so planned `kickoffAt` remains highest priority, otherwise Sports/FIFA exact-score positions use `endDate` for 10h pre-start clearing while Total Goals / Goal Differential / Handicap side markets stay on the old fallback. Local and remote `npm run verify` passed, backup `/opt/42space/src/event-sniper.js.bak-bot3-auto-exact-autosell-enddate-20260706T035119Z` was created, and only Bot3 event/dashboard services were restarted.
- [x] 2026-07-06 raise Bot3 FIFA/Sports exact-score auto-buy stake from `5U` to `10U` per selected outcome. Only `/etc/42space/profiles/42space-3.env` changed; planned-buy rows, default T+18.850 timing, buy `4.4gwei`, sell `0.15gwei`, `pre_start_exit`, and stop-loss disabled are unchanged. Production backup: `/etc/42space/profiles/42space-3.env.bak-bot3-auto-stake-10-20260706T045532Z`; Bot3 event/dashboard services were restarted and active. Readback shows `BOT3_FIFA_EXACT_SCORE_AUTO_STAKE_USDT=10`, `MAX_MARKET_STAKE_USDT=45`, `MAX_BATCH_STAKE_USDT=45`, and the next known automatic exact-score batch `Norway vs England` now requires `30U`; BUSDT is sufficient but BNB gas reserve remains below the current `4.4gwei` fast-buy requirement.
- [x] 2026-07-06 lower Bot3 global/default buy gas to `1.1gwei`, which also lowers the automatic exact-score buy path because it inherits `GAS_PRICE_GWEI` unless a planned-buy record has its own `gasPriceGwei`. Backups were created for `/etc/42space/profiles/42space-3.env` and `/opt/42space/data/42space-3/runtime-config.json` with suffix `bak-buygas-1p1-20260706T050807Z`; Bot3 event/dashboard services were restarted and active. Program status readback shows `gasPriceGwei=1.1`, `openBroadcastDelayMs=18850`, next batch `requiredBusdt=30`, gas reserve `0.00374 BNB`, and wallet BNB/BUSDT ready.
- [x] 2026-07-07 expand Bot3 automatic FIFA/Sports exact-score selection from three to five outcomes per lower-price win side. Side choice still uses the original three-score price-tier rows, then home-win selection buys `1-0`, `2-0`, `3-0`, `2-1`, `3-1` and away-win selection buys `0-1`, `0-2`, `0-3`, `1-2`, `1-3`. The code exports the auto outcome count and uses it for selector sizing, unknown-batch funding fallback, Router approval threshold, and wallet budget cap, so the current `10U` automatic basket requires `50U`.
- [x] 2026-07-09 add and enable Bot3 `Spain vs Belgium` exact-score orderflow-trigger sell coverage at `50U`. Added `42space-bot3-spain-belgium-orderflow-sell.service` for market `0x94FA631F5A8d830919db6d5B1571e438f0222Fb0`, configured initial watched token IDs `2048,1024,32`, enabled current-position watch for website/manual additions, and kept `matched_outcomes` / `100%` sell semantics. Remote orderflow self-test passed; production readback showed the service active with `NRestarts=0`, watched token IDs `2048,1024,32`, dynamic watched IDs `32,1024,2048`, no failed/processing txs, warning logs empty, and Bot3 event/dashboard plus existing Bot3 orderflow/value monitors active.
- [x] 2026-07-09 add and enable Bot3 single-outcome value-trigger sell for `Spain vs Belgium` / `ESP 2-1 BEL`. Added `scripts/position-value-trigger-sell.js` plus `42space-bot3-spain-belgium-esp-2-1-value-sell.service`; the service watches market `0x94FA631F5A8d830919db6d5B1571e438f0222Fb0`, tokenId `2048`, quotes every `5000ms`, and sells `100%` only when `expectedCollateralToUser >= 99U` with `minCollateralOut=99U`. Local and remote `npm run check`, script self-test, one-shot dry quote, daemon reload, and systemd enable/start passed; production readback showed active service with `NRestarts=0`, Bot3 event/dashboard still active, and current quote `19.481325U` below the `99U` trigger.
- [x] 2026-07-09 add Bot3 `Argentina vs Switzerland - Moneyline` planned buy. Added profile-local plan `bot3-argentina-switzerland-moneyline-2026-07-09` for market `0xb8A2C817d6457bf63D2EC755cA3dFa5dB7E71893`, outcomes `Argentina Win`, `Switzerland Win`, and `Draw` at `20U` each, per-record `openBroadcastDelayMs=19850`, buy gas `3.1gwei`, `kickoffAt=2026-07-12T01:00:00.000Z`, `autoSell.strategy=pre_start_exit`, 10h pre-start sell, and stop-loss disabled. Backup `/opt/42space/data/42space-3/planned-buys.json.bak-argentina-switzerland-moneyline-2026-07-09T08-28-55-625Z` was created; only Bot3 event/dashboard services were restarted. Production status/doctor showed the plan as the next known batch with `requiredBusdt=60`, wallet `81.044102127930121529 BUSDT`, `0.017856958979032559 BNB`, allowance ready, BNB gas reserve `0.01054`, broadcast RPC `2/2`, `prepared=true`, and blockers empty.
- [x] 2026-07-09 add code support for a Bot3-usable optional 48Club builder bundle path without changing Bot1/Bot2/Bot4/Bot5 defaults. The default remains `BUILDER_BUNDLE_ENABLED=0`; when explicitly enabled with a positive tip, fast buys submit `[buyRawTx, builderTipRawTx]` to the builder while public RPCs still receive only the buy raw transaction. The code supports per-planned-buy `builderBundle` overrides, funding reserve includes tip value plus transfer gas, buy results preserve builder/tip evidence, and receipt tracking writes a tip receipt/Gas ledger entry when the private tip tx lands. Local `npm run verify` passed; no production profile env has been changed by this code-only step.
- [x] 2026-07-09 execute a Bot3 one-off 48Club builder live test without changing Bot3 global defaults. A temp planned-buy file selected only Runner-Up `Spain`, bought `1U`, and configured `builderBundle.tipBnb=0.001`; Bot3 event/orderflow/value sell services were paused during the nonce-sensitive command and restored afterward. Buy tx `0xdd499389bef5d57dd80ed23b1f49c4a5f714cc01eb24ceafbb25982cf0ae5746` used nonce `152`, landed successfully in block `108965568`, txIndex `11`, gasUsed `389304`, and effective gas `1.1gwei`. Ankr accepted public fanout in `63ms`, Chainstack in `345ms`; 48Club accepted bundle hash `0xbc96046b33eeff1a97d5f9d724304a0d3d9f17628df3199020c7c5a1e9647c5a` in `359ms`, but tip tx `0xa13884e9bcfba3dc1dc819200b5670b6483cf550ffa065317feeb1ad3e048614` was not found after the configured max block. This means the public path won and the builder tip was not paid.
- [x] 2026-07-09 deploy the builder result persistence fix after the live test exposed missing builder fields in the executed result. `src/fortytwo.js` now propagates builder fields from the broadcast result for single and bundle buys; local and remote `npm run verify` passed. The test's Gas row was written under the temporary runtime directory first and then migrated into `/opt/42space/data/42space-3/gas-ledger.jsonl`; fills were backfilled with a `builder-bundle-tip-receipt` `not_found` audit row.
- [x] 2026-07-09 add safe `builder_only` mode and validate it on Bot2 ASML without changing global builder defaults. The ASML planned-buy row used `builderBundle.mode="builder_only"`, `tipBnb=0.002`, `maxBlocks=6`, and `timeoutMs=800`; the buy result recorded `broadcastMode=presigned_builder_bundle_only`, buy tx `0x6bea5412dd963b8eeb983dbd96a14987569b0eb96391ff71755052a9ea031d6c`, and tip tx `0xe0f7512296d56d3ff562b8f1fb1c46b15d402d38cbdabaf2718ebcdc71526d2c`, both successful in block `108968007` at txIndex `1` and `2`. Positive global builder tips should still remain off unless deliberately intended for every eligible profile buy.
- [x] 2026-07-09 deploy `builder_then_fanout` for explicit builder plans without adding a separate RPC set. The shared code now warms the builder endpoint during hot pending windows, races the existing broadcast RPCs for the bundle max-block lookup, and supports `builderBundle.fanoutDelayMs` / `BUILDER_BUNDLE_FANOUT_DELAY_MS` before public buy fanout. Bot2 env now sets `BUILDER_BUNDLE_MODE=builder_then_fanout` and `BUILDER_BUNDLE_FANOUT_DELAY_MS=120` while leaving `BUILDER_BUNDLE_ENABLED` and global tip unset; the ASML builder row was updated to the new mode. Only Bot2 event/dashboard services were restarted, both are active with `NRestarts=0`, and local plus remote `npm run verify` passed.
- [x] 2026-07-09 complete the Bot2 MU planned-buy override for the builder-hybrid test. The `Micron Technology (MU) price range, end of July 15th?` row now has five selected outcomes, `10U` each, explicit `openBroadcastDelayMs=18840`, buy gas `3.1gwei`, `autoSell.hold_to_settlement`, and per-record builder tip `0.002 BNB` in `builder_then_fanout` mode with `120ms` public-fanout delay. Production backup is `/opt/42space/data/42space-2/planned-buys.json.bak-mu-builder-10u-20260709T112627Z`; only Bot2 event/dashboard were restarted and both remained active.
- [x] 2026-07-10 complete the Bot2 World Cup Winner volume strict-Builder buy and operator-requested emergency exit without touching other profiles. The buy selected six `20U` ranges at T+18.850, `3.1gwei`, and `0.004 BNB` tip, but landed in an open+18s timestamp block despite open+19s bundle bounds. Bot2 then sold tokenIds `2,4,8,16,32,64` in one successful transaction, disabled the plan, verified all balances zero, and restored event/dashboard active with `NRestarts=0`.

## Phase 7: Central Discovery Feed

- [x] Add a read-only JSONL discovery feed writer to `event:premium-watch`.
- [x] Add `EVENT_DISCOVERY=feed` consumer mode to `event:watch` without changing profile-local execution state.
- [x] Deploy the shared feed mode to production and verify Bot1, Bot2, and Bot3 are configured for it while keeping separate Chainstack execution RPCs.

## Phase 8: Bot4 Daily Template Profile

- [x] Document Bot4 as isolated `42space-4` profile with dashboard port `4245` and profile-local state.
- [x] Add code support for daily-template-only display plus OpenRouter Python and BNB/USDT Futures Daily Volume buy gating.
- [x] Create `/etc/42space/profiles/42space-4.env` with Bot4 wallet, Chainstack RPC/WSS, Feishu webhook, and absolute profile-local paths without committing or printing secrets. Created on 2026-06-26; profile values were verified with a redacted config readback.
- [x] Create `/opt/42space/data/42space-4` with fresh runtime, follow, decision, fill, alert, auto-sell, and planned-buy state files.
- [x] Set `DASHBOARD_ACTIVITY_SINCE=2026-06-26T00:00:00Z` for Bot4 so dashboard activity, PnL, and project history ignore older wallet trades that predate this profile.
- [x] Start and verify `42space-dashboard@42space-4.service`.
- [x] Verify `http://127.0.0.1:4245/api/overview` returns `ok: true`.
- [x] Install and verify `42space-bot4-public-dashboard.service` as a port-80 public dashboard fallback for Bot4 while direct `4245` remains blocked by the SWAS firewall. Public `http://47.251.26.212/` returned `200`, and `/api/overview` returned Bot4 with 63 daily-template display rows on 2026-06-26.
- [x] Open the Aliyun SWAS firewall rule for public `47.251.26.212:4245` and verify the public API returns `ok: true`. On 2026-06-26 the China OAuth profile was reauthorized, `swas-open create-firewall-rule` succeeded with `--port 4245`, and `http://47.251.26.212:4245/api/overview` returned Bot4 `ok: true` with `historyCount=0` and the funded OpenRouter plus BNB/USDT next batch.
- [x] Run Bot4 read-only doctor checks: private key loads, wallet matches, HTTP RPC/broadcast RPCs work, and funding/allowance blockers are explicit. After adding the third OpenRouter Python outcome and pausing Hermes, the active target batch is `50U`; funding readiness depends on the current BUSDT/BNB balances and which future template markets are visible.
- [x] Enable/start `42space-event@42space-4.service` only after the operator-approved live profile is verified and funding/allowance are ready or the profile is intentionally waiting for funding. Worker is active with `ARM_WAIT_FOR_FUNDING=1`, `ARM_FUNDING_NOTIFY_WINDOW_MS=64800000`, and was funded for the 2026-06-27 OpenRouter Python plus BNB/USDT buys.
- [x] Install and verify a first-buy evidence collector. `42space-bot4-first-buy-evidence.timer` is enabled and writes `conclusion=timeout` if full proof is still missing after its retry window. The current OpenRouter schema requires a pre-signed `presigned_builder_bundle_only` path, T+19.500 private submission, T+20 atomic target, timed executor address, both 48Club and BlockRazor attempts, `positionFirst`/`noMerge`, `0.5gwei`, and no public broadcast. BNB/USDT remains independently checked inside T+22.000 to T+23.000 with `0.15gwei`; Hermes is paused. Completed targets also require selected outcomes, successful receipt, auto-sell monitor startup, stop-loss-disabled config, and no unintended non-target buy.
- [x] Add and deploy `npm run bot4:readiness`, a no-secrets JSON readiness gate that checks the Bot4 profile, status, local and public dashboards, systemd services, next batch, buy-only OpenRouter Python plus BNB/USDT gating, staggered target timing/gas, auto-sell monitor startup, and the current stop-loss-disabled hold setting. `42space-bot4-readiness.timer` writes `/opt/42space/output/bot4-readiness/latest.json` before daily openings.
- [x] 2026-06-27 disable `42space-bot4-public-dashboard.service` after confirming direct public `47.251.26.212:4245` still returns Bot4 `ok: true`; public port 80 is now used by Bot5.
- [x] 2026-06-27 set Bot4 buy `GAS_PRICE_GWEI=0.15` and sell `AUTO_SELL_GAS_PRICE_GWEI=0.15` in both profile env and runtime config, restart `42space-event@42space-4.service` plus `42space-dashboard@42space-4.service`, and verify dashboard readback shows both gas values as `0.15`.
- [x] Record first live Bot4 combined daily-template buy proof after the 2026-06-27 00:00 UTC opening: BNB/USDT bought the two configured bands at `0.15gwei`; OpenRouter broadcast was attempted as the same-batch second nonce but failed with Chainstack's in-flight delegated-account limit and was skipped at T+21.001. Bot4 now uses `EVENT_OPEN_WINDOW_SECONDS=25` to allow a short retry window while keeping T+19.985 as the scheduled broadcast time.
- [x] 2026-06-27 recover the US West SWAS host after SSH, dashboard HTTP, and Cloud Assistant stopped responding under high load. Rebooted instance `198cbb8ff2754651a49453aafe53069c`, traced the prior boot to a global OOM kill under `42space-premium-watch.service`, verified SSH/API/service recovery, and set persistent 2GB `/swapfile` plus `vm.swappiness=20` so dashboard and status subprocess bursts can use swap instead of stalling the host.
- [x] 2026-06-27 enable Bot4 `BUNDLE_DUE_MARKETS=1` with `EVENT_PRICE_GATE_ENABLED=0`, restart Bot4 event/dashboard services, and verify the running event process plus startup log show `bundleDueMarkets=true`, T+19.985 timing, and `0.15gwei` buy gas. Same-action funded daily templates now use one bundled transaction; if only one market is affordable, the single-market fallback still executes the affordable buy.
- [x] 2026-06-27 switch Bot4 from same-action bundle execution to staggered single-market execution: `BUNDLE_DUE_MARKETS=0`, profile `OPEN_BROADCAST_DELAY_MS=19900`, profile buy gas `3gwei`, sell gas `0.15gwei`, BNB planned-buy `openBroadcastDelayMs=30000`, BNB planned-buy `gasPriceGwei=0.15`, and stale-buy window `EVENT_OPEN_WINDOW_SECONDS=35` so the T+30 BNB action remains valid. Per-record gas is now carried through planned config, execution config, and funding reserve.
- [x] 2026-06-27 add Bot4 Hermes OpenRouter token usage planned buy and readiness coverage: outcome `> 900B`, `10U`, `openBroadcastDelayMs=21000`, `gasPriceGwei=0.15`, combined max batch `25U`, and evidence/readiness target checks for T+21.000.
- [x] 2026-06-27 raise Bot4 OpenRouter Python planned buy to `10U`: `DeepSeek V4 Flash`, T+19.900, `3gwei`, combined max batch `30U`, and readiness target checks updated for the new stake.
- [x] 2026-06-27 lower Bot4 Hermes OpenRouter token usage planned-buy gas to `0.15gwei` while keeping T+21.000 and `10U`; readiness/evidence target checks now expect the low-gas override for Hermes.
- [x] 2026-06-27 raise Bot4 BNB/USDT Futures Daily Volume planned buy to `10U` per selected outcome: `$150M – $300M` and `$300M – $450M`, T+30.000, `0.15gwei`, combined max batch `40U`, per-market cap `20U`, and readiness target checks updated for the new stake.
- [x] 2026-06-27 pause Bot4 stop-loss clear-out after the June 27 BNB/USDT stop-loss sell evidence: production env/runtime now set stop-loss disabled, while the event worker, dashboard, readiness, and first-buy evidence checks expect the paused stop-loss state.
- [x] 2026-06-27 convert Bot4 readiness/evidence timers from one-shot 2026-06-27 timers to daily 07:30/08:04 CST timers with dynamic UTC opening detection; pre-open readiness no longer requires a same-day evidence file that does not exist yet.
- [x] 2026-06-27 deploy dashboard load-control switches to `/opt/42space/src/dashboard-server.js` and `/opt/42space/public/assets/dashboard.js`, set `DASHBOARD_STARTUP_REFRESH=0`, `DASHBOARD_OVERVIEW_REFRESH_MS=0`, `DASHBOARD_OVERVIEW_CACHE_MS=300000`, and `DASHBOARD_OVERVIEW_STALE_MS=900000` in all production profile env files, and restart only dashboard services. This stops unattended multi-profile overview refreshes and slows visible-tab polling so dashboards no longer spawn `status` and `positions` child-process bursts; Bot4 event worker stayed active with `BUNDLE_DUE_MARKETS=1`, T+19.985 timing, and `0.15gwei` gas.
- [x] 2026-06-28 adjust Bot4 daily-template buys: OpenRouter Python buys `DeepSeek V4 Flash` and `Owl Alpha` at `10U` each, T+19.900 and `3gwei`; Hermes covers the current concrete `>900B` ranges `900B - 950B`, `950B - 1T`, `1T - 1.05T`, and `≥ 1.05T` at `2.5U` each, total `10U`, T+19.900 and `3gwei`, with a per-record secondary Chainstack HTTP broadcast RPC; BNB/USDT buys `$150M – $300M` and `$300M – $450M` at `10U` each, T+25.000 and `0.15gwei`; `MAX_BATCH_STAKE_USDT` is now `50`, and readiness/evidence target checks expect the new timings.
- [x] 2026-06-28 pause Bot4 Hermes OpenRouter token usage buying after the template/outcome change; the Hermes planned-buy record is disabled and Hermes is removed from the buy allowlist while still visible through daily-template display. Active Bot4 planned buys are OpenRouter Python `DeepSeek V4 Flash` plus `Owl Alpha` at `10U` each and BNB/USDT `$150M – $300M` plus `$300M – $450M` at `10U` each, so active `MAX_BATCH_STAKE_USDT` is now `40` and readiness/evidence target checks no longer require Hermes.
- [x] 2026-06-29 add `Hy3 preview` to Bot4 OpenRouter Python planned buys. Bot4 now buys `DeepSeek V4 Flash`, `Owl Alpha`, and `Hy3 preview` at `10U` each, total `30U`, while BNB/USDT remains `20U`; active `MAX_MARKET_STAKE_USDT` is now `30`, active `MAX_BATCH_STAKE_USDT` is now `50`, and readiness/evidence target checks expect all three OpenRouter Python outcomes.
- [x] 2026-06-30 move Bot4 BNB/USDT Futures Daily Volume to T+20.000 after pausing Hermes buying. The BNB record remains `10U` per selected outcome and `0.15gwei`, uses the secondary Bot4 Chainstack RPC, and stays separate from the then-current OpenRouter Python T+19.900/`3gwei` planned buy because `BUNDLE_DUE_MARKETS=0`.
- [x] 2026-06-30 move Bot4 BNB/USDT Futures Daily Volume to T+22.000 to keep more nonce spacing from OpenRouter Python. The BNB record remains `10U` per selected outcome and `0.15gwei`, continues to use the secondary Bot4 Chainstack RPC, and stays separate because `BUNDLE_DUE_MARKETS=0`.
- [x] 2026-06-30 fix Bot4 readiness journal handling so recent auto-sell startup events are not lost to the default spawn buffer, and `--skip-systemd` no longer fails the auto-sell monitor check it explicitly skipped.
- [x] 2026-07-02 set Bot4 daily-template auto-sell to `open_timed_exit` T+39600s / 100%, which maps the 08:00 Beijing daily opening to a 19:00 Beijing full exit. Kept sell gas `0.15gwei`, kept stop-loss disabled, set `AUTO_SELL_APPLY_AFTER_ISO` at cutover so pre-existing Bot4 positions are not immediately sold as overdue, and widened the runtime-config validator so delayed open exits up to one day are valid. Future explicit hold requests should be implemented as per-planned-buy `hold_to_settlement` overrides.
- [x] 2026-07-02 lower Bot4 OpenRouter Python buy gas from `3gwei` to `0.5gwei`. Updated the profile default gas, runtime gas, OpenRouter planned-buy `gasPriceGwei`, and Bot4 readiness/evidence expectations; BNB/USDT remains a separate T+22.000 planned buy at `0.15gwei` on the secondary Bot4 RPC, and sell gas remains `0.15gwei`.
- [x] 2026-07-09 update Bot4 OpenRouter Python planned buy to the exact market label `Non-listed Model` (未列出模型) and `DeepSeek V4 Flash` at `10U` each, T+19.900, `0.5gwei`, and per-record `builder_then_fanout` with a `0.001 BNB` builder tip. The OpenRouter record now adds a scoped 70% take-profit that sells that outcome 100% before the 19:00 fallback exit, Bot4 uses `AUTO_SELL_POLL_MS=60000`, BNB/USDT remains two `10U` outcomes at T+22.000 and `0.15gwei`, active caps are `MAX_MARKET_STAKE_USDT=20` and `MAX_BATCH_STAKE_USDT=40`, and readiness/evidence checks expect the builder and auto-sell fields.
- [x] 2026-07-10 audit and repair Bot4 OpenRouter ranking after tx `0x5080fae36880081ffe91f90d15266defcc4d1a8150c95084dd5befb71bf07e5b` ranked third in the first 60s. The first target trade landed in block `109071963`, the all-outcome trade in `109071964`, and Bot4 in `109071965` at txIndex `1`; Bot4 started at T+19.964 and 48Club accepted at T+20.459, so two whole blocks were lost before in-block gas ordering mattered. The optimized plan keeps public fallback T+19.900 and buy gas `0.5gwei`, pre-submits the private `0.001 BNB` bundle at T+19.000, sets `minTimestamp=T+20.000` plus `positionFirst=true`, and fans fallback to both Bot4 Chainstack endpoints plus 48Club privacy RPC. Failed early submission releases the unused tip nonce and invalidates later pre-signed records so BNB can be re-signed without a nonce gap. Readiness/evidence now require the early-submit/min-timestamp fields and three fallback RPCs.
- [x] 2026-07-11 replace Bot4 OpenRouter's timestamp-hint/fallback route with the proven atomic strict route. Deploy Bot4-owned timed executor `0x6034a8672020114673e320203b1bA6692F53Ec50`, approve BUSDT, add authenticated BlockRazor without exposing its token, and set only `bot4-openrouter-python-daily` to `builder_only`, `first_20s_block`, T+19.500 private submission, `positionFirst`/`noMerge`, `0.001 BNB` tip, `0.5gwei`, and no public fallback. The July 11 market and both named outcomes are present; wallet funding, allowance, nonce, executor state, both Builder probes, readiness, worker/dashboard services, and local/remote verification pass. BNB/USDT is unchanged at RPC T+22/`0.15gwei`.

## Phase 9: Bot5 Bot2-Like Profile

- [x] Document Bot5 as isolated `42space-5` profile with dashboard port `4246`, profile role `bot2_like`, and profile-local state.
- [x] Add no-secret Bot5 profile and runtime-config templates under `ops/profiles/`.
- [x] Add Bot5/Bot2-like code support for profile-role validation, dashboard rule summaries, and event-intelligence notification routing.
- [x] Create `/etc/42space/profiles/42space-5.env` after the operator provides Bot5 Chainstack RPC/WSS, wallet/private key, and Feishu webhook. Created on 2026-06-27 with dedicated Chainstack HTTP/WSS/broadcast RPCs, dedicated Feishu webhook, and profile-local paths; then aligned to Bot2 production selection and sell policy with `DRY_RUN=0`, `EXECUTE=1`, live acknowledgements, middle-three `10U`, one market per open, `OPEN_BROADCAST_DELAY_MS=19900`, `REBROADCAST_DURATION_MS=5000`, and buy gas later raised to `6gwei` while sell gas stayed `0.15gwei`.
- [x] Create `/opt/42space/data/42space-5` with fresh runtime, follow, decision, fill, alert, auto-sell, and planned-buy state files.
- [x] Start and verify `42space-dashboard@42space-5.service` on `127.0.0.1:4246`. The dashboard service is enabled/active and `/api/overview` returns `ok: true`.
- [x] Configure and verify Bot5 public access. `42space-bot5-public-dashboard.service` serves Bot5 on public `http://47.251.26.212/` and returns `ok: true`; direct public `47.251.26.212:4246` is now open through SWAS firewall rule `42space-5-dashboard` and `/api/overview` returns Bot5 `ok: true`.
- [x] Run Bot5 read-only doctor checks: private key loads, wallet matches, HTTP RPC/WSS/broadcast RPCs work, and BUSDT/BNB/allowance blockers are explicit. Doctor verified HTTP RPC and one Chainstack broadcast RPC, a separate WSS `eth_blockNumber` check passed, allowance is ready, and blockers are BUSDT balance below the `30U` next-batch requirement plus zero BNB for fast gas reserve.
- [x] Enable/start `42space-event@42space-5.service` in production waiting-for-funds mode after live-mode acknowledgements. The worker is enabled/active, reports `mode=execute`, uses shared feed discovery, Bot2-like strategy, Bot5 Chainstack RPC, and waits on BUSDT/BNB funding before buying.
- [x] 2026-06-27 adjust Bot5 buy broadcast to `OPEN_BROADCAST_DELAY_MS=19900` (T+19.900) while keeping `open_timed_exit` T+25 full exit, fast randomized T+24.5s to T+26.0s open-exit, and sell gas `0.15gwei`; event, dashboard, and public dashboard services were restarted and returned active.
- [x] 2026-06-27 compare Bot5 against current Bot2 production after the T+19.900 retune. Core selection/sell policy matches Bot2-like production; remaining launch gaps are funding (BUSDT and BNB), first live buy proof, first auto-sell proof, and lower broadcast RPC redundancy than Bot2.
- [x] 2026-06-27 open the lightweight cloud server SWAS firewall rule for Bot5 direct public dashboard `4246/TCP` with remark `42space-5-dashboard`. During verification the host accepted TCP but stopped responding at HTTP/SSH banner level, Cloud Assistant returned `ClientNotRunning`, so the instance was recovered with `swas-open reboot-instance`; after it returned `Running`, SSH, port 80, direct `4245`, and direct `4246` all returned healthy responses.
- [x] Fund Bot5 with at least the current `30U` BUSDT next-batch requirement plus BNB gas reserve before expecting it to buy. Production readback on 2026-07-11 shows about `31.01 BUSDT` and `0.0371 BNB`, wallet ready.
- [x] Record first live Bot5 same-event buy proof on CRCL. Bot5 bought the three planned `5U` outcomes in the first T+20 block, txIndex `5`, rank `4` overall / `3` in the target block; 48Club's `0.001 BNB` tip mined at txIndex `6`, public fallback was absent, and nonce continuity held. Auto-sell started but exposed the shared fast-exit Builder/pause defect; RPC fallback later cleared all positions.

## Phase 10: Shared Strict Builder Runtime

- [x] Add a no-secret shared Builder env template plus Event and Dashboard systemd templates that load it before each profile env, preserving profile-local override precedence.
- [x] Move strict 19s/20s timing into the shared execution layer: `auto` maps T+18.x to the 19-second target and T+19.x to the 20-second target, pre-submits one second early with `minTimestamp=maxTimestamp`, forces `builder_then_fanout`, and leaves other action times RPC-only.
- [x] Add one strict Builder lane per wallet/target second, failed-early-submit tip nonce release, accepted-but-missed target expiry detection, dependent pre-sign clearing, and pending nonce resynchronization.
- [x] Deploy the shared env and both unit templates to `/etc/42space`, restart Bot1-Bot5 Event/Dashboard services outside a hot buy window, and verify effective process env plus profile timing readback. On 2026-07-10 all ten services returned `active/running` with `NRestarts=0`; Event and Dashboard processes loaded `BUILDER_BUNDLE_TIMING_MODE=auto`, the five Event processes loaded the shared `0.001 BNB`/`builder_then_fanout` policy, server-side mapping resolved Bot2/Bot3 to 19s, Bot1/Bot4 OpenRouter/Bot5 to 20s, and Bot4 BNB T+22 to Builder-disabled RPC-only. 48Club returned BSC chain ID `0x38`; Bot1-Bot4 funding remained ready, while already-unfunded Bot5 now requires `0.021421 BNB` including the shared tip reserve.
- [x] Capture the first strict 19-second receipt proof on Bot2 World Cup Winner volume. The result is a failed boundary proof: early submit T+18.006, accepted T+18.260 in `254ms`, payload declared `minTimestamp=maxTimestamp=open+19s`, but buy/tip landed in block `109095952` whose chain timestamp was open+18s. Public fallback was not used.
- [x] Run a value-limited not-before re-proof without buying a market position. Twenty-four heavy Lens-simulation canaries completed under a `0.005 BNB` cap; `1000ms` was T-1 in `3/3`, and hybrid Builder `300ms` plus public `150ms` was still T-1 in `1/6` despite all Builder responses succeeding.
- [x] 2026-07-10 emergency-pause shared Builder across Bot1-Bot5 after the Bot2 open+18s incident. Shared env now sets enabled/tip/timing to `0/0/legacy`; Bot2's three and Bot4's one explicit Builder rows were changed to `enabled=false`; all ten Event/Dashboard services restarted active with `NRestarts=0` before code work continued.
- [x] 2026-07-10 retain the non-overridable Builder kill-switch implementation but remove the unapproved fast-buy quote/min-out patch before production activation. The speed-first buy path remains prebuilt with `minOut=1`.
- [x] Restore Bot2 production after the stress run with Event/Dashboard active, profile Builder/tip `0/0`, public double-RPC execution, and no pending nonce. Do not activate the fixed-300ms prototype after its hybrid T-1 result.
- [x] Choose and deploy the enforceable production timing contract for explicit Bot2 Builder plans: verified `TimestampGuard`, `[guard,buy,tip]` nonce order, `T-500ms`/`100ms` private retries, nonce-gapped public buy propagation, and chain-time guard release. Builder/tip stay default-off and Bot1/Bot3/Bot4/Bot5 profile behavior is unchanged.
- [x] Complete production-region guard pressure proof: guarded Builder `10/12` target-second, `2/12` no-cost miss, `0` T-1; safe public fallback `4/4`, `0` T-1.
- [x] Configure Bot4 OpenRouter as the next strict 20-second production proof without changing Bot4's global transport or BNB/USDT route. The pre-open readiness/evidence path is complete and verifies T+19.500 private submit, T+20 atomic target, two Builders, Bot4-owned executor, no public fallback, exact outcomes, funds, allowance, and contiguous nonce.
- [x] After the next Bot4 OpenRouter opening, capture the strict T+20 production receipt proof. Tx `0xb1d3f3a42dd2fb1f00ac63f9fe2dba5f6400732cfc18624d8c91cac5386e57b8` landed in first T+20 block `109263919`, txIndex `1`, rank 1; 48Club's `0.001 BNB` tip mined, BlockRazor's same-nonce tip did not, and no public fallback occurred.
- [x] 2026-07-13 move only Bot4 OpenRouter's private submission from T+19.500 to T+19.300 by changing its plan-local preposition lead from `500ms` to `700ms`. Keep the T+20 target, dual Builder, `builder_only`, `positionFirst`/`noMerge`, `0.001 BNB` tip, `0.5gwei`, no public fallback, outcome sizing, sell policy, and BNB/USDT T+22 route unchanged; update Bot4 readiness/evidence expectations to match.
- [x] 2026-07-14 isolate and repair Bot4's strict Builder failure without changing another profile: deploy exact-second executor `0xC2B2F78C620228Ea8d1B2E155664ceBbc7212148`, add the `[T+20,T+21)` window and profile flag, reconcile a failed buy nonce before later profile-local transactions proceed, honor retry cooldown, and retain T+21 abandonment. Bot4-only services restarted healthy; exact historical-block calls, runtime identity/allowance, local/remote verification, readiness, contiguous nonce, and the next real-plan no-broadcast pre-sign pass.
- [ ] Verify the first post-repair Bot4 live opening and confirm profile isolation: exact T+20 receipt or clean terminal miss, no public fallback, contiguous nonce recovery before the independent T+22 BNB/USDT action, and no state/config change in Bot1/Bot2/Bot3/Bot5.
- [x] 2026-07-11 deploy profile-owned timed executors and max BUSDT approvals for Bot1 (`0xe40b5c53d2C6566219f85431a334BA9692d0d6E4`) and Bot5 (`0xe8e714bE74480788cABe470935DEb82236793bc3`). Runtime hashes, owner/operator, Router/collateral, allowances, and contiguous nonces pass.
- [x] 2026-07-11 enable Bot1/Bot5 strict dual Builder T+20 globally with T+19.500 private submission, `0.001 BNB`, `1.1gwei`, `positionFirst`/`noMerge`, authenticated BlockRazor plus 48Club, and no public fallback. Production Event/Dashboard readback is healthy with `NRestarts=0`.
- [x] Fix executor deployment read-after-write verification by polling bytecode and allowance after successful receipts; verify local and production test suites.
- [x] 2026-07-11 deploy Bot1 `bot3_like` strategy isolation and profile-local runtime health across Bot1-Bot5. Bot1 now matches Bot3 exact-score selection/sell semantics but retains T+20, its own executor and all profile data. All ten Event/Dashboard services are active with `NRestarts=0`, and all five heartbeat APIs report fresh cgroup-matching processes.
- [x] 2026-07-12 separate Bot1's sell policy from Bot3 while preserving the shared exact-score selector. Bot1 now uses `2.1gwei`, a `0.005 BNB` Builder tip, and randomized T+23-27 `100%` RPC-only exit; Bot3 remains `1.1gwei` with ten-hour `pre_start_exit` and its Builder tip was later raised to `0.003 BNB` on 2026-07-13. Only Bot1 Event/Dashboard restarted, both returned active with `NRestarts=0`, and Bot1's five existing long-dated positions remained under their event-level pre-start overrides with no cutover sell.
- [x] Capture Bot1's first live strict T+20 receipt/rank proof. France-Spain mined exactly at T+20 through BlockRazor with the paid `0.005 BNB` tip, ranked `4` overall / `3` in the target second, and used no public fallback; the adjacent-event nonce collision discovered after this buy is repaired in the production Bot1 process.
- [x] Capture Bot5's first live strict T+20 receipt/rank proof through CRCL. The buy target passed; the intended T+40-50 exit failed pre-broadcast because sell inherited buy-only Builder validation, then the stale pause delayed fallback to T+96. Patch sell to RPC-only, release pause on failure, and retain the next live timed-exit proof as the remaining operational evidence.
- [x] Complete Bot5's repaired timed-exit proof on CASHCAT, then pause the profile after rank `8` overall / `5` in T+20. The sell succeeded at target T+45.601 and final positions/nonces were clean, but four larger same-block Builder tips preceded Bot5. Disable only the Bot5 Event unit and retain its Dashboard; Bot2 remains active.
- [x] Re-enable Bot5 at operator request with a profile-local `0.006 BNB` tip. The unit is enabled/active and the dashboard reads the new tip, but BNB preflight correctly holds execution until balance exceeds the `0.009761 BNB` reserve; current shortfall is `0.00372535 BNB`.
- [x] Fund and complete Bot5 HOODRAT live proof with the `0.006 BNB` tip. The buy landed in T+20 at txIndex `5`, rank `5` overall / `3` in target second; BlockRazor's tip candidate mined, public buy fallback was absent, the randomized sell succeeded at T+40.702, all three balances are zero, and latest/pending nonce is `31/31`.
- [x] Pause Bot5 after the HOODRAT ranking review by stopping/disabling only its Event unit and retaining its Dashboard. Before the pause, Bot5 transferred its remaining BUSDT/BNB to Bot2 and reached balances `0/0`, nonce `33/33`; the pause itself sent no transaction. Retune only Bot2's Builder preposition lead from `500ms` to `700ms`; process-env and normalized backup comparison confirm every other Bot2 env value is unchanged, and its worker plus buy/sell monitors returned healthy with zero errors.
- [x] Retune Bot2 production to three Meme outcomes x `10U`, `30U` market/batch caps, buy gas `1.1gwei`, and `0.003 BNB` Builder tip. Back up profile env/runtime config, restart only Bot2 Event/Dashboard, verify normalized diffs contain only the requested selection/cap/tip fields, and complete production doctor with funding/allowance/Builder readiness plus `blockers=[]`.
- [x] Replace only Bot2's disabled profile-local Ankr HTTP/WSS credential and matching broadcast-list entry. Preserve Chainstack and every execution parameter; verify both protocols directly, confirm the live process loaded the new credential, and restore doctor broadcast readiness from `1/2` to `2/2`.
- [x] 2026-07-12 re-enable and align Bot5 for the recreated GRVT event. Bot5 moved from strict T+20/T+40-50/`0.006 BNB` to Bot2-matching strict T+19, T+18.300 private submit, `1.1gwei`, `0.003 BNB`, and randomized T+24.5-26 exit while retaining profile-local wallet/RPC/executor/state. Bot5 outcome 4-6 and Bot2 outcome 1-3 buys landed in the same T+19 block, both sold successfully in T+25 timestamp blocks, all balances are zero, final nonces are contiguous, and the executed plans are disabled.
- [x] 2026-07-12 move Bot2/Bot5 default randomized exit from T+24.5-26 to T+23.5-25 and fixed fallback from T+25 to T+24. Update both env and runtime layers so Bot5's old runtime `40s` fallback cannot override the profile env. Keep profile isolation and every buy parameter unchanged; restart and verify Event/Dashboard services with effective-config readback.
- [x] 2026-07-13 repair Bot1's profile-local fast-exit nonce ordering after a post-buy operator approval replaced the next event's pre-signed buy. Add pre-buy market approval, one-at-a-time buy pre-signing while fast exit is active, pending-buy priority checks, and a hard refusal to spend a nonce on post-buy approval. Deploy the verified shared source but restart only Bot1 Event; Bot3 remains on its existing process and reads the newly configured `0.003 BNB` tip from its profile env.
- [x] 2026-07-13 execute the two ARROW address-scoped profile splits. Bot2 owned outcomes 1-3 and Bot5 owned outcomes 4-6 at `10U` each. Both July 14 buys landed in T+19 and fully exited; on July 31 Bot2 bought in T+19 and sold successfully, while Bot5's pre-signed candidate remained unmined because an external wallet swap had converted `66 BUSDT` to USDC. Plan rows are disabled, Bot5 nonce stayed contiguous, and the profile is now BUSDT-blocked rather than Builder- or nonce-blocked.
- [x] 2026-07-13 advance only Bot2's sell window by two seconds to randomized T+21.5-23 with fixed T+22 fallback. Bot5 remains T+23.5-25/fixed T+24. Bot2's July 31 live sell was accepted at T+22.032 and confirmed successfully; effective env/runtime/dashboard values match, Event/Dashboard are active with `NRestarts=0`, and no buy timing, gas, tip, outcome, Builder, or nonce setting changed.
- [x] 2026-07-13 align Bot2 and Bot5 on randomized T+20.5-21.5 full exit with fixed T+21 fallback, without changing either profile's strict T+19 buy transport. Execute the Hakimi split with Bot2 outcomes 1-3 and Bot5 outcomes 4-6 at `10U` each; both buy receipts and tips land in T+19, both sells land in T+21, final positions are empty, nonces are contiguous, plans are disabled, and all four Event/Dashboard services remain healthy with zero restarts.
- [x] 2026-07-13 temporarily pause Bot2 and Bot5 automatic buying without disabling discovery or automatic selling. Stop both Event workers first to invalidate six prepared in-memory buys, back up each profile env and planned-buy file, set `MARKET_BUY_QUESTION_ALLOWLIST_REGEX=a^`, and disable every enabled planned row (`13` on Bot2, `1` on Bot5) with `pauseReason=manual-pause-bot2-bot5`. Restart only the two Event/Dashboard pairs; process env reads the hard gate, enabled plans and pending/prepared queues are `0`, no post-restart pre-sign or broadcast appears, both Event units are active with `NRestarts=0`, and both auto-sell monitors clear startup guard into `watching` with errors `0`.

- [x] 2026-07-14 deploy the Bot4-only OpenRouter outcome rotation without changing another profile. Back up the profile-local plan and validation scripts, replace outcomes with `Hy3 (free)=20U` and `MiMo - V2.5=10U`, move the existing `$0.0020/$0.0017` price thresholds to those outcomes, restart only Bot4 Event/Dashboard, and verify exact July 15-19 labels, July 15 plan tokenIds/stakes, isolated no-broadcast profile-signer pre-sign, readiness with zero failures, contiguous `120/120` nonce, matching local/remote hashes, and both services active with zero restarts.

## Phase 11: Shared Read-Only RPC Observer

- [x] Add the shared observer and report scripts, package self-tests, shadow systemd service, and five-minute report timer without changing Event/Dashboard/orderflow/address-watcher units.
- [x] Create `/etc/42space/profiles/42space-shared-observer.env` as root-only `600` with read-only HTTP/WSS roles only. The file contains no private key, wallet, webhook, Builder token, or broadcast endpoint and is not committed.
- [x] Deploy and verify the observer at lower CPU/IO priority with `MemoryMax=384M`, `NoNewPrivileges`, `ProtectHome`, and private temp storage. The service is enabled, active, has `NRestarts=0`, and its first production window recorded `274` HTTP RPC requests with zero errors.
- [x] Confirm coexistence does not degrade Bot3's live orderflow sidecar: during observer probes and production windows, Spain-vs-Belgium remained at roughly `85-92ms` average `eth_blockNumber` and `81-83ms` average `eth_getLogs`, `0` errors, matching the preceding baseline windows.
- [x] Add and deploy a restart-safe JSONL tail plus optional feed consumers for orderflow and address watchers. Both retain direct RPC as the default, and production profiles contain no source override; deploying the files did not restart any existing reader.
- [x] Complete no-execution production canaries for both consumer types. Orderflow preserved the real receipt-level threshold result with no sell, while address hydration reproduced the existing logged hit fields exactly with Feishu disabled. A checkpoint-only canary advanced its persisted block cursor with zero events or actions.
- [x] Reconcile live RPC endpoint fingerprints against the current Chainstack organization: all 17 runtime Chainstack endpoints matched platform nodes, including separate Bot1/Bot3 Event nodes and dedicated Bot1/Bot3 transaction-monitor nodes. Credentials and raw endpoints remain outside the repository.
- [x] Pause and disable the three address watcher services, then switch the shared observer to orderflow-only mode. Event, Dashboard, orderflow, and value-sell ownership remains profile-local; the report no longer requires address samples. The first two full windows each measured `2 RPC/min` with zero errors.
- [x] Verify the current auto-sell RPC boundary across Bot1-Bot5. Main workers use REST-only idle scans under current positions/strategies; chain RPC remains reserved for active stop-loss quotes, due exits, approvals, and execution. Add future-load telemetry plus an operator cache to the separate Bot3 value watcher.
- [ ] Complete the 24-hour machine-evaluated shadow gate and perform only a one-reader canary cutover after `ready=true`; execution RPCs, signer/nonce state, Builder paths, and sell calls stay profile-local.

## Update Rule

After each completed implementation step, change the matching checkbox from `[ ]` to `[x]`. Do not mark a server step complete until the server state has actually been verified.
