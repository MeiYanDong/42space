# Plan Outline

Goal: run a 42 Event Market bot that detects eligible new markets, buys at open, tracks positions, and gives a simple operator view.

Active stage: [07 Single Install Multi Profile Runtime](./plans/07-single-install-multi-profile-runtime.md)

| Stage | Plan | Status |
| --- | --- | --- |
| 01 | [Operating Contract](./plans/01-operating-contract.md) | Running, keep verified |
| 02 | [Discovery And Filtering](./plans/02-discovery-filtering.md) | Active, configurable display/notification filters independent of buy allowlists, plus a gated FIFA/Sports exact-score selector for Bot1/Bot3 and a first-observation Meme FDV/price-range selector for Bot2/Bot5. Planned buys remain authoritative. |
| 03 | [Execution And Risk](./plans/03-execution-risk.md) | Running, speed-first pre-signed buys and profile-local dual-Builder atomic bundles. Bot2/Bot3/Bot5 use strict T+19; Bot1 uses strict T+20. Bot1 uses `2.1gwei` buy gas, a `0.005 BNB` winning-Builder tip, and randomized RPC-only full exit at T+23-27. Bot2 and Bot5 start private retries `700ms` before T+19 with a `0.003 BNB` tip, `1.1gwei` buy gas, and no-public-fallback path. Both now exit at randomized T+20.5-21.5 with fixed T+21 fallback. Fast timed sells are RPC-only; their market approval is mined before buy pre-signing, and profiles that sell between nearby buys serialize the nonce lane so a sell cannot invalidate a future buy. Bot4 OpenRouter uses an exact-second T+20 executor with a non-zero `[T+20,T+21)` Builder window and a `700ms` private preposition lead; Bot4 BNB/USDT remains RPC-only at T+22. Address alerts are paused; all five orderflow readers now consume the Chainstack-only shared feed while retaining profile-local validation and sell execution. A temporary one-second shared HTTP audit preserves the old worst-case discovery cadence during the production burn-in. |
| 04 | [Dashboard And Ops](./plans/04-dashboard-ops.md) | Running, profile-aware holdings/PnL/orderflow/address monitoring and four-stage Strategy views. A 5s profile-local heartbeat and lightweight 10s UI refresh now report worker, auto-buy, and auto-sell state separately, including scan evidence, buy protection, errors, and circuit pauses. |
| 05 | [Multi Region Execution](./plans/05-multi-region-execution.md) | Active, US West primary live and Hong Kong standby |
| 06 | [Multi Bot Profile Merge](./plans/06-multi-bot-profile-merge.md) | Complete, `MeiYanDong/42space` is the canonical codebase and contains the `42space-2` safety fixes |
| 07 | [Single Install Multi Profile Runtime](./plans/07-single-install-multi-profile-runtime.md) | Active, run isolated profiles from one `/opt/42space` install with separate wallets, env, data, services, dashboards, executors, nonce state, and runtime-health files. Bot1 mirrors Bot3's exact-score selection but keeps its own T+20 executor and randomized T+23-27 full exit; Bot3 remains T+19 with ten-hour pre-start exit. Bot5 remains independently enabled with Bot2-matching T+19 buy transport, `1.1gwei` gas, `0.003 BNB` tip, and the shared T+20.5-21.5 exit window. The Hakimi split supplied a funded end-to-end proof for both profiles. |
| 08 | [Event Intelligence](./plans/08-event-intelligence.md) | Running, first-observed market intelligence sidecar uses local TwitterAPI.io skill provider when configured; OpenRouter Python usage-winner templates classify as daily fixed templates; Bot2/Bot3/Bot5 Feishu follows configurable display filters and covers all non-filtered events, with `价格` now hiding only BTC Price while non-BTC Meme price-range and FDV price-resolution events remain notifiable; Bot2/Bot5 buy focus still defaults only Meme and Binance strong |

Current profile-specific addition:

- The unguarded 24-round test and Bot2 legacy timestamp-guard stress remain historical proof that timing offsets alone cannot enforce not-before. Bot2 production has now moved to its own atomic timed executor at `0x961E5954B4e1812d87955a2b7A614cCc8B1E7Cc1`; the old separate-nonce Guard is disabled, while dual Builder, strict T+19 targeting, `0.003 BNB` tip, and `1.1gwei` buy gas are enabled globally for Bot2.
- Bot3 uses the profile-local atomic timed executor at `0x14e3f8172388FDDb85918792a63294F4C740343B`. It submits `[timedBuy, providerTip]` to 48Club and BlockRazor, signs both provider tips at the same nonce so at most one can mine, and preserves `positionFirst`/`noMerge`. CASHCAT exposed the invalid equal timestamp payload and overwritten `builder_only`; after those fixes, JUGGERNAUT and HOODRAT both landed through BlockRazor in T+19 at market rank 1 with no public fallback. Bot3's current global default is `0.003 BNB` tip and `1.1gwei` buy gas; per-event overrides remain authoritative.
- Bot4 OpenRouter uses exact-second executor `0xC2B2F78C620228Ea8d1B2E155664ceBbc7212148`. The plan is `builder_only`, submits privately to 48Club and authenticated BlockRazor at T+19.300, declares `minTimestamp=T+20` and `maxTimestamp=T+21`, pays the winning Builder `0.001 BNB`, and has no public RPC fallback. The contract permits execution only when `block.timestamp == T+20`; a one-second-late buy is abandoned. Bot4's BNB/USDT plan remains an independent RPC-only T+22 action. Planned buys can override stake by outcome while retaining one default stake; Bot4 OpenRouter uses `Hy3 (free)=20U` and the default `MiMo - V2.5=10U`.
- Bot4 supplied the first strict T+20 live proof on 2026-07-11. OpenRouter buy tx `0xb1d3f3a42dd2fb1f00ac63f9fe2dba5f6400732cfc18624d8c91cac5386e57b8` landed in the first T+20 block `109263919` at txIndex `1` and ranked `1` overall. 48Club accepted in `239ms` and received the single mined `0.001 BNB` tip; BlockRazor timed out, its same-nonce tip did not mine, and no public fallback occurred.
- The 2026-07-14 OpenRouter miss triggered the deferred exact-second Roadmap: both Builders rejected the old lower-bound transaction during pre-target simulation, then the old recovery path exposed a missing buy nonce to the BNB lane and caused `gapped-nonce tx`. The exact executor, non-zero Builder window, target-expiry nonce reconciliation, retry cooldown, and strict late-buy abandonment are deployed. The next live Bot4 opening remains the receipt-level acceptance test.
- Bot1 owns executor `0xe40b5c53d2C6566219f85431a334BA9692d0d6E4`; Bot5 owns `0xe8e714bE74480788cABe470935DEb82236793bc3`. Bot1 retains strict T+20 with T+19.500 private submission, `2.1gwei`, and a `0.005 BNB` tip. Bot5 now uses strict T+19 with T+18.300 private submission, `1.1gwei`, a `0.003 BNB` tip, `positionFirst`/`noMerge`, and no public fallback.
- HOODRAT is historical proof of the earlier asymmetric profile policy: Bot2 ranked first in T+19 and sold at T+25.079, while Bot5 ranked `5` overall / `3` in T+20 and sold at T+40.702. Both used BlockRazor-winning bundles, skipped public buy fallback, and ended with zero balances and contiguous nonces. GRVT and ARROW later superseded those timing/tip settings.
- After HOODRAT, Bot5 temporarily transferred its BUSDT/BNB to Bot2 and its Event unit was stopped. It was subsequently funded and explicitly re-enabled for GRVT/ARROW with strict T+19, `0.003 BNB`, and `1.1gwei`. Its current blocker is the later external BUSDT-to-USDC wallet swap, not a disabled service.
- Bot2 now buys three Meme outcomes at `10U` each: the locked metric bucket plus one adjacent outcome on each side, with middle-three fallback and `30U` market/batch caps. Its buy gas remains `1.1gwei`; the global Builder tip is `0.003 BNB`.
- GRVT was cancelled and recreated before open on 2026-07-12. The retired market `0xFBB0BacCaB3D847e9e38cC55D1E4d986917be302` was marked resolved; exact address-only plans targeted replacement `0xd2837E2FA107b5bF9F7E3628a67266bE318CBbB4`. Bot2 bought outcomes 1-3 and Bot5 bought outcomes 4-6, each `3 x 10U`, in the same T+19 block at txIndex `1/3`; their winning `0.003 BNB` tips followed at txIndex `2/4`. RPC-only exits succeeded in T+25 blocks, all six balances are zero, final nonces are `863/863` and `39/39`, and both executed plans are disabled.
- Two 2026-07-13 ARROW markets exercised the same split plans. On July 14, Bot2 outcomes 1-3 and Bot5 outcomes 4-6 both bought in the T+19 block and later fully exited. On July 31, Bot2 again bought outcomes 1-3 in T+19 and its newly advanced fast exit was first accepted at T+22.032; Bot5 outcomes 4-6 did not buy because an external nonce-43 wallet transaction had converted `66 BUSDT` to USDC before pre-signing. All four one-off plans are disabled; Bot5 needs BUSDT restored before another buy can execute.
- Bot1 sets `EVENT_PROFILE_ROLE=bot3_like`: ordinary Meme/Binance auto-buy is disabled, FIFA/Sports exact-score uses Bot3's lower-price win-side five-score selector at `10U` per outcome, and planned buys remain first priority. Its profile default is now `open_timed_exit`: sell `100%` through the RPC-only fast-exit path at a cryptographically random T+23-27 target, with fixed T+25 as normal-monitor fallback, `0.15gwei` sell gas, and stop loss disabled. The market operator is pre-approved before buy pre-signing; only the earliest buy is pre-signed while a faster intervening sell owns the next nonce. Existing Runner-Up and 3rd Place holdings retain their event-level ten-hour `pre_start_exit` overrides. Bot1 alone keeps the T+20 Builder target.
- Bot1-Bot5 workers write profile-local `runtime-health.json` every 5 seconds. `/api/automation-status` validates the Node PID against the systemd cgroup and reports worker, auto-buy, and auto-sell health separately; the Strategy UI refreshes this lightweight state every 10 seconds.
- Bot1 and Bot3 run separate orderflow-trigger sell monitors for long-dated World Cup country baskets. Runner-Up and 3rd Place now trigger an immediate matched-outcome sell on external same-market buys of at least `150U` on a watched held outcome. Both profiles use dedicated uncommitted orderflow read RPC env files; read-side consolidation must preserve profile-local trigger and sell execution semantics.
- Bot3's optional 48Club builder path has one live proof on Runner-Up / Spain. The 2026-07-09 test bought `Spain` for `1U` and submitted a `0.001 BNB` builder tip bundle; public fanout landed the buy first, while the builder tip tx did not mine. Treat concurrent builder mode as "submit bundle concurrently and measure acceptance", not as guaranteed builder-paid inclusion.
- The optional 48Club builder path now supports `builder_only` in addition to the default concurrent mode. Bot2 ASML verified the builder-paid path on 2026-07-09 with a per-planned-buy override: ASML bought three `10U` outcomes through `presigned_builder_bundle_only`, and the `0.002 BNB` tip tx mined immediately after the buy in the same block.
- The optional `builder_then_fanout` path remains available as an explicit per-plan override: submit the private buy+tip bundle first, wait a short configured window, then fall back to the profile's public RPCs if the Builder has not accepted. Bot2's current global default is the stricter atomic `builder_only` path, so this fallback is not used unless a record explicitly overrides the mode.
- Builder-enabled pre-signed buys now pre-sign the builder tip and prebuild the bundle skeleton during the T-60s hot window. Broadcast-time builder submission defaults to skipping the optional max-block RPC lookup and persists segment-level builder latency fields for the next live audit.
- Bot2's second Builder live proof exposed the exact T+18 cause. The plan's public action was T+18.850, but the old target preset additionally moved the private request to T+18.006; 48Club accepted it at T+18.260 and included buy/tip at txIndex `1/2` in its next block. `minTimestamp=open+19s` did not act as a lower bound, while `maxTimestamp=open+19s` still considered the bundle valid. The operator immediately sold all six outcomes and disabled the plan.
- Fast exact-in buys remain speed-first with prebuilt calldata and `minOut=1`; the rejected size-aware quote/min-out patch was never activated in production. This intentionally avoids adding Lens RPC calls or re-signing to the opening hot path.
- Gas ledger and dashboard PnL now count mined builder tips as total execution cost: the tip transaction's normal gas plus the BNB transfer value via `extraFeeBnb` / `totalFeeBnb`, instead of showing only the 21000-gas transfer fee.
- Bot3 also runs a `Spain vs Belgium` exact-score orderflow-trigger sell monitor on market `0x94FA631F5A8d830919db6d5B1571e438f0222Fb0`; external same-market buys of at least `50U` on a watched held outcome trigger an immediate matched-outcome sell.
- Bot1 now also runs independent address transaction watchers for `0x96FDe227f3863812464dC1320B505016837a3650`, `0x1Bc7dF2AA0DBE1a489A7205f2D1fF92C3d51A80b`, and `0x51349f0B9b8C21A34781273e37F16B0233239C41`. Any direct BSC transaction or token transfer involving a watched address sends a Feishu alert with a 10-minute cooldown; 42 trades are enriched from receipt logs with a REST-by-hash fallback so cards show buy/sell, event, outcome, actual USDT, position size, and BNB input. The 4242 dashboard keeps the same address-monitoring readback.
- Bot3 now has an independent single-outcome value-trigger sell monitor for `Spain vs Belgium` exact score `ESP 2-1 BEL` on market `0x94FA631F5A8d830919db6d5B1571e438f0222Fb0`; it watches the live sell quote for tokenId `2048` and sells 100% once the quote reaches at least `99U`.
- Bot3 bought `France vs. Sweden - Handicap` / `France −1.5` at `10U` with per-record `1.1gwei`; the same kickoff anchor drives the 10h pre-start auto-sell at `2026-06-30T11:00:00Z`.
- Bot3 has a pending `Argentina vs Switzerland - Moneyline` planned buy for all three outcomes at `20U` each, per-record `T+19.850s`, buy gas `3.1gwei`, and the normal 10h pre-start auto-sell.
- Bot3 automatic FIFA/Sports exact-score buys now share the planned-buy `pre_start_exit` timing semantics: planned `kickoffAt` remains highest priority, and unplanned automatic exact-score positions use the exact-score market `endDate` as the match anchor instead of market-open `startDate`; side markets remain excluded from this fallback.
- Bot3 `World Cup: Final Matchup` market `0x951B04c71Ff284C35e7C46554CbD1D7C9FA8F44b` attempted the first legacy separate-nonce guarded Builder buy at `T+20`, `2.1gwei`, and `0.002 BNB` tip. The buy and tip did not mine; only the Guard mined because wallet-budget Gas had consumed the BNB that should have remained reserved for Guard/tip costs. The expired plan is disabled. Funding preflight now applies each planned market's final Builder config, wallet-budget signing subtracts Guard/tip reserves, and pre-sign enforces maximum signed cost against pending BNB. The legacy Bot3 Guard is disabled and superseded by the atomic timed executor.

Update this file after code changes that alter scope, stage, or priority.
