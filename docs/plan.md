# Plan Outline

Goal: run a 42 Event Market bot that detects eligible new markets, buys at open, tracks positions, and gives a simple operator view.

Active stage: [07 Single Install Multi Profile Runtime](./plans/07-single-install-multi-profile-runtime.md)

| Stage | Plan | Status |
| --- | --- | --- |
| 01 | [Operating Contract](./plans/01-operating-contract.md) | Running, keep verified |
| 02 | [Discovery And Filtering](./plans/02-discovery-filtering.md) | Active, speed-first fallback, decision log, Bot2/Bot3 configurable display/notification filters with default display for non-filtered events independent of strategy question allowlists, Bot3-only gated FIFA/Sports exact-score lowest-price-tier auto-buy selector added with planned-buy priority, Bot4 daily-template-only display plus buy gating for OpenRouter Python and BNB/USDT Daily Futures Volume while Hermes OpenRouter token usage remains display-only, Bot2 Meme/Binance default-follow buy filter kept profile-local, explicit named-outcome one-off selection, profile-local planned buys, follow-rule event library, and REST known-future eligible recheck added |
| 03 | [Execution And Risk](./plans/03-execution-risk.md) | Running, middle-three/named/planned buys, Bot1 exact-score execution retuned to fixed T+19.850 with `2.2gwei` and single-Ankr buy broadcast after Argentina vs Cabo Verde ranked behind earlier 18s/19s buys plus a lower-gas same-20s tx; Colombia vs Ghana is the next funded Bot1 single-Ankr test and Bot1 buy-rank timer is enabled, Bot2 buy defaults remain T+18.840 with `1.1gwei`, automatic buying is restored for the Bot2 Meme/Binance strong middle-three `10U` path while old planned-buy rows stay disabled, stale future seen records no longer block restored automatic buys, and auto-sell stays active but Bot2 is currently waiting for BUSDT top-up, Bot3 remains T+18.850 with `1.1gwei` buy gas and now has a Bot3-only FIFA/Sports exact-score auto-buy path that selects the lowest price win-side tier at `10U` per outcome behind an independent switch, Bot4 single pre-signed daily-template plans now buy OpenRouter Python `DeepSeek V4 Flash`, `Owl Alpha`, and `Hy3 preview` at T+19.900 with `0.5gwei` plus BNB/USDT Daily Futures Volume at T+22.000 with `0.15gwei` on the secondary Bot4 RPC; Hermes OpenRouter token usage buying is paused after the template/outcome change, Bot4 active batch cap is `50U`, Bot4 stop-loss clear-out remains paused, and Bot4 default sell is now Beijing 19:00 timed full exit for post-cutover daily buys unless a planned buy explicitly holds; planned per-market buy timing/gas/RPC, planned hold-to-settlement/pre-start auto-sell overrides including receipt-triggered fast open-exit gating, Bot2 fast randomized open-exit path for profile-scoped T+24.5s to T+26.0s full sells, retained-settlement sell production config verified, auto-sell return quotes are strategy-gated to reduce idle RPC load, auto-sell starts before buy-funding wait with shared tx lock, buy-priority auto-sell guard, receipt evidence, profile-local Gas ledger/backfill with live USDT valuation, fast-exit approval coverage, and priced tx de-duplication, realtime premium watcher, and shared discovery feed mode active |
| 04 | [Dashboard And Ops](./plans/04-dashboard-ops.md) | Running, follow-controlled upcoming/project pages added; project holdings layout deduplicated with expandable active/history option rows; dashboard PnL now shows Gas cost and net PnL from profile-local gas ledgers; Bot2 and Bot3 show/notify all non-filtered events with checkbox-controlled Price, daily-template, total-goals, and goal-differential display filters even when buy allowlists block execution; Bot4 can show only daily fixed templates; production dashboards can disable startup/background overview refresh to avoid multi-profile status subprocess bursts |
| 05 | [Multi Region Execution](./plans/05-multi-region-execution.md) | Active, US West primary live and Hong Kong standby |
| 06 | [Multi Bot Profile Merge](./plans/06-multi-bot-profile-merge.md) | Complete, `MeiYanDong/42space` is the canonical codebase and contains the `42space-2` safety fixes |
| 07 | [Single Install Multi Profile Runtime](./plans/07-single-install-multi-profile-runtime.md) | Active, run Bot1, Bot2, Bot3, Bot4, and staged Bot5 profiles from one `/opt/42space` program install with isolated env, data, services, dashboards, and execution RPCs; Bot3 is active with T+18.850 planned-buy timing, `1.1gwei` default buy gas, `pre_start_exit` default auto-sell, stop-loss disabled, Mexico vs Ecuador, Australia vs Egypt, Argentina vs Cabo Verde, one-off World Cup 3rd Place and Runner-Up country baskets, and the France vs. Sweden Handicap one-off bought successfully; Bot3 website-bought France vs Sweden exact-score positions now retain `10%`; long-dated World Cup baskets are explicitly `hold_to_settlement` and not in the 10h pre-start auto-sell path; Bot4 direct 4245 profile now uses staggered single planned buys instead of bundle buys; Bot5 mirrors Bot2-like selection/sell policy with T+19.900 buy broadcast, dedicated secrets/state, direct 4246 plus port-80 public dashboards, and active waiting-for-funds worker; the US West host was recovered after OOM and now uses swap with `vm.swappiness=20` |
| 08 | [Event Intelligence](./plans/08-event-intelligence.md) | Running, first-observed market intelligence sidecar uses local TwitterAPI.io skill provider when configured; OpenRouter Python usage-winner templates classify as daily fixed templates; Bot2/Bot3/Bot5 Feishu follows configurable display filters and covers all non-filtered events, with `价格` now hiding only BTC Price while non-BTC Meme price-range events remain notifiable; Bot2/Bot5 buy focus still defaults only Meme and Binance strong |

Current profile-specific addition:

- Bot3 runs a separate Runner-Up orderflow-trigger sell monitor for the four Bot3-held `2026 FIFA World Cup Runner-Up?` outcomes; confirmed external same-market buys of at least `200U` on a watched outcome trigger an immediate matched-outcome sell, with a profile-local dedicated read RPC supplied by an uncommitted systemd env file.
- Bot3 bought `France vs. Sweden - Handicap` / `France −1.5` at `10U` with per-record `1.1gwei`; the same kickoff anchor drives the 10h pre-start auto-sell at `2026-06-30T11:00:00Z`.
- Bot3 automatic FIFA/Sports exact-score buys now share the planned-buy `pre_start_exit` timing semantics: planned `kickoffAt` remains highest priority, and unplanned automatic exact-score positions use the exact-score market `endDate` as the match anchor instead of market-open `startDate`; side markets remain excluded from this fallback.

Update this file after code changes that alter scope, stage, or priority.
